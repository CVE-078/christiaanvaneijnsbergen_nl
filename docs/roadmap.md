# Pulse, Product Roadmap

## Product vision & positioning

**Pulse is an adaptive strength coach that works on any device.** Build a plan, track your lifts, and it adapts when life gets in the way so you keep progressing for years, not weeks. The periodization, adherence, ramp-back, and deload logic are the mechanism behind that promise, not the pitch.

**The moat:**
- Completion-paced programs (you advance by finishing cycles, not by the calendar).
- Ramp-back weeks after a layoff, auto-deload on stalled lifts, adherence awareness (on-track / behind / lapsed).
- Deterministic, equipment-aware generation that is genuinely strong for home-gym, dumbbell-only, and travel setups.
- Cross-device and offline-first (any browser, no ecosystem lock-in), the gap Apple-only apps like Liftin cannot fill.

**Target user:** the training-first lifter (strength and hypertrophy), at home or in a gym, who wants structure without a spreadsheet. Not a social network, not a running / CrossFit / rehab app, not an AI chatbot. Expand to adjacent users (for example movement restrictions) through the generation engine, never by diluting the core.

**Design principle:** simple surface, ignorable intelligence. The app carries a lot of smarts; the UI stays calm and lets users ignore most of it until it matters.

**Not building** (focus guards, all evaluated): AI chatbot, LLM-generated workouts, heavy fatigue modeling, goal prediction, native apps and wearables until real traction (the native *how* is settled and the *whether* is held open under "Native client, deferred direction" below).

**Social -- hard-out permanently:** global leaderboards, competitive rankings, public follower counts, feed-first home screen (Train is always the home screen), viral challenges, engagement farming mechanics, likes or reactions on body or progress photos, network/comparison achievements (self-referential milestones -- your own PRs / streaks / volume bests -- are on-vision; only the comparative kind stays hard-out), DMs or in-app messaging, creator economy or paid content. (Achievements split into two lanes: self-referential milestones are post-launch-OK and on-vision; only the network/social kind stays hard-out, since it pulls toward the social-network anti-goal.)

**Social -- deferred, not ruled out:** lightweight training-community features (routine link sharing, friend activity, private accountability groups, public routine pages) are a documented long-term direction, not a near-term build. The distinction is "not social-first", not "not social". See "Social direction, deferred" below.

---

## Status & next session (2026-06-08)

**In progress:** (none).

**In review (on a branch, not yet merged):** Movement restrictions (Tier 2 #5), on `feature/movement-restrictions`.

**Where things stand:** Generation-engine **Phase 0 is complete**, the muscle bridge (`muscleMap.ts`), the 6-field exercise-metadata seed across 94 exercises, the movement-pattern correction, the fractional per-muscle volume wiring, and now **session-linked logging + the unified `DecisionEvent` log** (`session_id` + `workout_date` on `set_logs`; auto-deload / progression captured at set-save through the pure `decisionForExercise`; ramp-back dual-written) all shipped. The pre-public **launch floor** advanced too: offline-queue cross-user safety + poison-pill/dead-letter hardening, decimal-comma parsing, rest-timer-survives-lock, and the live RLS audit are done. A full code/security audit (2026-06-06) came back clean except two items, both now fixed. The first payoff of the decision log also shipped: the **Coach Decision Timeline** (#10 increment 1) surfaces ramp-back / deload / progression in the desktop right rail, inline on Train, and a full week-grouped overlay. Two more Tier 1 wins then shipped together: **Lighten this week** (#11, the manual "go easier" deload) and the **priority-tilt "why" line** (#1's non-visual half), and **#1 is now fully shipped** with per-muscle weekly volume filling the desktop right rail (`RailMuscleVolume`). The **guided set-row polish (#2)** then shipped: single-active focus, grouped inputs, a decluttered logged row, plus the opportunistic a11y Q8 overlay fix. **Positioning copy (#3)** landed on the two live surfaces (login subtitle + onboarding intro), which **completes the Tier 1 surfacing/polish layer** (#1, #2, #3 all done). The two "shape your program" setup choices are now both real steps in `RoutineSetupFlow`: the start-date prompt and the **program-length step** (Prompt 2), alongside a **gender-optional setup polish** (a first-class "Prefer not to say" choice). The **guided set-row + plate-calculator polish** (live-test section B) then shipped: prescription chips (rep target + RIR) by the inputs, a one-tap "Same as last", "Set N / total", a first-time start-light cue, "+ Add drop" moved into a per-set ⋯ overflow, and a reworked **plate calculator** (loaded-bar visual, labeled, gated to barbell / plate-loaded lifts via `isPlateLoaded` so it no longer leaks onto dumbbell / cable / machine, closes on save). Most of the **live-test screen tweaks (section C)** then shipped: a green check (not "Done" text) on a completed Train exercise, Routines as the first Library tab, and a per-exercise how-to-perform icon on Plan. The remaining live-test polish then shipped: the Plan **rationale split** into fact chips + prose, **generator transparency (A.3)** as per-exercise resolved-equipment chips on Plan (so an equipment mis-tag is visible, not just in the seed), and **clearer supersets** (header parity + an alternate-then-rest cue on both Train and guided). The **perceived-performance pass** then shipped: tab navigation paints a skeleton instantly via a `(protected)/loading.tsx` Suspense boundary (the shell + SWR provider stay mounted, so only the content area swaps) instead of the old page freezing during the server round-trip; sign-out gives immediate feedback (a shared `useSignOut` hook flips a pending state on both shells); and the Templates tab shows skeleton rows on first load. The **train header + weekday-strip cleanup** then shipped: the Train header overflow from re-open / clear-day fixed by splitting it into an identity row + action band (constant-height band, "Week N" stepper, "Complete" done-state), the weekday strip redesigned to a glyph + selected-day summary line, the today marker scoped to the current program week, a contextual "Same as set N" quick-fill in guided set rows, plus a viewport-scaled guided exercise title and top-anchored toasts in guided mode. The **finish / debrief screen rework** then shipped (merged): the post-workout screen became a debrief (session RPE + note) with a coach summary from the decision log / PRs / per-muscle volume / tonnage and real PNG export. The first **Tier 2** work then shipped: the **training style generation input** (#4, merged as **#85**), the first generation-input refinement and the start of the personalization moat. Now in review, the second #4 input: the **variety preference generation input** (see the In-review line above). A **UI polish pass** then shipped (2026-06-07, `feature/ui-polish-superset-library-plan-swap`): the superset card header now stacks exercise names (primary full-width, secondary dimmed below with `+`) instead of a single truncated line, with the alternating cue moved inside the expanded body; the Library routine row is now two-line (full name on line 1, sets x reps + all actions on line 2); and the Plan page gained a permanent Swap button per exercise (`swapRoutineExercisePermanently` server action, rewrites `exercise_id` on the `routine_exercises` row). **GQ1** then shipped (`feature/gq1-exercise-ordering`): three focused improvements to `generation.ts` -- (1) a post-select **tier sort** (squat/hinge compounds first, then big multi-joint, then isolation, then calf/core) so sessions read coach-standard; (2) a **heavy-compound dedup guard** (`HEAVY_DEDUP_PATTERNS` = hinge + squat) that caps each at one compound per session and blocks backfill from adding a second deadlift variant, with a thin-pool fallback that relaxes the cap after a full round yields nothing; (3) **backfill prefers uncovered slots** (stable sort: patterns with zero entries before those with one) so diversity comes before depth. No schema change. +16 tests. **Loading lean** then shipped (`feature/loading-lean`, 2026-06-07): the third #4 generation input. `LoadingPreference` ('barbell' / 'dumbbell' / 'machine' / 'cable') secondary-sorts candidates within `byPattern` so preferred-equipment exercises float first, while fresh-preference still wins (a fresh non-preferred beats a used preferred). null/undefined is byte-identical to base. Persisted on `profiles.loading_lean`, optional `RoutineSetupFlow` 'loading' step (toggle-to-deselect, "Skip" when nothing chosen), `TemplatesTab` opts out. +8 tests. **GQ2** then shipped (`feature/gq2-fatigue-backfill`, merged **#92**): `fatigue?: number` wired into `ExerciseMeta` and the pool query, used as a secondary sort in `byPattern` (lower fatigue preferred; loading lean still trumps it; fresh still beats used regardless). Undefined fatigue = neutral midpoint (3). No migration. +4 tests. **GQ3** then shipped (2026-06-08, `feature/gq3-role-aware-ranking`): five coach-quality fixes layered onto the same `byPattern` engine, an **anchor-aware fatigue sort** (the six `COMPOUND_ANCHOR_PATTERNS` now prefer the higher-fatigue, better anchor when fresh, e.g. Bench Press over Push-Up, inverting GQ2's tiebreak for anchors only), a **substitution-class cross-session dedup** (`usedSubstitutionClasses`, soft, mirrors `used`), a **unilateral cap** (at most one `unilateral: true` pick per session, hard block mirroring `HEAVY_DEDUP_PATTERNS` / `heavyPatternFilled` / `relaxedHeavyCap`), **Front Raise suppression** (new `front_delt_isolation` substitution class, soft-deprioritized after a vertical press fills the session), plus a Smith Machine Bench Press equipment re-tag (`{machines, bench}`, migration `2026-06-08-gq3-exercise-data-corrections.sql`). `ExerciseMeta` + pool query gain `substitution_class` and `unilateral`. +6 tests. **Movement restrictions (#5)** then shipped (`feature/movement-restrictions`, 2026-06-08, in review): the first injury-aware generation input. A new `RestrictionFlag` ('knee' / 'lower_back' / 'shoulder' / 'wrist') + per-exercise `contraindications` metadata drive a **hard pool filter** (`isContraindicated`) beside `hasEquipment` in `generateRoutine`, never relaxed (a flagged lift is never re-added to fill a slot; the existing backfill covers an emptied pattern from safe patterns). Empty/undefined restrictions are byte-identical to base (golden identity test). Persisted on `profiles.movement_restrictions` (migration), tagged exercises seeded against the corrected catalog (migration), threaded through the generate action with a **safety-aware write-back** (an absent param never clears a stored flag; only an explicit array persists). Collected in an optional `RoutineSetupFlow` step (multi-select, `TemplatesTab` opts out) and a standing **Profile editor**. v1 is a pure subtractive filter (no preferred-substitute front-loading; future hook is `substitution_class`). +8 tests. Suite: typecheck clean, **887 tests**. Strategy + spec stay frozen in the two design docs (`…-00-30-19-generation-engine-v2-audit.md` and `…-00-54-52-phase0-source-material.md`).

**Next up, in order:**
1. **Validation block** (the highest-leverage item now that Tier 1 #1, #2, and #3 have all shipped): train on it with intent and run the deliberate missed-week test (skip a scheduled week to fire ramp-back), now that the `DecisionEvent` log records the adaptive events as first-class rows for the gate to query. Review query ready at `docs/queries/decision-events-review.sql` (newest-first, today-only, and ramp-back-only variants).
2. **Continue Phase 1 / Tier 2**, personalization in generation. Training style (#4, merged #85), variety preference (#4, merged #86), loading lean (#4, merged #91), GQ2 (merged #92), GQ3 (merged #93), and now **movement restrictions (#5)** (`feature/movement-restrictions`, in review) all shipped. Next: **equipment profiles** (#6), save multiple equipment sets (Home / Gym / Travel) and switch/regenerate.

**Validation, first live test (2026-06-06):** the validation block is underway, the first real workout surfaced a batch of UX/bug findings, captured + triaged in the **"Validation block, live-test findings"** section directly below (bugs · set-row & plate-calc polish · screen tweaks · finish-screen & exercise-media features). Pick the build order from there; the launch-quality bugs (bodyweight logging, end-of-session teardown, routine-scoped decisions, week-1 anchoring, equipment-filter leak) are the immediate queue.

**Launch-readiness track (gated behind validation, not due while private validation runs):** the **auth + billing lifecycle** is the long pole, then the **a11y audit**, both in the Launch floor section below.

Full detail and rationale live in the phased plan under "Roadmap (re-tiered)" below and the two 2026-06-06 design docs.

---

## Validation block, live-test findings (2026-06-06)

First real workout of the validation block (the user training with intent, returning after a layoff, so the ramp-back case is live too). One session surfaced a batch of UX and correctness findings, triaged below with the same discipline as the launch-floor triage: fix the launch-quality bugs on a branch (tests green), land the polish as a guided set-row follow-up, scope the two features properly. Build order to be picked with the user; a recommendation closes the section. Most line items name a likely code site as a starting point, not a verified root cause.

**A. Bugs (correctness, launch-quality)**

1. ~~**Bodyweight exercises require a weight.**~~ **SHIPPED 2026-06-06 (commit `d89a9ac`).** Push-Up can't be logged without a weight, so it went in at 0.5 kg, which pollutes the PR map and volume. Fixed: `isBodyweight` (empty `equipment`) makes the weight field optional (blank = pure bodyweight, a number = added load shown as `+10 kg`), a logged set reads "Bodyweight × N", progression is rep-based, and the plateau/deload detectors are skipped for bodyweight (0 e1RM by construction). The routine query now also loads `equipment` (it didn't, so the client couldn't tell bodyweight apart). Deferred: rep-based bodyweight PRs (e1RM-based PRs need a weight, so bodyweight lifts don't PR yet). +4 tests.
2. ~~**Equipment filter leaks bench-dependent exercises.**~~ **SHIPPED 2026-06-06 (migration applied in Supabase).** Root cause confirmed: `hasEquipment` is correct (bench IS a modeled `EquipmentKey`, and the selector has a separate "Weight bench" checkbox); the data was wrong. The 2026-06-04 seed put **Chest Fly** in the dumbbells-only group, but a dumbbell fly is done lying on a bench, so it leaked into no-bench setups. Migration `2026-06-06-22-17-01-chest-fly-requires-bench.sql` re-tags it `['dumbbells','bench']`, **apply it in the Supabase SQL editor**. Deliberately left: Dumbbell Single-Arm Row stays dumbbells-only (a bench-free staggered-stance variant exists; over-tagging would wrongly exclude it from home setups), flagged for the user to override if desired. No code change, the filter was already right.
3. ~~**Generator transparency.**~~ **SHIPPED 2026-06-06** as per-exercise resolved-equipment chips on the Plan exercise breakdown (`EQUIPMENT_LABELS`), so an equipment mis-tag like the bench leak is visible in the app, not only by reading the seed. The deeper per-exercise "why this slot" reasoning (movement-pattern / emphasis) would need instrumenting the frozen generator and is deferred.
4. **Week 1 mid-week start (decided: Runna model, keep all sessions).** Started Sunday with Mon/Wed/Thu/Sun; Week 1 showed Mon/Wed/Thu still to-do, which felt wrong. Decision (2026-06-06, validated against Runna): completion-paced means a week is an ordered set of N sessions, so the fix is NOT to hide the past-calendar days but to ensure they read as **up next, never overdue**, plus a friendly start-date prompt so users can start clean. Two pieces:
   - **Piece 2 (honest rendering) , VERIFIED ALREADY CORRECT + locked (commit `cff9e07`).** Step-0 audit found the engine + day strip already key off completion state, not `date < today`: `computeWeekAdherence` anchors the week window to the start day (`winStart = start + (calendarWeek-1)*7`), the shipped false-behind fix makes day-1 read `on_track`, and `DayTabs` has no calendar-overdue styling (neutral `done/total` from logs). So a Sunday start already classifies Mon/Wed/Thu as upcoming, not missed. Added 2 regression tests (mid-week start → zero missed in week 1, full session count; on_track on day one). No production change needed.
   - **Piece 1 (friendly start-date prompt) , SHIPPED 2026-06-06 (commit `7074858`).** A Runna-style "When do you want to start?" final step (Today / Tomorrow / Next Monday / Pick a date) in `RoutineSetupFlow`, defaulting to Today, that sets `program_anchor` at creation. The chosen date resolves to a noon-UTC anchor (`startAnchorISO`, "Next Monday" lands on today when today is Monday) and is applied via the existing `setProgramAnchor` after create, threaded through all three flow renderers (`GenerateRoutineButton`, `OnboardingModal`, `TemplatesTab`); each create fn already returns the routine. Reused plumbing, no engine change; anchor drives the calendar overlay only. Stays editable in Plan. +4 tests (787 total). **Companion still open: the program-length setup step (Prompt 2, in section D).**
5. ~~**Coach decision feed isn't scoped to the active routine.**~~ **SHIPPED 2026-06-06 (commit `6d86cf6`).** `PulseProvider` now exposes a `routineDecisions` memo (decisions filtered to `activeRoutine.id`, same pattern as `routineSessions` / `routineAdjustments`) as the context `decisions`, so the Coach panel only shows the active routine's events. +1 provider test.
6. ~~**End-of-session state isn't torn down.**~~ **SHIPPED 2026-06-06 (commit `5ba5e9b`).** Four symptoms, two root causes. (a/b) The rest timer fired after the session's last set and re-appeared in the rail after finishing: `handleSave` now suppresses the timer when the saved set completes the whole day, and `RestTimer` only starts on an actual `trigger` change, never on mount (the rail timer remounting after guided mode no longer fires a phantom countdown; this also fixes a stale timer when re-entering Train after navigating away). (c) The rail's "session in progress" line is now conditional (complete / in progress / not started, derived from logged sets). (d) The Train header gains a **Workout complete** done state with **Re-open** (re-enter guided mode, no new session) and a confirm-gated **Clear day** (per-row optimistic deletes; the completed session row is left as a record). +10 tests.
7. ~~**Plate calculator can't be closed after saving (guided).**~~ **SHIPPED 2026-06-06** with the section-B plate rework: `handleSave` now resets `platesOpen` (and `overflowOpen`), and the panel has a labeled Close button, so it is never stranded open.

**B. Set-row + plate-calculator polish (guided; follow-up to shipped Tier 1 #2) , SHIPPED 2026-06-06 on `feature/guided-set-row-polish` (2 commits), mockup approved.**

- ~~**Show the active exercise's target sets/reps** near the inputs.~~ **SHIPPED.** Prescription chips (rep target + RIR) sit by the inputs and show even first-time, plus "Set N / total" in the set label.
- ~~**Copy previous set into the current empty set** ("same as last set").~~ **SHIPPED.** One-tap "Same as last" fills weight + reps from the previous set (shown only when there is history).
- ~~**Hide "+ Add drop"** behind the set overflow.~~ **SHIPPED.** Moved into a per-set ⋯ overflow (editorial); the Train card keeps it inline. The overflow is the home for future per-set actions (swap, notes).
- ~~**Plate-calculator rework (one piece, not patches).**~~ **SHIPPED.** Reworked into a labeled **loaded-bar** visual (plates drawn heaviest-first, with an sr-only breakdown), gated to barbell / plate-loaded lifts via the new `isPlateLoaded` (no longer leaks onto dumbbell / cable / machine, e.g. Chest Fly), and closes on save (bug 7). The gate also applies on the Train card.
- **Suggested starting weight (PARTIAL).** Shipped the light "start light and leave N in the tank" first-time cue; the *computed* starting estimate still needs the strength model, deferred, scope separately.
- ~~**Superset clarity + presence.**~~ **SHIPPED 2026-06-06.** (1) An explicit "one set of each, alternating, then rest after both" cue on both the Train `SupersetCard` header and the guided superset step; (2) the Train superset header now matches a normal exercise-card row in height + type scale (with a "Superset" badge), so it no longer reads as a minor strip.

**C. Screen tweaks (small, cross-surface) , mostly SHIPPED 2026-06-06 on `feature/live-test-screen-tweaks`.**

- ~~**Train:** replace the "DONE" text on a completed exercise row with a green check.~~ **SHIPPED** (accent check circle, keeps the "All sets done" label).
- ~~**Plan:** add the how-to-perform info icon per exercise.~~ **SHIPPED** (per-built-in-exercise info icon opening the existing `ExerciseInstructionModal`, parity with the Train card + guided mode).
- ~~**Library:** make Routines the first tab, before Exercises.~~ **SHIPPED** (Routines is now first and the default landing tab).
- ~~**ProgramView: split the rationale into labeled sections.**~~ **SHIPPED 2026-06-06.** The view now parses the persisted ` · `-joined rationale into scannable fact chips + the trailing prose (no data migration; degrades to plain prose for any string not in that shape). Emitting fully structured rationale from the generator stays a future option if richer labels are wanted.

**D. Features + open questions (need design, scope separately)**

- **Exercise "how to perform" media.** Add an image / gif / video to the instruction modal (text-only today). Needs a storage + content decision, and it shares infra with Tier 3 #9 Progress photos (Supabase storage bucket + RLS + a CSP `img-src` / `connect-src` update). Scope the two together.
- ~~**Finish / share screen rework.**~~ **IN REVIEW 2026-06-07 (`feature/finish-debrief`, moves to Shipped on merge).** Replaced `ShareCard` with a debrief-first `FinishDebrief`: (a) optional **session RPE (1-10)** + free-text note (new nullable `session_rpe` / `session_note` columns); (b) a deterministic **coach summary** from the `DecisionEvent` log (progressions / deloads / ramp-back), PRs, session tonnage, and per-muscle volume; (c) a general UX pass with **real PNG export** (`html-to-image` `toBlob`, Web-Share / download). This pulls the **post-workout session rating** forward from generation Phase 3. Deferred follow-ups: surface the RPE / note read-only in the History session detail; use session RPE × duration as a session-load signal in the engine (Phase 3).
- ~~**Reset / clear a workout day (decided: both).**~~ **SHIPPED 2026-06-06** with bug 6 (commit `5ba5e9b`): the finished Train header shows "Workout complete" with "Re-open" and a confirm-gated "Clear day". Deferred refinement: clearing leaves the completed `workout_sessions` row, so adherence still counts the day; revisit if re-doing should also drop / un-complete that session row.
- **Guided screen landscape layout (needs a new design).** In landscape (a phone turned sideways, where `useMediaQuery('(min-width: 1024px)')` is still false so the single-column Focus layout renders into a short viewport, and on the desktop two-pane), the "Now lifting" hero, exercise title, "next exercise", and "Finish workout early" stack too tall and eat the whole screen, so the set inputs and rest timer fall below the fold. Needs a height-aware guided layout: detect short/landscape viewports and compress the hero (smaller title, inline meta, shrink or drop the big progress ring) so the logging area stays visible. Surfaced in the 2026-06-07 train-cleanup pass; the title-size clamp shipped there only helps the portrait case. Scope a proper redesign rather than patching font sizes.
- ~~**Permanent swap on Plan page.**~~ **SHIPPED 2026-06-07 (`feature/ui-polish-superset-library-plan-swap`).** Each exercise row in ProgramView now has a Swap button that opens `ExerciseSwapPicker` with same-movement-pattern candidates. Selecting a replacement calls `swapRoutineExercisePermanently` (new server action), which updates `exercise_id` on the `routine_exercises` row permanently. **Still open:** swap inside a superset member (button not yet on superset pairs in Train/guided); reason-split (permanent vs this-week) in a single entry point; Tier 3 #8 smart substitution v2.
- ~~**Program length as a setup step (Prompt 2).**~~ **SHIPPED 2026-06-06 (commit `90b8c17`).** A `'length'` step in `RoutineSetupFlow` (4 options 8/10/12/16, default 12 = "Recommended", values from the single `PROGRAM_LENGTHS` source, no custom lengths) sits with the start-date step as the two "shape your program" choices. The chosen length is applied after create via the existing `updateRoutineProgramWeeks` write path, only when it differs from the DB default of 12, threaded through all three flow consumers (`GenerateRoutineButton`, `OnboardingModal`, `TemplatesTab`). No `buildProgram` change; stays editable in Plan. +2 tests. Folded in the same branch: a **gender-optional setup polish** (commit `c34fbbe`), the gender step now offers a first-class "Prefer not to say" option and Next is always enabled, so no gender is a real choice (neutral strength standard) instead of an easy-to-miss Skip link. +2 tests.

**Recommended order (to confirm with the user):** bodyweight bug first (unblocks honest data this session), then the end-of-session teardown cluster + routine-scoped decision feed + week-1 anchoring + equipment filter (all independent of training, no live screen needed), then the set-row & plate-calc polish as one guided follow-up branch, then the finish-screen and exercise-media features as their own scoped work. The reset/clear-day question gets answered alongside the end-of-session cluster (same surface).

---

## Launch floor (pre-public)

The data-safety and account-lifecycle work that gates a public launch. These are not features, they are the floor: "don't lose or leak user data, don't trap the user." Features wait behind them. Sourced from the multi-app review sweep (Liftin', Runna), whose loudest one-star drivers were sync data-loss and billing opacity, cross-checked against the code. Nothing here jumps the floor or the validation gate; the action is verifying these failure modes are handled before strangers arrive.

- **Auth + billing lifecycle (the long pole).** Public launch needs the full account lifecycle built together: signup / email verify / password reset / account delete (no signup route exists in code yet). The sweep extends this to **billing specifically**, cancellation difficulty, opaque billing, and charges after account deletion are the #1 complaint driver across Runna reviews. So the paid-tier seam must include a clean, honest cancel / refund / billing-transparency path built as carefully as signup, plus a real support contact (not a bot). **Positioning note (not a build):** a one-time / lifetime payment option is a real weapon against subscription fatigue and fits Pulse's no-dark-patterns stance, worth considering when designing the tier, or at minimum keep subscription terms transparent. **Cross-device session continuity (security boundary):** the safe version is already the default, Supabase persists an authenticated session per device (the SSR cookie session the middleware refreshes), so "logged in everywhere" needs no new mechanism. If you want lower sign-in friction, do it through owner-bound standard flows: single-use, short-TTL magic-link / OTP sign-in delivered to the account's own email, or a QR device-pairing approval from an already-authed device (the WhatsApp-Web model). **Never a link that *carries* authentication**, a forwarded or shared link that auto-logs-in whoever holds it is an account-takeover path, and stays out.

- **Accessibility (a11y) audit, sequenced AFTER the auth lifecycle, not before.** A full a11y sweep, not yet done. The code audit (2026-06-06) surfaced one symptom, the guided `WorkoutModeScreen` overlay missing `role="dialog"`/`aria-modal` and some unlabeled icon-buttons (audit Q8), and one flagged modal almost always means the same patterns recur elsewhere, so this needs a dedicated pass, not a spot-fix. Treated as a launch-quality **floor, not deferred**, because the launch audience is EU/NL, where the European Accessibility Act (in force 2025) makes basic digital accessibility a legal consideration, not only a quality one. **Triage like the general audit:** fix the floor (WCAG 2.1 A + AA essentials, keyboard operability, focus management on modals/overlays, accessible names, semantic structure, contrast) before launch; file richer ARIA/live-region/reduced-motion as opportunistic Bucket 2; drop/defer the AAA-and-edge tail. **Highest-priority surface: the core logging loop** (`ExerciseCard`, `SetLogger`, guided `WorkoutModeScreen`), a keyboard or screen-reader user must be able to log a set start-to-finish. **Pulse-specific checks the standard sweep would miss:** (1) verify contrast against **every accent preset** (coral/emerald/sky/violet/amber/rose), not just default coral, since a user-picked accent multiplies the contrast surface by six; (2) part-manual, not tooling-only (automated scans catch ~⅓–½; the manual VoiceOver/keyboard "log a set without looking" core-loop test is the highest-value part); (3) test on a real mobile device with VoiceOver (iOS) / TalkBack (Android) + touch-target sizing, since installed-PWA mobile is the primary surface. **Preliminary contrast already computed** (so the eventual sweep starts from data): the bright accent-as-foreground passes AA on the dark theme for all six presets (6.4–11.4) and dark-label-on-accent buttons pass, so that fear is defused; the real AA gaps are accent-independent, `pulse-muted` body text fails the 4.5 floor on bg/surface/surface-2 (4.21/3.89/3.61, large-text-only) and the dim-accent foregrounds fail on most presets (≈3.3–4.4). Confirms this is a genuine defect to fix, not a theoretical one. **Sequencing rationale:** signup / login / reset / verification are net-new, high-stakes a11y surfaces that don't exist yet (a screen-reader user who can't complete signup never reaches the app); auditing now would miss the most important new front-door surface and force a re-audit. **Exception that jumps the queue:** fix the known Q8 modal issues opportunistically if already inside `WorkoutModeScreen` for the guided-set-row polish (Tier 1 #2), that is the opportunistic-fix discipline, not running the formal sweep early.

- **Operational infrastructure (the "operating it in production" floor).** These are the gaps a feature-focused solo dev overlooks because at two users *you* are the monitoring, support, and error reporting, which silently stops being true at launch. Triage: the first four are floor (regret-launching-without), the rest are confirm-don't-assume.
  - **Error / crash tracking (floor).** Today a production runtime error is invisible until it surfaces as a one-star review ("it glitched and lost my workout", a real Runna/Hevy review pattern). Need production error capture with stack + route context. Sentry (free tier ample at this scale) or PostHog error tracking (one fewer vendor if PostHog is adopted later, see the analytics decision record above). Pairs with the offline-queue dead-letter work, a dead-lettered write should surface to *you* (the `deadLetteredCount` already exists), not just sit in IndexedDB. Launching blind to production errors is launching without a smoke detector.
  - **Database backups + a tested restore (floor, confirm/upgrade).** The product promise is "your training history, for years," so DB loss is existential and a botched restore loses a user forever. Confirm the Supabase plan's backup cadence, recovery window (point-in-time recovery), and *actually test a restore once*. Free-tier Supabase backup retention is limited, if so, this is a reason to upgrade the tier before launch. "There are backups" ≠ "I've restored one."
  - **Rate limiting / abuse protection (floor, build into the auth lifecycle).** No rate limiting on write paths or auth today; fine at two trusted users, not public. Two surfaces matter: auth (signup/login, credential-stuffing, signup spam, verification-email abuse) and the mutation routes (a runaway/buggy client flooding the DB). Needn't be elaborate, Supabase has some built-in auth limiting; a basic per-user/per-IP limit on mutations covers the rest. Build it *into* the auth-lifecycle work rather than bolting it on later.
  - **Production email deliverability (floor, part of the auth lifecycle).** Signup verification, password reset, and transactional email depend on email actually arriving (SPF/DKIM/DMARC, sender reputation, not landing in spam). Supabase's default email sending is dev-grade and rate-limited; production signups likely need a transactional provider (Resend / Postmark / SendGrid) wired to Supabase Auth with domain email auth configured. A reset email that bounces or spam-folders is a locked-out → dead user. Part of the auth-lifecycle build, not separate, it's the part people forget until reset emails fail.
  - **Confirm-don't-assume (lower effort, mostly near-free on Vercel + Supabase):** uptime/health monitoring (a free pinger on a health endpoint, so "is it down" isn't answered by a user); a staging/preview environment with a non-production Supabase so destructive migrations are never tested against the only users' real data (Vercel preview deploys give most of this free); migration safety/rollback discipline (hand-written migrations against prod data get higher-stakes once it's irreplaceable, test against a copy, know how to roll back); CI on push (the 725 tests + typecheck running automatically so a regression can't reach prod silently, Vercel can gate deploys on it).
  - **Explicitly NOT needed yet (don't over-build):** CDN beyond Vercel's default, a separate caching layer (Redis), container orchestration, load balancing, multi-region, a dedicated logging pipeline beyond error tracking. All scale infrastructure for a user base that doesn't exist; the Vercel + Supabase stack handles this class of concern precisely so a solo dev doesn't reach for it early.

- **Legal / compliance pages (floor, named deliverable, not just an implied mention).** Privacy policy, terms of service, and a cookie/consent mechanism with a clear data-processing basis. Currently referenced only in passing (the analytics GDPR note, implied in the auth lifecycle) but not owned as a concrete item. Required floor for an EU/NL launch handling health-adjacent data (bodyweight, measurements, training history), same tier as account deletion. Not a big build, but a GDPR-compliant privacy policy for health-adjacent data is not a fill-in-the-blank template, and it's the kind of task that's nobody's until it blocks launch day, so name it now. Shares the consent surface with the analytics decision record above. **Caveat (not lawyer-able from here):** these roadmap items mean "make sure this is handled," not "this is now legally sufficient", for health-adjacent EU body data, a one-time actual legal check (even a single consultation on the privacy policy + consent basis) is worth more than any roadmap wording. Treat the obligations named here as the checklist, not the clearance.
- **Incident + rollback plan (floor, lightweight, extends migration safety).** The connective tissue between backups and error tracking: the deliberate answer to "what do I do when production breaks with real users," not a heavy runbook (solo dev). Three concrete cases: (1) roll back a bad deploy (Vercel is ~one click, confirm the procedure is known); (2) roll back a bad *migration*, the real one, since migrations are hand-written and applied to prod data, so it is the higher-stakes case and overlaps the migration-safety confirm-item above; (3) a stated tolerance for "app is down while I'm asleep." The value is having thought it through once, before it happens at 2am on live user data.
- **Account data export (floor, GDPR portability, pairs with account deletion).** GDPR's data-portability right. The shipped CSV export partially covers this for workout logs; the action is to confirm whether that satisfies the portability obligation or whether the account-deletion floor item should also carry a full-account export companion (deletion and export are the two halves of "the user controls their data", same surface). Likely small or already mostly covered; confirm sufficiency rather than assume new build.

**Floor already shipped (do not re-audit):** offline-queue cross-user safety (per-user replay, never dropped/leaked) **and poison-pill / dead-letter hardening** (a permanently-failing write no longer stalls the queue; auth-expiry stays transient), live RLS audit (all user-data tables owner-scoped in prod), decimal-comma input parsing, rest-timer-survives-lock reliability, and the **2026-06-06 code/security audit** (server-action authz, input validation, secrets, CSP all verified, see `docs/audits/`). See Shipped.

**Still open on the floor:** the auth + billing lifecycle (above), the a11y audit (below, sequenced after it), the **operational-infrastructure** items above (error tracking + tested backups stand alone; rate-limiting + email deliverability fold into the auth lifecycle), the **legal/compliance pages** (privacy policy + ToS + consent) and **incident/rollback plan**, confirming the CSV export covers GDPR portability, and capturing the live RLS policies in version control (a reproducibility gate flagged by the audit, RLS is enabled and prod-verified, but the policy bodies aren't all in `docs/migrations`).

---

## Roadmap (re-tiered 2026-06-06)

Ordered to deepen the adaptive-coach moat. Tier 1 is cheap and makes existing intelligence visible; Tier 2 is the real personalization differentiation; Tier 3 builds on it.

### Tier 1, now (cheap, high-trust, on-vision)

| # | Feature | Notes |
|---|---------|-------|
| 1 | ~~Surface adaptation decisions (+ fill the desktop right rail)~~ **SHIPPED 2026-06-06** | The smartest features (ramp-back, auto-deload, priority-tilt) compute their reasons but never explain them. Add plain-language "why" lines: "Volume eased this week after your 11-day break", "Deload on bench, 3 weeks without a PR", "More chest this block, your selected priority". Reuses already-computed state (`programPosition`, `shouldDeload`, `priority_muscle`); no new data. **Absorbs the old "desktop right context rail, fill it out" item**, the sparse `/train` right rail is the natural home for glanceable adherence + adaptation + per-muscle volume. Files: Train / guided surfaces, `DesktopLayout.tsx`. **Status (2026-06-06):** the Coach Decision Timeline (#10) shipped the ramp-back / deload / progression surfacing, and the **priority-tilt "why" line** shipped too (`priorityFocusLine`, a caption above the per-muscle volume bars on Progress). **Done (2026-06-06):** the visual half shipped too, per-muscle weekly volume vs target now fills the desktop right rail (`RailMuscleVolume`, Option A: all 9 targeted muscles, lagging-first, floor tick, success-green once the floor is hit), reusing `computePerMuscleVolume` + `computeVolumeProgress` so it matches Progress exactly. #1 complete. |
| 2 | ~~Guided set-row polish (Editorial draft C)~~ **SHIPPED 2026-06-06** | Guided-only (the `editorial` `SetLogger` variant), so Train's `card` behavioural-contract tests stay green. (a) Single-active focus: a new `active` prop (default true) means only the next unsaved set shows inputs; the rest render as dimmed "Set N · Not started · target RIR" previews (`WorkoutModeScreen` computes the active index = first unsaved set). (b) Inputs grouped: Weight/Reps balanced (both flex-1) with Save in the same row, killing the ballooning-Weight / gap-before-Save. (c) Logged row decluttered to value · PR · RIR · ✓; the plate button is dropped and Edit/delete are de-emphasized, brightening on hover/focus (kept visible for touch). Also landed the opportunistic **a11y Q8** fix: both guided overlay roots now carry `role="dialog"` + `aria-modal` + label. Open scope question resolved guided-only; Train's unlogged row left as a possible later pass. |
| 3 | ~~Positioning copy~~ **SHIPPED 2026-06-06 (live surfaces)** | The two live surfaces are wired: the login subtitle now reads "An adaptive strength coach, on any device" (positioning, replacing the feature-y "12-week progressive overload"), and onboarding leads with the mechanism-as-benefit ("Pulse adapts as you train: miss a week, hit a plateau, or train somewhere new, and your plan adjusts so you keep moving forward") via a new onboarding-only `intro` prop on the shared `RoutineSetupFlow`. Per the Claude.ai positioning review: capability-forward headline (login) + concrete-benefit voice (onboarding), and **no longevity claim** ("for years, not weeks" stays an internal north star until validation earns it). **Deferred:** the public marketing hero (no public landing page exists yet, launch floor); the agreed hero line is "An adaptive strength coach that works on any device" with the equipment framing in the sub-line, to wire when that page is built. |
| 11 | ~~Lighten this week (user-initiated)~~ **SHIPPED 2026-06-06** | A "Go easier this week" button on Train that applies the ramp-back ease (0.6 volume, +1 RIR) to the current week on demand. New `AdjustmentKind` `manual_deload`: `progressionInfo` treats it as a lighter week (`isRampBack`) but, unlike a gap-driven re-entry, it does NOT insert/offset the program (a one-off easier week, progression continues). `lightenThisWeek` action writes the adjustment + dual-writes a `decision_events` row (type `ramp_back`, new trigger `manual`) so it shows in the Coach Timeline with its own copy ("You chose to go easier"). Banner + week label are kind-aware (Lighter vs Ramp-back). Migration `2026-06-06-15-58-32-manual-deload-adjustment-kind.sql` widens the `kind` CHECK. **Collision:** the button hides once a week is lightened, so a manual write never clobbers an auto re-entry from the UI; the upsert is idempotent regardless. **Deferred:** undo (a manual_deload delete) and a button busy-state (double-fire is idempotent + the optimistic re-render unmounts the button). **Review confirmations (science/UX):** placement (understated, on Train below the week tabs) is correct, leave it; undo stays deferred until validation shows a real need; the dose is a mild modeling error to refine, tracked as Tier 2 #7. **Guard preserved:** reduces load only, never diagnoses or rehabs. |
| 12 | ~~Session-linked logging + `DecisionEvent` log (Phase 0 #3)~~ **SHIPPED 2026-06-06** | The last generation-engine Phase 0 foundation. `session_id` (nullable FK to `workout_sessions`) + `workout_date` (user-local, stamped client-side at save) added to `set_logs`; the unified `decision_events` table + the `DecisionEvent` schema (`type` = ramp_back / deload / progression / swap; + `trigger`, `affected_area`, `magnitude`, `confidence`) define the canonical log. Capture point: **set-save**, derived purely by `decisionForExercise` at the provider choke point (components untouched), deload (plateau) and progression (targets_hit) land for the first time; ramp-back is dual-written from `acceptReentryDeload` so the timeline reads one table. Best-effort + idempotent (re-derivable from logs, so not queued). Unblocks trustworthy skip detection, behavior-driven adaptation (#7), and the **Coach Decision Timeline** (#10). Migration: `2026-06-06-14-30-53-session-linked-logs-decision-events.sql`. |

### Tier 2, next (the moat: personalization in generation)

| # | Feature | Notes |
|---|---------|-------|
| ~~GQ1~~ | ~~Generation quality pass -- exercise ordering and pattern guardrails~~ **SHIPPED 2026-06-07** | Three focused improvements to `generation.ts`: (1) post-select **tier sort** (`patternTier`: squat/hinge compounds T1, big multi-joint T2, isolation T3, calf/core T4); (2) **heavy-compound dedup guard** (`HEAVY_DEDUP_PATTERNS` = hinge + squat) caps each at one compound per session, blocks backfill reuse, relaxes when pool is thin; (3) **backfill prefers uncovered slots** (stable sort by coverage count). No schema change. +16 tests. |
| ~~GQ2~~ | ~~Wire `fatigue` metadata into generation backfill priority~~ **SHIPPED 2026-06-07** | `fatigue?: number` added to `ExerciseMeta`; added to the pool DB query (`select(..., fatigue)`); used as a secondary sort key in `byPattern` -- lower fatigue preferred between exercises of the same loading class (loading lean priority unchanged; fresh still beats used regardless of fatigue). Exercises with no fatigue data use a neutral midpoint (3) so they neither win nor lose against well-characterised ones. No migration (field already seeded). +4 tests (fresh beats used, undefined pool stays stable, loading lean overrides fatigue). Suite: **874 tests**. |
| ~~GQ3~~ | ~~Role-aware exercise ranking and weekly structure validation~~ **SHIPPED 2026-06-08** | Five coach-quality fixes to `generation.ts` plus a data correction (the weekly-structure repair pass named in this item's title is held pending a real reproduction, not shipped here): (1) **anchor-aware fatigue sort** inverts the GQ2 tiebreak for the six `COMPOUND_ANCHOR_PATTERNS` so the higher-fatigue (better) anchor wins when fresh (Bench Press over Push-Up); (2) **substitution-class cross-session dedup** (`usedSubstitutionClasses`, mirrors `used`) soft-deprioritizes a repeat of the same substitution family across sessions; (3) **unilateral cap** blocks a second `unilateral: true` pick per session (hard block, mirrors `HEAVY_DEDUP_PATTERNS` / `heavyPatternFilled` / `relaxedHeavyCap` incl. the thin-pool relax fallback); (4) **Front Raise suppression**, a new `front_delt_isolation` substitution class (split from `lateral_raise`) is soft-deprioritized once a vertical press fills the session; (5) Smith Machine Bench Press re-tagged `{machines, bench}` (migration `2026-06-08-gq3-exercise-data-corrections.sql`). `ExerciseMeta` + pool query gain `substitution_class: string | null` and `unilateral: boolean`. +6 tests. Suite: typecheck clean, **882 tests**. |
| ~~4~~ | ~~Training-style + variety + loading lean preferences~~ **SHIPPED 2026-06-07** | Three orthogonal generation inputs on the existing slot engine. **Training style** (merged #85): Balanced/Strength/Bodybuilding/Powerbuilding via `resolveBias` + `resolveRepRange`, persisted on `profiles.training_style`, with a `RoutineSetupFlow` step. **Variety preference** (merged #86): 'consistent' anchors main compounds (`COMPOUND_ANCHOR_PATTERNS`) via a per-generation anchor map, 'varied' is the byte-identical default; persisted on `profiles.variety_preference`, `RoutineSetupFlow` step. **Loading lean** (merged, `feature/loading-lean`): `LoadingPreference` ('barbell' / 'dumbbell' / 'machine' / 'cable') secondary-sorts `byPattern` candidates so preferred-equipment exercises float before others while fresh-preference still beats the loading sort (a fresh non-preferred wins over a used preferred); persisted on `profiles.loading_lean` (new nullable column, migration `2026-06-07-loading-lean-preference.sql`); optional `RoutineSetupFlow` 'loading' step with toggle (tap again to deselect, shows "Skip" when nothing chosen). null/undefined is byte-identical to base (golden identity test). `TemplatesTab` sets `collectLoadingLean={false}`. +8 tests. Suite: typecheck clean, **870 tests**. |
| ~~5~~ | ~~Movement restrictions~~ **SHIPPED 2026-06-08 (in review, `feature/movement-restrictions`)** | Restriction flags (knee / lower_back / shoulder / wrist) that exclude contraindicated exercises and substitute, a hard pool filter (`isContraindicated`) beside `hasEquipment`. Per-exercise `contraindications` metadata + `profiles.movement_restrictions` (two migrations); optional `RoutineSetupFlow` step + standing Profile editor; safety-aware write-back (absent param never clears a stored flag). v1 is pure subtractive (no substitute front-loading; future hook `substitution_class`). Identity-preserving no-op path. +8 tests. **Ranked multi-priority stays deferred to generation Phase 3** (needs the volume-first allocator; a single `priority_muscle` covers ~80% of users). |
| 6 | Equipment profiles | Save multiple equipment sets (Home / Gym / Travel), switch and regenerate. Equipment is captured per-generation today, not stored as reusable profiles. The concrete home-gym / cross-device feature. |
| 7 | Reason-driven manual deload dose | **Refinement of the shipped Lighten-this-week (#11), validation-informed.** Today a manual lighten reuses the post-layoff ramp-back ease (0.6 volume, +1 RIR). That is a mild modeling error (Claude AI, science): a post-layoff ramp-back rebuilds a *detrained* lifter (a bigger cut is justified), whereas a mid-program "go easier" manages accumulated fatigue in a *fully-trained* lifter (less reduction is appropriate; over-cutting needlessly drops stimulus). So the manual dose should be **meaningfully gentler than the layoff dose**, and ideally **reason-driven**: add a reason on the lighten control (e.g. sick/run-down vs slammed-schedule, the latter is really a missed-volume week, not a fatigue event) and map each reason to its own ease factor, all gentler than ramp-back. Rough direction (not a defensible exact number, calibrate by use): ~0.7-0.8 volume, +0.5 to +1 RIR. **Code-truth gate (do this first):** `rampBackPrescription`'s volume cut has **no consumer** today, so the only *felt* ease is the integer `+1 RIR` (LogView), and the stored volume factor is inert. A "gentler" dose is therefore not expressible until actual volume reduction is wired into set rendering (and RIR can't go below +1 without being no ease at all). Sequence: wire real volume reduction → then reason→dose with manual < ramp-back. **Calibrate during the validation block:** if the cut feels too heavy for ordinary fatigue in real use, that is better evidence than the reasoning. **Copy already made honest (2026-06-06):** the ramp-back / lighten / manual copy now promises only the easier RIR (the overclaimed "reduced volume" was removed everywhere), so this item is purely the engine work, not a copy fix. |
| 13 | Exercise history context while logging | A consolidated "what did I do here last time" panel surfaced at the point of logging (last performed date, last session's sets, best set + estimated 1RM, a short recent trend, the previous note for this exercise), the highest-value gap flagged in the June 2026 competitive sweep (Hevy / Strong / Boostcamp and others all ship a version of this). **Code-truth: a UI/aggregation job, not a data job.** Every input already exists as a tested pure function in `utils.ts`, just not wired to the logging surface: `computeLastSession` (today renders as the one-line "Last: 80 kg × 8 × 3 sets" in `ExerciseCard`), `computePRMap` / `computeBestSets` (best set + e1RM via the Epley `calcE1RM`), and `computeE1RMHistory` (the per-exercise week-by-week e1RM series already driving `E1RMChart` in HistoryView, just not surfaced at logging time). Notes are keyed `week-routineExerciseId`, so the previous week's note for the same exercise is one lookup away in the existing map. No schema change, no migration, no new query, the work is composing existing tested computations into a panel on `ExerciseCard` / `SetLogger` / guided mode, the natural richer successor to the already-shipped one-line "Last session display". |

**Foundations first:** the 2026-06-06 audit found generation's bottleneck is data richness, not the algorithm. The generation **Phase 0** data foundations (muscle-attribution bridge, exercise-metadata expansion, session-linked `set_logs`) precede #4 / #5. See "Routine generation v2, engine direction" below.

### Tier 3, then

| # | Feature | Notes |
|---|---------|-------|
| 7 | Behavior-driven adaptation (v1.5) | Learn from logged behavior (repeated swaps, skips, added / cut volume) and bias future blocks' avoid-set + emphasis. The pre-AI alternative to "AI generation v2", build this first. Foundation already exists: `exercise_swaps` + `set_logs` + the routine-wide avoid-set. |
| 8 | Smart substitution v2 | Reason-tagged swaps (pain / no equipment / crowded) offering 2-3 ranked same-stimulus suggestions, on top of the existing swap. Small enhancement to a shipped feature. |
| 9 | Progress photos | (Promoted from Later.) Date-stamped photos alongside measurements, paired with the recomp dashboard. Outcomes beat metrics for most users. Needs file upload + a Supabase storage bucket (RLS) + a CSP `img-src` / `connect-src` update. Only item here with real infra cost. |
| 10 | ~~Coach Decision Timeline~~ **SHIPPED 2026-06-06 (increment 1)** | The user-facing payoff of the adaptive engine: a plain-language log of the program decisions Pulse makes, turning invisible smarts into proof Pulse is a coach, not a logger. **Shipped:** read path (`loadDecisionEvents` → `/api/pulse/decisions` → `useDecisionEvents` → context), the pure `decisionCopy` mapper (what changed / why / what next, per type), `CoachPanel` as a compact surface (desktop right rail + Train inline on mobile) plus a full week-grouped timeline overlay (`role="dialog"`, Escape/backdrop close). Surfaces the three logged decision types (ramp-back / deload / progression); progressions use the existing `pulse-success` green, others the themeable accent. Design draft: `docs/superpowers/designs/2026-06-06-15-08-34-coach-decision-timeline-draft.html`. **Deferred (increment 2):** derived events the log does not yet store, program created, block transition, PRs, priority change, layered into the same timeline with no schema change. |
| 14 | Program pause / injury mode | An explicit "pause this program" action (illness, injury, travel, life), the most-requested gap against Runna-style adaptive plans per the June 2026 sweep. Pulse already has the *return* side (ramp-back, `acceptReentryDeload`, the `DecisionEvent` log); the gap is the pre-emptive, user-initiated half: suspend the calendar so a deliberate gap reads neither `behind` nor `lapsed` and never eats a missed-week adherence hit, then hand off to the existing ramp-back on resume. **Code-truth (medium, not small):** `AdjustmentKind` and the `decision_events.type` CHECK both extend cleanly and append-only, one new kind / event-type each (`pause` / `program_paused` / `program_resumed` fit the existing idempotent-per-week schema). The real cost lives in `adherence.ts`: `computeProgramPosition`, `progressionInfo`, and `computeWeekAdherence` all treat time linearly (anchor to now, `daysSinceLastSession`, `behindBy` from calendar dates), so a pause is a non-linear gap that has to be threaded through all three (subtract the paused span before any calendar math, freeze the lapsed countdown, exclude paused dates from `behindBy`). Roughly 300-500 lines of engine change plus another ~100 in actions / types, a deliberate rethink of the anchor semantics, not a one-line `AdjustmentKind` addition. Pairs naturally with #5 (movement restrictions) as the other half of injury-aware training: pause now, restrict patterns on return. |

### Routine generation v2, engine direction

A full code-grounded architecture audit (2026-06-06; both AI reviews converged) is captured in `docs/superpowers/designs/2026-06-06-00-30-19-generation-engine-v2-audit.md`. Conclusion: **the generator is already well-architected and should not be rewritten. The next bottleneck is data richness, not the algorithm.** The shipped pipeline already is the layered model both reviews proposed: profile → training archetype (`STYLES` by day count) → movement-pattern slots (`EMPHASES`) → equipment-filtered selection → progression.

Single most important finding: generation is purely **slot-first** (fill session slots, weekly volume emerges). `VOLUME_TARGETS` / `priorityAdjustedTargets` exist but feed only the Progress / recovery analytics, never generation, and the two systems speak different vocabularies (15 `MovementPattern`s vs 10 `ExerciseCategory`s) with no bridge. That missing bridge is the foundation everything else depends on.

Phased plan (do in order, foundations before features; "build trust before more intelligence"). A research-consensus draft of the Phase 0 content (muscle map, metadata, contraindications, volume landmarks, training-style definitions, strength standards, adaptation copy), with reconciliation notes against Pulse's actual vocabulary, is captured in `docs/superpowers/designs/2026-06-06-00-54-52-phase0-source-material.md`.

- **Phase 0, data foundations (highest leverage, no visible feature) = "make coaching decisions computable":** build the weighted MovementPattern ↔ muscle bridge (reconcile the draft map to Pulse's real 15 patterns; the 6 isolation patterns are already muscle-specific so attribution is near 1:1, the real work is the ~7 compounds' secondary muscles; keep it a data table so more patterns can be added later without a rewrite, 7 compounds is enough to ship Phase 0-2, not the final model). **Decide muscle granularity first: keep the existing 10-category taxonomy and collapse only the muscle target side of the map into it; the 15 movement patterns stay the generation control layer, separate from the reporting categories (see the three-layer rule in Product decisions). Expand to quad/ham + delt splits only if specialization later demands it.** Expand exercise metadata (primary/secondary muscles into the generation path + backfill, add `fatigue_cost`, `unilateral`, `joint_stress`, `difficulty`, and a substitution class for equivalence grouping); add `session_id` + a workout date to `set_logs` so skip detection / adherence / behavior learning become trustworthy. **Define a standard `DecisionEvent` schema** (`type` = ramp_back / deload / progression / swap; plus `trigger`, `affected_area`, `magnitude`, `confidence`) and log every adaptive event through it, ramp-back already persists in `program_adjustments`, but auto-deload and progression do not, so the Phase 2 timeline is a computable system log, not a cosmetic journal. Optionally tune `VOLUME_TARGETS` toward the research landmarks (the draft's MAV runs higher than Pulse's current bands for back / glutes / shoulders). **(Status, 2026-06-06: Phase 0 COMPLETE. The muscle bridge (#1), the metadata seed + movement-pattern correction (#2), the fractional volume wiring + `VOLUME_TARGETS` retune, and now the session-linked logging + unified `DecisionEvent` log (Tier 1 #12) all shipped; `exercise_swaps.week` cap raised 12 → 52.)**
- **Phase 1, personalization (= Tier 2 generation work, all clean slot-first hooks):** training style (general / bodybuilding / strength / powerbuilding) → session `bias` + `repRange`; injury restrictions → pool filter (use the contraindication map); equipment preference (prefer barbell / dumbbell / machine, distinct from owned) → `byPattern` secondary sort; variety preference → avoid-set strictness. Generate fast, expose these as optional refinement (see Product decisions below).
- **Phase 2, trust & visibility (surface the intelligence already built, before building more):** the Coach Decision Timeline (Tier 3 #10), per-decision "what changed / why / what next" explanations (the adaptation copy is drafted), and a forward-looking "Today's focus" panel on Train. Requires the Phase 0 event / session logging.
- **Phase 3, behavior learning:** trustworthy skip tracking (needs Phase 0 session linkage), behavior-driven adaptation (= Tier 3 #7), smarter substitution, and an optional one-tap post-workout session rating. *Session rating is deferred here on purpose:* there is no session-level field today (RIR is per-set only), so it is genuinely new signal, not a duplicate, but average session RIR is already derivable from the logged sets as a cheap proxy, and it has a hard dependency on Phase 0 #3 (no generic event/session table exists to hang a rating on until then, `recordDecision` is ramp-back-only). Not near-term, not foundational.
- **Phase 4, advanced programming (only when ranked specialization genuinely demands it):** ranked multi-priority + the volume-first planner, built on the Phase 0 bridge. A single `priority_muscle` already covers ~80% of users, so this is deliberately last.

### Product decisions & principles (2026-06-06)

- **Gender is no longer a programming input, and is now fully optional.** Its only generation role today is `genderDefault` (female to glutes priority), a weak proxy. Replace it with a direct "what do you want to prioritize?" question (the deferred explicit-priority step). **Gender no longer gates the Strength Score (shipped 2026-06-06):** with a gender set it uses the gender-specific standards; without one it scores against a neutral gender-midpoint standard (`NEUTRAL_STANDARDS` / `standardsFor`) and flags the result `approximate`, so the user can skip the question and still get a number, refined later by setting gender. Net: gender stays for analytics + score precision, stops guessing the program, and is never required. **Science-validation candidate:** the midpoint as the "unspecified" baseline is a defensible engineering choice, not a researched standard, worth a pass through the Claude.ai loop if it matters.
- **Generate fast, refine later (onboarding-friction guard).** Every Phase 1 preference input is another onboarding step on top of the existing 6-7. Keep onboarding minimal and generate immediately, then expose style / restrictions / equipment-preference / variety as an optional post-generation "refine your plan" step (Profile or a tune-it screen), not as new required onboarding questions. Decide this before building Phase 1 so the funnel does not bloat.
- **Templates vs generation, pick a role.** 17 hand-authored templates and the generator coexist, and `recommendTemplate` is now largely vestigial, so two systems are drifting. Decide deliberately: either templates stay as the "named program I trust" anchor (5/3/1, PPL) with a distinct purpose, or they fold into generation over time. Otherwise they are maintenance debt. This is a decision to make before extending either path, not a feature.
- **Three-layer architecture (keep the layers separate).** Movement layer (the 15 `MovementPattern`s + equipment + slots) is the generation control system; Exercise layer (per-exercise muscles / fatigue / substitution class) is the mapping; Outcome layer (per-muscle volume, progression, adherence, recovery, the 10 categories) is reporting. **The outcome layer must never feed generation directly, only through the mapping layer.** This is already true (analytics never touch the generator) and must stay true when volume-first generation arrives: the planner reads targets through the bridge, it does not import the analytics. It is also what keeps `MovementPattern` (control) and the 10 categories (reporting) from collapsing into one another. The real moat this protects is traceable, explainable adaptation: every program change answers what changed, why, which rule fired, and what happens next.

### Infrastructure, i18n foundation (next-intl)

Deliberate infrastructure, chosen early. **Not a launch blocker, and must not block or delay the auth-lifecycle or comma-parsing floor work**, it runs as a parallel track on an isolated branch (large diff expected; pure extraction, review carefully). The goal: adding a second language later is a drop-in message file with zero refactor of the string layer, so we set up a real, App-Router-native i18n library now rather than a hand-rolled JSON convention that would itself need migrating later (that would defeat the purpose).

- **Library: `next-intl`**, built for Next.js 15 App Router + React Server Components, native ICU MessageFormat. Chosen over react-i18next (client-first, awkward in RSC), next-i18next (Pages-Router era), and Lingui / Paraglide (fine but not materially better here, less App-Router-native).
- **Mode: without i18n routing.** No `[locale]` route segment and no next-intl routing middleware. Configure `getRequestConfig`, wrap the tree with `NextIntlClientProvider` (in `PulseLayout`), and use `getTranslations` (server) / `useTranslations` (client). This avoids restructuring every route under `app/[locale]/` and any collision with the existing `/pulse` Supabase-session middleware (`src/middleware.ts`); no CSP change. A real second locale later is a small, localized add (detection + switcher), not a re-extraction.
- **Structure:** `messages/en/<namespace>.json` composed in `getRequestConfig`; a single `en` locale; no UI switcher. Namespaces by surface: `train`, `progress`, `plan`, `library`, `profile`, `common` (buttons, nav, generic labels), and `coaching` (all adaptive-voice strings: progression targets, stalled / deload copy, ramp-back nudges, priority-tilt rationale).
- **Extraction scope:** move every current user-facing hardcoded string into the right namespace. A move, not a rewrite, English text stays byte-identical.
- **Interpolation contract (strict):** all formatting and unit conversion happen in TS before the value reaches the message layer; pass display-ready named variables (weights arrive already unit-converted). No conversion functions (`toDisplay`) or unit logic inside message files. Conditional clauses use ICU `select` / composed messages, never string concatenation of fragments. Canonical case: `You stalled around {prevWeight} {unit} × {prevReps}, so back off on purpose. Drop to {targetWeight} {unit} × {targetReps}{deloadClause} to reset.`, `deloadClause` is an ICU-conditional message, not a glued string. The just-shipped `deloadTankClause` / `rirClause` (currently TS string-concat in `SetLogger`) are the first conversions and the proof of the pattern.
- **Double-duty strings (cannot use the React hook):** pure lib functions that emit user-facing strings, notably `buildRationale` (a pure function called from a server action, builds the priority-tilt sentence), plus the regen / ramp-back nudge copy, `strength.ts`'s `reason`, and the recommendation reasons. Restructure to return keys / structured data and translate at a layer that has a translator (`getTranslations` in the server action), or pass a translator in. Do not leave them hardcoded.
- **Verify:** rendered app visually and textually identical to before across all views (zero copy changes). Deliverable: a summary of the namespace structure and any strings that were doing double duty or needed special interpolation handling.

### Infrastructure, typed domain vocabulary (magic strings → const-objects/unions, domain-grouped types)

**Bucket 2 (maintainability), file-opportunistic, NOT launch floor.** A refactor with no user-facing payoff; it must not compete with the launch floor or the validation gate, and must not jump them. Do it **domain-by-domain, opportunistically, when already in a domain for feature work**, the same incremental discipline as the i18n extraction above. A one-pass type-reorg is a large diff touching nearly every import, exactly the churn to avoid near the floor (breakage risk when stability matters most). Each domain moved keeps typecheck + the full suite green.

- **Goal:** eliminate scattered hard-coded domain strings (movement patterns, categories, adjustment kinds, biases, etc.) in favor of centralized, typed, single-source-of-truth values, and organize shared type declarations by domain.
- **Group by DOMAIN, not by language construct.** Do **not** create `/types`, `/interfaces`, `/enums` folders, folder-by-construct fights how the code changes (a domain's types/interfaces/values change together, so the split forces every conceptual change across three directories). Instead group shared declarations by domain area under `lib/pulse/types/` (e.g. `exercise.ts`, `generation.ts`, `program.ts`, `adherence.ts`), each holding the types, interfaces, AND domain value-constants for that domain together. One barrel `index.ts` if clean imports help.
- **Do not split `type` vs `interface` into separate locations**, the distinction is a local implementation choice in TypeScript, not an organizing principle; both are shape declarations and belong together.
- **Co-locate vs centralize (decide per type, don't force one bucket):** centralize the cross-cutting shared vocabulary (`MovementPattern`, `ExerciseCategory`, the core domain unions/values used by many modules) in `lib/pulse/types/`; keep module-private types co-located with their module.
- **No TypeScript `enum`.** Use the const-object + derived-union pattern (`as const` + `type Foo = typeof Foo[keyof typeof Foo]`). TS `enum` has runtime quirks and `const enum` has known problems with bundlers / isolated-modules (relevant to the Next/SWC build); the const-object gives the same named-constant ergonomics, autocomplete, and exhaustiveness with none of the footguns. **Reframe the work:** much of the vocabulary already exists as union types (`MovementPattern`, `ExerciseCategory`, `AdjustmentKind`, `Bias`, and now `DecisionEventType`/`DecisionTrigger`), so for most code this is "centralize the canonical values and make scattered inline strings reference the union/const," not "introduce enums", pair each union with a const object of its values where logic needs the runtime values.

### Infrastructure, monorepo extraction then full dependency upgrade

Two sequenced tasks; the **order is hard: extract first on current versions, verify and deploy, only then upgrade.** Both are isolated jobs, not feature work. **Timing:** after the variety preference branch merges and the current feature sprint settles -- the extraction is a large diff (all import paths, routing, service worker, CSP, Supabase URLs) and deserves a clean window with nothing else in flight. Sequencing note vs the i18n entry above: do i18n after the extraction, in the new repo, so two large diffs never collide.

**Destination (decided):** the existing `G:\Workspace\pulse-app` Turborepo monorepo, reset and re-scaffolded from a fresh Turborepo Next.js + pnpm starter. The monorepo structure is the right long-term home: `packages/lib` will hold the Pulse brain (`lib/pulse/*`) when a native app eventually arrives, and `packages/ui` will hold shared components. Mobile (`apps/mobile`) is excluded until the native reopen bar is cleared. Package manager for the monorepo: **pnpm** (stricter workspace isolation, better `workspace:*` protocol, already the monorepo convention).

**Monorepo target structure:**
```
pulse-app/
├── apps/
│   ├── web/          ← Next.js, Pulse PWA (all current Pulse code, paths flattened to root)
│   └── api/          ← empty scaffold, placeholder for a shared API surface when mobile arrives
├── packages/
│   ├── lib/          ← empty for now; destination for lib/pulse/* when web + mobile share logic
│   ├── ui/           ← empty for now; destination for shared components when mobile arrives
│   ├── eslint-config/
│   └── typescript-config/
├── turbo.json
└── package.json
```

**Path decision (decided):** flatten to root. Every `/pulse/*` route becomes a root route (`/train`, `/plan`, `/progress`, `/profile`, `/library`, `/login`). This changes the PWA `scope`/`start_url`, the service worker scope, the middleware matcher, and every Supabase Auth redirect URL. Two users is the right moment; post-launch is too late (installed PWAs and bookmarks break). Do not defer this decision.

**Task 1: Reset pulse-app and extract Pulse into `apps/web`.** Same code, new home, NO dependency changes during this step.
- Reset the pulse-app repo: remove the current obytes mobile template and Turborepo boilerplate. Scaffold a fresh Turborepo Next.js + pnpm monorepo with the structure above.
- Identify the full Pulse surface to move: routes (`(protected)` group + `login`), all `src/components/pulse/`, `src/lib/pulse/`, `src/lib/supabase/`, `src/hooks/pulse/`, `src/context/`, `src/app/pulse/`, `src/app/api/pulse/`, the PWA manifest, the `/pulse`-scoped service worker, migrations in `docs/migrations/`, and env vars. Flag anything shared with the portfolio as copy-not-move (`globals.css` Pulse tokens, Tailwind/PostCSS/tsconfig).
- Move code into `apps/web`. Fix all import paths (`@/*` alias carries over, confirm in `tsconfig.json` and `vitest.config.mjs`). Strip portfolio-specific assumptions (layout structure, root `globals.css` split, `next.config.mjs` global headers vs Pulse-only CSP).
- Flatten paths: remove the `/pulse` prefix from every route, update the middleware matcher (`/pulse/:path*` → all protected paths at root), update the service worker scope to `/`, update `next.config.mjs` CSP to match root paths.
- Carry env requirements into `.env.example` (Supabase URL/keys, no real secrets).
- Verify: `pnpm install`, `pnpm build`, `pnpm dev` all green; typecheck clean; full test suite passes; core flows work in the browser (auth, log a set, generation, PWA install).
- Manual steps (user does these): create/update Vercel project pointing at the new repo and `apps/web` as the root; update Supabase Auth redirect URLs in the dashboard to the new domain/paths; remove Pulse from the portfolio repo.

**Task 2: Full dependency upgrade.** Precondition: Task 1 done, deployed, and running correctly in production. Do not stack with the extraction.
- Produce an upgrade plan table first (dep / current / latest / risk) before touching anything.
- **Target: fully current stack.** Next 15 → 16 (official codemods, not hand-migration; watch caching defaults, async request APIs, Supabase SSR cookie handling, PWA service worker behaviour). React 19 is already current. Tailwind v4 is already current.
- **Upgrade in three isolated passes, each verified green before the next:**
  - Pass 1 (low-risk group): `swr`, `vitest` (2.x → 3.x is a major; config may shift), `eslint` + `eslint-config-next` (sync to the Next version), `typescript`, `prettier`, `prettier-plugin-tailwindcss`, `react-icons`, `@types/*`, `jsdom` (pin consistently), `postcss`, Tailwind v4 minors.
  - Pass 2 (Next major): Next 15 → 16 as its own isolated commit using official codemods. Verify core flows: auth, log a set, generation, progress, PWA install/offline. Report any behavioural changes, especially caching and RSC behaviour.
  - Pass 3 (Supabase): bump `@supabase/ssr` and `@supabase/supabase-js` against the upgraded Next. Never stack this with the Next major bump. `@supabase/ssr` is pre-1.0, treat a minor as potentially breaking; read its changelog and verify auth + data flows end-to-end.
- Output: plan table first, then the upgraded project with codemods handled and anything deliberately not upgraded (and why). User soak-tests on real training before merging. Upgrades only -- no features, no copy changes, no refactors beyond what an upgrade strictly requires.

### Native client, deferred direction (Expo + react-native-web)

A decision record, not a planned task. This settles **how** Pulse would go native **if** it ever does; **whether** it does is genuinely open and gated behind two gates, not one. Most likely it stays "not yet" for a long time, and that is a fine place for it to stay: the PWA already is the cross-platform app.

- **Unbundle "App Store presence" from "rewrite the client."** They are separate decisions. Store presence is testable cheaply by wrapping the existing PWA with Capacitor plus one genuine native feature (push) to clear Apple's guideline 4.2 (thin web wrappers get rejected). **The wrap tests discovery and trust only, not native feel**, so a positive wrap result is evidence for *staying wrapped*, not for rewriting. Don't let a good wrap justify the expensive bet. **iOS caveat (price it as a small project, not a config flag):** Capacitor renders in WKWebView, which does not reliably run service workers, so the SW-based offline *shell* needs Capacitor's native cache equivalent. The IndexedDB write queue (the data-safety layer) works fine in WKWebView, so offline writes survive the wrap, only the SW piece is re-homed.
- **The traffic motivation argues for the wrap, against the rewrite.** App-store discovery for an unknown solo app in a category owned by Hevy / Strong / Fitbod is effectively zero without paid UA or an existing audience; the store is a conversion-and-trust surface for traffic you drove there, not a discovery channel. That benefit is almost entirely captured by the cheap wrap. The rewrite buys native feel + watch, which is a *different* goal, confirm you actually have it before paying.
- **If the gates are cleared and native feel / watch is genuinely needed: route is Expo + react-native-web** (one codebase to iOS / Android / web), **not native-only RN**, RNW keeps a web target, so it protects the cross-platform moat instead of trading it away. Viable for Pulse specifically (audited): the data layer is already client-SWR (read path ports; supabase-js runs in RN; only the server-action mutations need rework), 14 of 20 `lib/pulse` files are web-independent (the brain ports verbatim), and NativeWind carries the Tailwind tokens. Cost: a full presentation-layer rewrite (App Router, service worker, server actions, all CSS do not port).
- **RNW keeps a web app, so the product stays linkable / usable-in-browser**, shareability is *not* lost (and the app is already behind login today). The real, smaller loss is web-app polish / SEO vs bespoke Next, handled by **keeping the Next marketing + landing site as the real-web SEO surface** (app screens are authed, never needed SEO). Escape hatch if the RNW web app proves too weak for the app itself: Next-for-web + RN-for-native sharing the brain (two UI layers, the maintenance cost RNW avoids), a build-time fork, not a now-decision.
- **Credit the no-install edge to the web target, not to deeplinks.** The indie distribution advantage is the *absence of an install step on the conversion path*: a link opens the running web app with zero friction, while a deeplink to a native app still resolves, for an *uninstalled* prospect, to an App Store download (the conversion cliff). Deeplinks only serve installed-user continuity. So the web target is what preserves cold-prospect conversion. **Do not drift to native-only on the theory "deeplinks handle sharing"**, that drops the no-install path, and deeplinks cannot recover it. Keep the web target and deeplinks are a bonus on top.
- **Knock-on effects on already-queued work:** the monorepo question reopens only at the point of committing to Expo (Expo universal *is* a monorepo: shared `packages/app` consumed by `apps/native` + `apps/web`), informed by two real apps rather than guessed at. The queued standalone-repo extraction should keep `lib/pulse` cleanly importable so it becomes that shared package with no re-extraction. The launch-floor offline-queue poison-pill fix doubles as separating queue *policy* from *storage*, which makes the eventual IndexedDB → SQLite/MMKV swap a swap, not a rewrite.
- **Reopen bar (a real gate, not a formality):** a committed active-user number, **plus** the validation gate cleared (completion-paced adaptive coaching proven to work for real users, native is downstream of the product being worth distributing wider), **plus** specific evidence the PWA itself is the wall: watch-centric lifters refusing to switch, iOS PWA storage eviction, or App Store being the channel you actually need.

### Social direction, deferred (not social-first)

A decision record, not a planned task. Pulse is a training coach, not a social network. This section documents the long-term direction so the distinction between "hard-out permanently" and "deferred, not ruled out" is clear and explicit, and so the right design constraints are captured before any social surface is built. Nothing here is scheduled work.

**The reframe.** "Not social" was always a shorthand for "not social-network-first, not engagement-optimised, not comparison-driven." It was never a permanent ban on features that help people train together, share useful training work, or hold each other accountable. The honest framing is: Pulse is not social-first. The adaptive coach is the product. Social features, if and when they arrive, support training adherence -- they do not replace it.

**The test for any proposed social feature.** Does it make training better, or does it only make the app more sticky? Features that only do the latter are out. A friend seeing your PR is useful accountability. A leaderboard ranking your PR against strangers is engagement farming.

**The four phases, as a documented direction.**

- **Phase 1: Routine link sharing.** A user can share a routine via a URL. The recipient sees the routine's structure and can install it. On install, Pulse adapts the routine to their equipment, session length, and preferences via the existing generation machinery. No social graph required. No account needed to view. No follower mechanics. Closest analogy: an export that someone else can run. This is the smallest useful first step and the foundation for Phase 4. *Reopen bar:* validation gate cleared, real users exist, and routine sharing has a clear observed use case (users asking how to share their program).
- **Phase 2: Friend activity.** Users can add friends (mutual accept required -- no follow-without-consent). Friends see a simple activity stream of training events: workout completed, new PR, program completed, streak milestone, first workout back after a gap. No engagement actions beyond a single congratulate tap (no like counts displayed). Activity is a secondary surface, never the home screen. *Reopen bar:* Phase 1 shipped, enough active users that friend activity feels alive for at least some of them.
- **Phase 3: Private accountability groups.** Small closed groups (2-20 people) for friends, family, or gym partners. The group sees the same Phase 2 activity events scoped to members. No public discovery of groups, no public member lists, no rankings within the group. *Reopen bar:* Phase 2 shipped, evidence that bilateral friend activity isn't sufficient for the accountability use case.
- **Phase 4: Public routine pages.** A user can make a routine public, which generates a browsable page showing the split, exercises, equipment required, program length, and copy count. No public user profile. No follower count. Light signals: copy count (not likes), optional 1-5 rating on install. Users browse without an account; installing requires one. A content moderation plan is required before this ships. *Reopen bar:* Phase 1 shipped, real user base exists, evidence that routine discovery is a meaningful acquisition channel.

**Hard-out list (permanent, not deferred).** Global leaderboards or competitive rankings. Public follower counts displayed anywhere. Feed-first home screen. Viral challenges or streak wars. Likes or reactions on body or progress photos. Network/comparison achievements. DMs or in-app messaging (high moderation burden, low training value). Creator economy or monetised profiles. Follow without mutual consent. Public profiles that surface personal body data.

**Domain model to consider when the time comes (not schema yet).**

- Phase 1: `routines.visibility` ('private' | 'link_only' | 'public'), `routines.share_token` uuid, `routines.slug` text, public route `/r/[share_token]`.
- Phase 2: `user_connections(id, requester_id, addressee_id, status, created_at)` status: pending | accepted | blocked. `activity_events(id, user_id, event_type, payload jsonb, visibility, created_at)` -- event_type: workout_completed | pr_achieved | program_completed | streak_milestone | first_back_after_gap. Visibility: friends_only (default) | group_only | private.
- Phase 3: `groups(id, name, created_by, created_at)`, `group_members(group_id, user_id, role, joined_at)`. Activity events gain optional `group_id` scoping.
- Phase 4: `routine_installs(id, routine_id, installed_by, installed_at)` for copy count. `routine_ratings(id, routine_id, rated_by, rating smallint, created_at)`, one per user per routine.
- Never: a general `followers` table (asymmetric follow moves toward the Twitter model), engagement metrics stored as columns (view counts, like counts -- these optimise for the wrong thing), personal body data joined to any public-facing table.

**Privacy and safety constraints to carry from the start.** Private by default on every new social object. Friend requests require mutual acceptance. GDPR erasure cascades to all social graph data (connections, activity events, group memberships). Account deletion removes all of it. No body photo sharing in any social context. Activity events are friends-only by default, never public by default. Phase 4 moderation plan (routine names and descriptions are user-generated text, even if benign) must exist before public routines ship. No username search / user discovery until Phase 2, and even then prefer invite-by-link over open search.

**What does not change now.** Auth surface, account profiles, visibility settings, and the Train screen are all unchanged. Phase 1 (routine sharing) is the only phase that touches existing surfaces (adding a share button to a routine and a public-facing read-only route). Phases 2-4 each add new surfaces rather than modifying existing ones, which keeps the blast radius of each phase contained.

**Risks.** (1) Social gravity: each phase makes the next easier to justify. The hard-out list above is only useful if it is actively defended at each phase gate, not just written once. (2) Network effects in Phase 2: friend activity needs a cluster of users training simultaneously to feel alive. If the user base is small when Phase 2 ships, the feature will feel empty. Phase 1 has standalone value; Phase 2 does not. (3) Moderation cliff in Phase 4: user-generated text at scale requires a plan before it ships, not after. (4) Auth surface expansion: friend-adding requires user discovery. Username/handles or email-based invites are both new surfaces with non-trivial privacy and abuse implications. Phase 1 avoids this entirely.

### Monetization philosophy, decision record (not a planned task)

A decision record, capturing the principle that should govern pricing once the auth + billing lifecycle (launch floor, above) actually gets built, so choices made under launch pressure don't drift from what the product already promises. The launch-floor bullet above names the *mechanics* (a clean cancel / refund / billing-transparency path, the positioning note that lifetime pricing fits the no-dark-patterns stance); this names the *principle* behind them, so the mechanics have something to answer to.

- **The test.** Premium pays for the adaptive coaching, personalization, generation, adaptation, the Coach Decision Timeline, the actual moat, never for the basic logging loop. A free tier that can log a workout, run one program, and watch real progress is the floor; paywalling the core loop turns a training partner into a toll booth and contradicts "an adaptive coach, not a logger" at the business-model layer, not just the marketing layer. Define this free / paid line on purpose, before any paywall gets built, not as a side effect of "whichever surfaces were easiest to gate in code."
- **Carried from the launch floor (restated for the record, not new policy):** no ads, no selling user data (matches the "calm, private, no surveillance" stance already stated for analytics); no dark patterns, no confusing cancellation, no charges after account deletion (already named the #1 complaint driver across the Runna review sweep in the launch-floor bullet above); a one-time / lifetime option carries real weight as a subscription-fatigue antidote and a trust signal even if the eventual call stays subscription-with-honest-terms; account deletion and billing cancellation read as one clean flow, not two separate frustrations to survive.
- **New to this record.** The free / paid line itself (logging, one program, and basic progress free; generation personalization, the Coach Decision Timeline, and deeper analytics as the premium surface is the natural cut, matching what the product is actually for) and the pricing structure (subscription vs lifetime vs hybrid) are product decisions with real consequences, not implementation details to default on. Decide them at the same table as the billing-lifecycle build, not earlier (pricing decided pre-validation calcifies around assumptions the validation block may overturn) and not as an afterthought once the billing code already exists.
- **Reopen bar.** This stays a principle, not a build, until the auth + billing lifecycle item is actually scheduled, that is the moment the free / paid line and the pricing structure stop being a decision record and become a real build decision that needs answering before the first paywall ships.

### Infrastructure, product analytics (PostHog, EU), post-launch

A backlog decision record, not a launch-floor item. Instruments the **public** phase, once there are strangers to learn from; it does **not** gate launch and must not delay it.

- **Tool: PostHog, EU Cloud region.** Chosen over Vercel Analytics / GA because the need is **product analytics** (event-level user behavior), not web analytics (pageviews / traffic). The **EU region is required, not optional**: Pulse is NL-based, privacy-positioned (no ads, no surveillance, a calm space), launching to an EU audience, data stays in the EU and the GDPR story stays simple and honest.
- **Posture: instrument for learning, not surveillance.** Do **not** track-everything-figure-it-out-later, that is the surveillance default the positioning rejects, and a solo dev won't analyze undirected event noise anyway. Define a small, deliberate event set (~a dozen), each tied to a question already on the validation gate: **adaptive events firing** (ramp-back offered, deload triggered, behind/lapsed flagged); **override behavior** (deload followed vs dismissed, ramp-back accepted vs ignored, lighten-this-week used); **core-loop completion** (workout started vs finished, sets logged per session); **feel-coached surfaces** (Coach Timeline opened, adaptation explanations seen). Nothing beyond that set.
- **GDPR / consent is a hard requirement, the same legal floor as account-deletion + a privacy policy.** Behavioral analytics needs a truthful privacy-policy disclosure and a consent story (cookie/consent banner, with the privacy-preserving default selected). Analytics does not skip the legal floor; those legal pages are themselves launch-floor work.
- **Sequencing, the two-user validation gate does NOT need PostHog.** The gate is covered by the `DecisionEvent` log (Phase 0 #3 / Tier 1 #12), which captures the adaptive events as first-class rows in our own Supabase, no third party, no consent story for our own data, so validation runs off `DecisionEvent` + direct DB queries on our own training. PostHog is therefore deferred until real users exist whom you can't eyeball in the DB. Don't build it pre-launch.
- **Guard: analytics tells what, not why.** The deepest validation question, does completion-paced coaching actually keep someone progressing, is a judgment from watching real training, not a dashboard number. Instrument to *support* that judgment, never to replace it.

---

## Later

The Hold bucket from the re-tiered roadmap, in detail. Web-native moderate-value items first, then data-gated work, then native-platform and scale-gated items last. (Progress photos was promoted to Tier 3 #9 on 2026-06-06; CSV export and Muscle priority shipped earlier.)

| Feature | Notes |
|---|---|
| Expand template library | Evaluate and add high-demand named programs (Starting Strength, StrongLifts 5x5, PHUL, PHAT, PPL variants, Juggernaut Method, etc.) from the top-30 list reviewed 2026-06-07. Gated behind the "Templates vs generation, pick a role" product decision (see above) -- if templates are staying as the "named program I trust" anchor, this is the content work that populates that lane. Do not build before that decision is made. |
| Supersets (advanced) | Tri-sets, giant sets, AMRAP tracking. After basic superset support (shipped). Niche. |
| Achievements + gamification | Milestones: first set, full week, PR, full 12-week cycle, streak records. Implement after real usage data, badges only land well at milestones users actually reach. (also in: Jefit, Boostcamp) |
| Year / period in review | Shareable annual and monthly recap of volume, PRs, streaks, and milestones. Retention and organic reach. Needs a year of data to be meaningful. (also in: Hevy, Jefit, Boostcamp) |
| AI workout generation (v2) | Rule-based generation from onboarding is shipped. v2 adapts split, volume, and exercise selection based on actual logged performance. Needs months of logged data to not feel random, premature at current scale. Superseded for now by Tier 3 #7 (behavior-driven adaptation), which gets most of the benefit without the data requirement or the black-box feel. (also in: Fitbod, Jefit, Boostcamp, Alpha, Setgraph) |
| ExerciseCard memo effectiveness | `ExerciseCard` is wrapped in `React.memo`, but the save path still passes the whole `logs`/`prMap` objects (new refs on every save) so all cards re-render anyway. Slice per-card data upstream in `LogView` (own set entries + savedCount + per-exercise PR) so unchanged cards keep stable props and a save touches one card. Refactor of the hottest screen, defer until it actually hurts at scale. |
| Apple Health / Google Fit sync | Import bodyweight / activity from wearables and calorie trackers; export workouts. Native-gated, HealthKit is iOS-native (no web API) and Google has been moving Fit to Android-native Health Connect, so this means going off the web (Capacitor/native). Low ROI at two users. (also in: Hevy, Strong, Fitbod, Jefit, Caliber) |
| Wearable integration | Garmin, Apple Watch, Whoop. Heart rate during sets, auto rest timer from HRV. Native-platform integration, same off-the-web cost as Health/Fit sync; far off. |
| Social / sharing | Friends feed, likes, follow. Requires critical user mass. Not before traction. |
| Expand exercise library, 94 to 200-250 | Pulse has 94 global exercises with a complete 6-field metadata seed (`secondary_muscles`, `fatigue_cost`, `unilateral`, `joint_stress`, `difficulty`, `substitution_class`, the 2026-06-06 metadata migration); competitors run from the high hundreds into the thousands. This is content work, not engine work, the existing seed-migration pattern and metadata schema scale as-is and the generator needs no change (94 with rich metadata is already "enough to ship Phase 0-2", per the engine-direction notes above). The case for doing it anyway: a thin pool caps substitution quality as movement restrictions (#5) and equipment profiles (#6) deepen, more candidates per slot means better same-pattern, same-equipment alternatives once a pattern or a piece of kit gets excluded. Not a blocker for either, an enhancer, sequence loosely alongside or after them. Target 200-250 metadata-complete entries weighted toward common-gym / home-gym / dumbbell-only / cable / machine / travel substitutions over raw count, tagging discipline matters more than the number (a poorly-tagged exercise pollutes the generator pool worse than a missing one). |
| Myoreps / rest-pause set type | A distinct set structure, not a `drops` variant (work a set close to failure, rest 3-5 breaths, repeat short clusters to a rep target), on MacroFactor Workouts' set-type list; Pulse ships drop sets + a failure tag but not this. **Code-truth:** there is no `set_type` concept today, set kinds are inferred (a non-empty `drops` jsonb array means a drop set, `rir = 0` means failure, warmup sets are computed client-side and never persisted), so this is genuinely new structure, not a `drops` extension: a set-type field, a multi-segment rest-pause UI/timer in `SetLogger`, and rep-attribution changes in the progression / volume math (deciding which reps count toward the working total and the e1RM). Niche, evidence-based-hypertrophy-segment signal that the target user skews toward; low priority, scope properly when picked up rather than bolting onto `drops`. |
| Per-side (bilateral asymmetry) tracking | MacroFactor Workouts logs left and right separately for unilateral lifts, useful for users training around an imbalance or a return from injury. Pulse already tags `unilateral` on exercise metadata (seeded across all 94 exercises) but the field is purely descriptive today, generation has exactly one comment referencing it and no behavior keys off it. **Code-truth:** zero per-side infrastructure exists, no left/right columns on `set_logs`, no UI, no aggregation. Adding it safely (per-side logging + a read-only asymmetry report) is moderate work; wiring per-side data into progression (which side sets next week's target?) is high-risk and not recommended as a first cut. Low priority for the current segment, becomes genuinely relevant on the movement-restrictions / injury-mode path (#5, #14) where imbalance tracking earns its keep, scope to logging + reporting only if it is ever picked up. |
| Travel mode (extension of Equipment profiles, #6) | "I'm away for two weeks, hotel gym / dumbbells only" temporarily swaps the active equipment set and auto-reverts when the window closes. **Not a standalone feature, a thin follow-on to #6:** equipment is captured transiently per generation today (`RoutineSetupFlow` feeds `generation.ts`'s `hasEquipment` filter directly) and nothing persists, the `Profile` type has no equipment field. Tier 2 #6's persistent equipment-set storage plus its switch-and-regenerate UI is the real prerequisite; once that lands, travel mode is a small expiry wrapper on top of it (a `travel_mode_expires_at` timestamp checked at generation time, reverting to the saved default set), reusing the same `hasEquipment` filter end to end. No existing temporary / time-boxed concept anywhere in the codebase to build on. Sequence strictly after #6, building it first would mean building the persistence layer twice. |

---

## Deprioritized (evaluated 2026-06-04, not planned)

From a ChatGPT feature audit. Real ideas, but low value at current scale or trust-risky, revisit only if the rationale changes.

Ordered by revisit-likelihood: small/standalone ideas first, then those gated on a future v2 adaptive engine, then the trust-risky or heavy-modeling ones last.

| Feature | Why not |
|---|---|
| 4-week program block | Too short to hold an accumulation → intensification → deload arc; it is a different program *type* (minicut / travel / peak), not a periodization length. Serve later as a short-block template if needed, not via `buildProgram`. |
| 24-week program block | Redundant with the repeating-block design: running the 12-week block twice yields two deloads (better spacing than one long ramp), and most users never finish a 24-week monolith. Program lengths stay at 8 / 10 / 12 / 16, default 12; each is already a distinct phase table (not a stretched copy). |
| Recovery warnings (e.g. legs Mon+Tue) | Schedule is user-chosen and small; low value. Smallest standalone rule if ever revisited. |
| Program coherence score (X/100) | Decorative for a deterministic generator that's already correct by construction (slot-based, equipment-filtered, volume floors). It can't emit an incoherent program. Only earns its keep guarding a fuzzy v2 adaptive engine. |
| Dynamic volume management | Needs the v2 adaptive engine + logged data. Bundle with v2 if pursued. |
| Exercise recommendation AI | Vague; rule-based selection + the shipped preferences/swap already cover the real need. |
| Goal forecasting ("86 kg in 12 weeks") | Easy to get wrong; a missed prediction erodes trust more than it motivates. |
| Muscle recovery / fatigue modeling | Fitbod's signature, but heavy modeling for marginal payoff at two users and risks feeling pseudo-scientific. |

---

## Reference & archive

_Dated market/strategy context, the round-by-round carry log, and the full Shipped history. Everything forward-looking is above this line._

---

## Competitive analysis (2026-06-03)

Reviewed 8 trackers: Hevy, Strong, Fitbod, Jefit, Boostcamp, Alpha Progression, Caliber, Setgraph. _Historical reference, all five "biggest gaps" below have since shipped; kept for the differentiation framing._

Biggest gaps versus the field:

- Live PR detection. Every serious tracker (Hevy, Boostcamp, Caliber, Strong) flags a personal record the moment you hit it. Pulse only surfaces PRs after the workout. Pulse already computes E1RM, so this is a small add with a big motivation payoff.
- Per-muscle weekly volume. Hevy, Fitbod, Jefit, Boostcamp, and Strong all show sets per muscle group per week, often as a body-diagram heat map. Pulse has a 10-category taxonomy and a volume-by-type chart already, so it can derive this from exercise tags.
- Plate calculator. Hevy, Strong, Boostcamp, Setgraph, and Caliber all ship one. It is a pure function with near-zero backend cost and removes real friction at the rack.
- Rich set types. Drop sets and failure sets are standard in Hevy, Strong, and Boostcamp. Pulse only has warmup and working sets.
- Mid-workout exercise swap. Boostcamp, Fitbod, Alpha, and Caliber let you swap a busy machine for a similar exercise and carry your weights across. High friction reducer for real gyms.

Differentiation opportunities:

- Recovery-aware volume nudges. Combine per-muscle volume with the existing RIR data to flag under- and over-trained muscles. Fitbod and Boostcamp do versions of this, but none pair it tightly with a fixed 12-week progressive-overload plan the way Pulse can.
- A single Strength Score. Caliber and Boostcamp prove a 0-100 composite metric drives engagement. Pulse already has E1RM PRs to compute it from, and it gives non-experts a legible headline number.
- Stay private and fast. Strong and Setgraph win on being distraction-free with no social noise. Pulse can lean into solo progressive overload as a positioning choice and skip the heavy social race that needs user mass.

---

## Strategic review (2026-06-06)

Two external product reviews (ChatGPT), cross-checked against the code. Net: the engine and feature depth are already competitive. The next work is deepening the adaptive and personalization moat and sharpening positioning, not adding generic tracker features.

**Validated direction:** completion-paced progression, ramp-back, auto-deload, adherence awareness, and staying private/fast are the right bets. The reviews' "do not build" lists (AI chatbot, social, leaderboards, wearables, native, fatigue modeling, goal prediction) match the Deprioritized / Later sections.

**Already shipped (reviews listed these as "future", do not rebuild):** exercise dislikes / hide (`user_exercise_preferences`, generation filters them out); same-movement-pattern swap with weight carryover; generation `rationale` ("why this plan"); gender-agnostic split selection with a `priority_muscle` tilt (the reviews' "use priorities, not gender" mistake is the exact thing Pulse already fixed); schedule-to-split mapping by day count (`STYLES`, `recommendStyle`); movement-pattern slots filled by equipment (`EMPHASES`). One review explicitly called the movement-pattern-slot design a strength.

**Taken into the plan:** lead with the one-line promise + cross-device / home-gym wedge; close the personalization gap in generation (training style, variety, multi-priority, restrictions); surface the adaptation decisions; build behavior-driven adaptation as the pre-AI personalization step.

---

## Carried implementation detail (round 3, 2026-06-05)

These two items are folded into the re-tiered roadmap above (desktop right rail → Tier 1 #1; guided set-row polish → Tier 1 #2). The detailed live-review spec is kept below for whoever implements them.

Selected 2026-06-05 (round 3), live-review follow-up on the guided screen.

| # | Feature | Notes |
|---|---------|-------|
| 1 | Guided set-row polish (Editorial draft C) | The WorkoutModeScreen redesign shipped and the shell is on-design, but the guided **set rows** don't yet match the approved Editorial draft (`docs/superpowers/designs/2026-06-05-workout-mode-set-rows{,-desktop}.html`). Three gaps seen in live review: **(a) single-active focus**, show only the *next* unsaved set as the active field-box row and render the remaining sets as dimmed "Set N, not started · target RIR" idle rows; today the `editorial` `SetLogger` variant renders every unsaved set as a full input form, so a 3-set exercise stacks three forms instead of one focal active set + idle previews. This is a guided-mode behavioural change (SetLogger has no "idle/preview" state today, Train must keep showing all inputs, so gate it behind the `editorial` variant or lift the active-set choice into `WorkoutModeScreen`/`StepBody`). **(b) field-box sizing**, the Weight box is `flex-1` and balloons full-width on desktop with a large gap before Save; make Weight/Reps compact fixed-width boxes grouped with the Save button and an inline target hint, matching draft C proportions on both mobile and desktop. **(c) logged-row declutter**, the editorial logged row crams RIR · PR · plate · Edit · ✕ · ✓ on the right; keep value · PR · RIR · ✓ visible and move Edit/delete behind a hover affordance or overflow menu. Files: `SetLogger.tsx` (editorial variant) + `WorkoutModeScreen.tsx`. **Direction (live review):** the Train `card` *logged* row is the clean look to keep, mirror that for the guided logged row (this is really what (c) is about) and don't over-decorate logged/done rows; the **unlogged/input** row is where the design effort goes. Open scope question for next session: keep the reworked unlogged-row design guided-only (`editorial` variant) or also give the Train `card` unlogged rows the cleaner treatment, the user likes the Train logged row but feels its unlogged row needs work too. Whatever the choice, keep the behavioural-contract tests green. |
| 2 | Desktop right context rail, fill it out | The `/train` desktop right rail (`DesktopLayout`, the "TODAY" panel: sets done / total, session-in-progress, streak, session volume, target intensity, pinned rest timer, phase line) reads sparse with a lot of empty space below the content, counter to the dense-layout preference. Revise it to carry useful, glanceable context instead of padding. Candidates (pick a coherent subset): this-week adherence / per-day completion, **per-muscle weekly volume** (Pulse has the 10-category taxonomy + RIR data, also a standing competitive-analysis gap), this week's PRs, an "up next" / upcoming-session preview, or a small volume-trend sparkline. File: `DesktopLayout.tsx`. |

_Everything else from the 2026-06-03 → 06-05 selection queues has shipped; the per-date "shipped" log that used to live here was retired as redundant with the Shipped section at the bottom of this file._

---

## Shipped

- **Movement restrictions** (Tier 2 #5; 2026-06-08, `feature/movement-restrictions`, in review): the first injury-aware generation input, materially widening the addressable audience (home/gym lifters training around a cranky joint). A `RestrictionFlag` union ('knee' / 'lower_back' / 'shoulder' / 'wrist') plus a per-exercise `contraindications: RestrictionFlag[]` metadata column drive `isContraindicated`, a **hard pool filter** applied beside `hasEquipment` in `generateRoutine`. It is deliberately never relaxed (unlike the equipment thin-pool / heavy-dedup / unilateral-cap relax fallbacks): a flagged lift is never re-added to fill a slot, and if a restriction empties a movement pattern the existing GQ1 backfill covers it from the remaining safe patterns. The empty/undefined-restriction path is byte-identical to base (golden identity test across 3/4/6 days). Two migrations (hand-applied in Supabase): `profiles.movement_restrictions text[]` and `exercises.contraindications text[]` with per-exercise tags seeded against the corrected catalog (knee: Barbell Squat / Hack Squat / Bulgarian Split Squat / Walking Lunge / Leg Extension; lower_back: the deadlift/RDL/row family; shoulder: overhead barbell press / upright row / dips; wrist: straight-bar press / push-up / barbell curl), with the seeding invariant that a safe leg + push option always survives each single flag. Threaded through `generateAndSaveRoutine` (param wins over stored, then `[]`) with a **safety-aware write-back**: restrictions persist only when the param is explicitly passed, so a re-generate that omits the setup step can never silently clear a stored flag. Collected in an optional `RoutineSetupFlow` step (multi-select chips, `TemplatesTab` opts out) and a standing **Profile editor** (`updateMovementRestrictions` action + optimistic hook + context). v1 is a pure subtractive filter, safe lifts surface because they are what remains, with no preferred-substitute front-loading (documented boundary; future hook is GQ3's `substitution_class`). Two external spec reviews folded (write-back sentinel, RLS inheritance note, name-specific exclusion + 3-day invariant tests). +8 tests (identity, name-specific exclusion, single-flag-never-empties-leg-work over 6-day and 3-day, two-flag union). Suite: typecheck clean, **887 tests**.
- **GQ3, role-aware exercise ranking** (Tier 2; 2026-06-08, `feature/gq3-role-aware-ranking`): five focused fixes to `generation.ts` that close the gap between the generator's pattern-level slot logic and how a human coach actually picks exercises, plus a small data correction. (1) **Anchor-aware fatigue sort**: for the six `COMPOUND_ANCHOR_PATTERNS` (squat / hinge / horizontal_push / vertical_push / horizontal_pull / vertical_pull) the higher-fatigue option now wins when both are fresh, the better anchor *because* it drives more adaptation (Barbell Squat over Goblet Squat, Bench Press over Push-Up), inverting GQ2's lower-fatigue-first tiebreak for anchors only; accessory slots are unaffected and fresh still beats used regardless. (2) **Substitution-class cross-session dedup**: a routine-wide `usedSubstitutionClasses` set (mirrors the `used` avoid-set) soft-deprioritizes a second exercise from a substitution class already seated elsewhere, so Romanian Deadlift on Pull no longer crowds Legs with another hinge variant when a distinct alternative (Good Morning) exists. (3) **Unilateral cap**: at most one `unilateral: true` exercise per session, a *hard* block once filled (`unilateralFilled` / `relaxedUnilateralCap`, faithfully mirroring `HEAVY_DEDUP_PATTERNS` / `heavyPatternFilled` / `relaxedHeavyCap` including the thin-pool relax-on-exhaustion fallback), so Walking Lunge no longer seats alongside Bulgarian Split Squat or Step-Up. (4) **Front Raise suppression**: a new `front_delt_isolation` substitution class (split out from `lateral_raise`, which keeps the legitimate lateral-raise family) is soft-deprioritized once a vertical press fills the session, pressing already loads the front delts, but Front Raise still surfaces when it is the only shoulder-isolation option (soft, not a hard block). (5) **Data correction** (migration `2026-06-08-gq3-exercise-data-corrections.sql`): Smith Machine Bench Press re-tagged `{machines, bench}` (was `{barbell, bench}`, leaking into barbell-only home-gym pools, a Smith machine is a fixed-rail machine, not a barbell). `ExerciseMeta` and the pool query gain `substitution_class: string | null` and `unilateral: boolean`. Two pre-existing GQ2 golden tests that asserted lower-fatigue-wins on the now-inverted `squat` anchor pattern were rewritten onto accessory patterns (`shoulder_iso` / `glute_iso`); their squat-based assertions moved to a new `GQ3: anchor patterns prefer higher fatigue` block asserting the inverted behavior. +6 tests (anchor ranking, substitution-class dedup, unilateral cap, Front Raise suppression x2, Smith Machine equipment gating). Suite: typecheck clean, **882 tests**. The "weekly structure validation" half of this item (a repair pass for malformed weekly structures) is held pending a real reproduction, not shipped here.
- Variety preference generation input (Tier 2 #4, second of three; 2026-06-07, in review on `feature/variety-preference`): a `VarietyPreference` ('consistent' | 'varied') axis. Under `consistent`, a per-generation, never-persisted anchor map (`Map<MovementPattern, exerciseId>`) pins the main compound lifts (`COMPOUND_ANCHOR_PATTERNS` = squat / hinge / horizontal_push / vertical_push / horizontal_pull / vertical_pull) across sessions while accessories keep rotating; `varied` (default, null-resolved at the generation boundary) reproduces today's output byte-identical, locked by a golden identity + `consistent`-determinism test. Anchor lookup takes precedence over the avoid-set for compound slots. Fully orthogonal to training style. Persisted on `profiles.variety_preference` (nullable, mirrors `training_style`), with an optional `RoutineSetupFlow` step (`collectVariety`). Two external reviews folded (Claude.ai coaching, Perplexity architecture). Migration `2026-06-07-15-13-53-variety-preference.sql`. Loading lean (the third #4 input) deferred to its own branch.
- Training style generation input (Tier 2 #4, first of three; 2026-06-07, merged **#85**): Balanced / Strength / Bodybuilding / Powerbuilding, a `resolveBias` remap (Balanced = identity) + `resolveRepRange` (Powerbuilding heavy-pattern split), persisted on `profiles.training_style`, with a `RoutineSetupFlow` step. Migration `2026-06-07-12-58-20-training-style.sql`.
- Fast workout logging (ExerciseCard, set logger, RIR)
- 12-week progressive overload programme structure (phases, RIR targets)
- Rest timer (auto-start, persistent)
- Personal records (E1RM-based PR map, per-exercise), top 5 shown in Profile
- Streak tracking (consecutive weeks trained)
- Bodyweight logging + SVG chart (date picker, delete, last 30 entries)
- Goal weight, target with distance-to-go display in Profile
- Body measurements logging, date-stamped waist / hips / chest / arms entries (cm)
- Progressive overload suggestions, SetLogger pre-fills weight from previous week adjusted for RIR delta
- Exercise library (global seed + user-created, filterable by category)
- Expanded exercise category taxonomy (chest / back / shoulders / triceps / biceps / legs / glutes / calves / abs / other)
- Routine builder (create, add exercises, set starting weights, reorder, delete)
- 17 global routine templates, Full Body, Upper/Lower, PPL, Push/Pull, Bro Split, Arnold Split (DB / Home Gym / Gym variants) + Glute Focus, Lower Body, Full Body Tone (female-focused, Gym / Dumbbells)
- 6-step onboarding modal with rule-based recommendation engine (equipment → experience → goal → days/week → specific days → session time → template match)
- Templates tab in Library (equipment filter, description, one-tap clone)
- Dynamic volume scaling, sessionTime adjusts sets/exercises at clone time (~30 min / 45–60 min / 90+ min)
- Workout history (per-week session cards with set replay and PR badges)
- `workout_type` on `routine_exercises` + `routine_schedule` table (dynamic per-day workout tabs)
- Weekly schedule in onboarding (day picker, auto-fills schedule from template pattern)
- DayTabs, Mon–Sun training day selector with workout-type subtitle, today dot, logged-set badge
- Next.js App Router pages (`/pulse/train`, `/pulse/plan`, `/pulse/progress`, `/pulse/profile`, `/pulse/library`)
- Desktop layout via `DesktopLayout` component (≥ 1024 px)
- Route group `(protected)`, login page excluded from auth layout, no more redirect loop
- Progress & analytics page, StreakCalendar (12-dot weekly filled), VolumeChart (sets/week by type), E1RMChart (progression line with exercise picker + PR marker), BestLifts summary, session history with name resolution and PR badges
- Toast notification system, `ToastProvider` + `useToast` + `ToastContainer`; replaces inline `saveError` banner; auto-dismiss 4 s, hover-pause, max 3 stacked; success toasts on name/unit save
- Last session display in ExerciseCard, `computeLastSession` utility; shows "Last: 80 kg × 8 × 3 sets" below subtitle
- Notes per set, free-text per-exercise note keyed `week-routineExerciseId`; `useNotes` hook, `/api/pulse/notes` route, inline edit/clear UI in ExerciseCard
- Per-exercise rest timer override, `rest_seconds` column on `routine_exercises`; editable in LibraryView routine editor; `fireTrigger` passes duration to RestTimer
- A/B exercise variation, `variant` column on `routine_exercises` + `routine_schedule`; `WorkoutTabs` with A/B switching; `routineExercisesByTabKey` computed in provider; `nextVariant` pure function; PPL-style routines get auto-seeded A/B variants at clone time
- Workout sessions, `workout_sessions` table (start/complete timestamps); POST `/api/pulse/sessions` + PATCH `/api/pulse/sessions/:id`; `useWorkoutSession` hook; `WorkoutModeScreen` full-screen guided mode with exercise-by-exercise navigation and early-finish
- Warmup set generator, `computeWarmupSets` pure function; 3 steps at 50%×5 / 65%×3 / 80%×1; rounds to nearest 2.5 kg / 5 lb; shown above working sets in ExerciseCard when expanded; hidden below 40 kg working weight
- Workout share card, `computeShareStats` utility; `ShareCard` full-screen overlay shown automatically after finishing a workout; shows workout label, duration, sets, week, top 3 lifts with weights, PR badges, PR count; "Screenshot to share" CTA; variant exercise snapshot preserved
- Bug fixes sprint, Profile PRs now show exercise names (not UUIDs); Plan view sections filtered by scheduled workout types; WorkoutModeScreen shows instantly on Start Workout (session creates in background); Onboarding re-triggers whenever user has no routines
- UX polish sprint, onboarding re-triggers on /train when no routines (prominent empty-state CTA added); auto-activate another routine when active one is deleted; Dumbbell naming consistency throughout exercise library; editable default sets/reps per exercise in Library
- Desktop layout overhaul, two-column sidebar layout for ≥ 1024 px: 180px fixed sidebar (logo, week badge, nav links, streak, active routine context card, sign out) + scrollable content column with rest timer pinned to the bottom
- Slate UX redesign, full reskin to the dark "Slate" direction with a coral accent (Hanken Grotesk + Sora), three-zone desktop shell (icon rail + content + context rail) with a CSS-driven responsive split, tone-shift surfaces over borders
- Live PR detection, `isSetPR` flags a new E1RM personal record the moment a set is logged, inline coral PR badge on the set row plus a quiet toast, in ExerciseCard and WorkoutModeScreen
- Per-muscle weekly volume, `computePerMuscleVolume` derives sets per muscle category for the week from the existing taxonomy; `MuscleVolumeBars` horizontal bar list on Progress
- Plate calculator, `computePlates` pure function (barbell + loadable dumbbell, default bar/handle weights and plate set); compact affordance in SetLogger showing the per-side plate breakdown for the target weight
- Rich set types, drop sets stored in a `drops` jsonb column on `set_logs` plus a failure tag at RIR 0; drop-set editor in SetLogger, rendered in ExerciseCard, WorkoutModeScreen, and history
- Supersets, pair two routine exercises as a superset; merged card in the train screen; rest timer fires once after both exercises' sets; guided mode treats the pair as one step; Pair / Unpair in the routine editor with pair-aware reorder
- Exercise instructions, `exercise_instructions` table (read-only RLS) with primary/secondary muscles + technique cues per global exercise; on-demand `ExerciseInstructionModal` opened from a "How to perform" affordance in ExerciseCard and an info icon in the Library exercise list; ~92 exercises seeded
- Rule-based routine generation, `generation.ts` engine builds a routine from onboarding inputs: split selection by days/experience, session-time-driven volume (with floors so 30-min full body never collapses to one exercise), A/B variations for repeated focuses, exercise selection by equipment + movement pattern; `generateAndSaveRoutine` action; reusable stepped `RoutineSetupFlow` with prominent Generate entry points (Train empty state, Plan, Routines); template "Use this" opens the same flow instead of native dialogs
- Routine editor session grouping, active routine groups by the session type the schedule uses (`sessionTypeFor`): full body shows Full Body, upper/lower shows Upper/Lower (with A/B variants), never the granular push/pull/legs the exercises are tagged with
- Routine rename, inline rename per routine in the Library (`renameRoutine` action + optimistic hook)
- Collapsible desktop sidebar, left rail toggles between a 74px icon rail ("P" mark) and a 208px labelled rail (full "Pulse" wordmark); choice persists in localStorage
- Scroll-rail muscle filter, single non-wrapping horizontal rail with a per-category count badge and a fade edge, replacing the wrapped 11-chip row
- Profile polish, streak shown as a coral hero stat; login screen and skeleton loader reskinned to Slate tokens
- Auto-progression, `computeProgression` double-progression engine: climbs reps within the rep range, then adds weight and resets to the bottom; SetLogger pre-fills both weight and reps and shows the next target, deloading when a set came in harder than the target RIR
- Instant loading (phase 1 of offline-first), shell-first render; data fetched client-side via SWR with per-view Slate skeletons; user-scoped localStorage SWR cache (cleared on logout) makes warm visits instant via stale-while-revalidate
- Routine generation redesign, emphasis-based generation so repeated focuses differ by design; program-style picker (Full Body / Emphasis Days / PPL / Upper-Lower / PPL+FB / Heavy-Med-Pump / ULPPL etc., keyed by day count); cross-session avoid-set variation; rep ranges by bias + lift type (incl. pump); auto-supersets for 30-min sessions; equipment correctness (every global exercise tagged + `pull_up_bar` key); A/B variants generalized to A-D with a per-day `routine_schedule.variant` pin
- Audit cleanup, input validation, perf (targeted log writes, ExerciseCard precompute, HistoryView single pass, swrCache throttle), and DRY/maintainability (label-map collapse, TabKey comparator + `baseWorkoutType` helpers, `ui.ts` consolidation, `actions.ts`/`RoutinesTab.tsx` splits); `explore` route renamed to `library`
- Exercise preferences (hide / never-show), `user_exercise_preferences` table (RLS); `setExercisePreference` action + `/api/pulse/preferences` GET + `usePreferences` hook; `hiddenExerciseIds` on context; hide/unhide + "Show hidden" in the Library; generation filters hidden exercises out of the pool; non-destructive "Hidden" marker in the routine editor
- Mid-workout exercise swap, week-scoped `exercise_swaps` (per routine_exercise + week); same-movement-pattern candidate picker (`swapCandidates`) excluding hidden + in-session; `resolveExercise` overrides the displayed exercise for that week only (logs/PRs/volume stay slot-keyed); weight carries over via the existing slot suggestion; available from ExerciseCard, guided mode, and reflected in history
- Routine generation explainability, generated routines persist a human-readable `rationale` (built from the onboarding inputs + the style's `bestFor`); shown as a "Why this plan" line on the Plan screen and previewed live in the setup flow's final step
- Weekly per-muscle volume targets, goal-agnostic `VOLUME_TARGETS` set-range table + pure `computeVolumeProgress`; the Progress per-muscle bars show progress toward each target with "N sets to go" nudges and a lagging-muscle summary
- Recomp dashboard, a Progress headline card combining strength (summed best E1RM/week), bodyweight, and waist trends into a plain-language verdict ("You're recomping…", "Gaining…", "Keep logging…") with per-signal deltas; also adds the body-measurements read path (previously write-only)
- Offline-first logging (phase 2 of offline-first), IndexedDB offline write queue for set logs + notes: writes enqueue when offline or on network failure (optimistic SWR mutate keeps the UI live), flushed FIFO on reconnect / focus / mount with last-write-wins; pending-sync indicator on the train screen; installable PWA web manifest scoped to `/pulse`; `/pulse`-scoped service worker (navigation network-first with cached-shell fallback, static assets cache-first, skips `/api`, cleans old caches on activate); CSP gains `worker-src` + `manifest-src 'self'`
- Gender in profile, nullable `gender` column on `profiles` (male/female); Gender toggle in Profile + an optional onboarding step (skipped once set); `recommendStyle(count, gender)` lightly biases the default program style for female users (4-day → Aesthetic Upper/Lower, 3-day → Full Body Emphasis Days); also feeds the Strength Score. Deeper gender-specific volume/emphasis tuning is deferred to the Muscle priority item
- Strength Score, 0-100 relative-strength composite (`computeStrengthScore` in `strength.ts`): each main lift's best E1RM ÷ latest bodyweight is scored against gender-specific bodyweight-multiple standards (bench / squat / deadlift / OHP) via piecewise-linear interpolation across Beginner→Elite, averaged to one headline number with a level label and per-lift breakdown; `StrengthScoreCard` headline on Progress, with a CTA when gender / bodyweight / a main lift is missing
- Recovery-aware volume nudges, `computeRecoveryFlags` pairs each muscle's weekly working-set count with its average RIR to classify under (below floor), high fatigue (in range, avg RIR ≤ 0.5), over target (above ceiling), or on track; surfaced as a dedicated `RecoveryCard` on Progress that triages the muscles needing attention first (originally inline chips on the volume-by-muscle bars; no change when no targets/recovery passed)
- Rest timer auto-advance, guided mode (`WorkoutModeScreen`) now shows a visible rest timer and, with the Profile "Auto-advance rest timer" toggle on, jumps to the next exercise when rest ends and the current exercise is fully logged; the global pinned timer is suppressed while guided mode is open so only one timer runs (`RestTimer` gains an `onComplete` callback, `workoutModeOpen` + `autoAdvance` on context)
- Progress dashboard re-tier, feature-placement pass after the screen had grown three competing headlines plus the full log: Strength Score + the promoted `RecoveryCard` lead in a two-up row, then Recomp, then the training-trends grid (volume, e1RM, volume-by-muscle, streak, Best Lifts), with Personal Records moved over from Profile; Session History collapses to the last 4 sessions (most recent first) behind a "Show all N" toggle. Same component renders dense single-column on mobile. Layout-only; all `HistoryView` data memos preserved
- Body measurement units (cm / in), `length_unit` ('cm' | 'in') preference on `profiles` mirroring the weight `unit`; `toLengthDisplay`/`toCm` helpers convert for display and parse inputs back to canonical cm (no data migration). A compact cm/in toggle in the Profile measurements header recalculates in real time, alongside a new latest-measurements readout (waist / hips / chest / arms, previously write-only) and inch-aware inputs; the Recomp waist delta honours the unit too. Profile is slimmed to identity + settings on the left and one unified Body block (bodyweight, goal, measurements) on the right, with PRs and the streak hero removed (now on Progress)
- Recovery detail chips, `computeRecoveryFlags` now returns a `RecoveryDetail` (status plus sets, avg RIR, target band, sets-to-go); the Progress `RecoveryCard` surfaces per-muscle chips with the detail string ("high fatigue · RIR 0.4", "add volume · N to go", "over target · N sets", "on track") and token-based tints (coral for fatigue, green for on track)
- Progress time window, Week / Cycle / All segmented control in the Progress header; a windowed-logs filter scopes volume, e1RM, the recomp trend, and Session History. Strength Score and Personal Records stay all-time (lifetime records, not trends). `cycle` = weeks 1–12 (default, matches prior behaviour), `all` = every logged week, `week` = the active week; cycle and all are distinct in code and coincide until data spans multiple cycles
- Clearer nav icons, Train house → dumbbell, Plan menu-lines → calendar, Library magnifier → stacked books, swapped in both `BottomNav` and the desktop rail; Progress (bar chart) and Profile (person) kept as-is
- Profile Body trend chips, a "feeds Recomp" badge on the Body block plus bodyweight and waist trend chips (the delta of the two most recent entries, `pulse-success` when trending down, `pulse-dim` when up)
- Shared `PageTitle`, one heading component (matching the Progress header) used on Progress, Profile, Plan, and Library so every view except Train opens with the same title treatment
- Train layout consolidation, the four stacked top strips (week/phase/RIR metadata, the day/workout tabs, the 12-week dot strip, the detached Start button) fold into a single context-bar card: metadata + a compact `‹ Wk N ›` week stepper + Start on top, the day/workout tabs below. The expanded `ExerciseCard` groups its secondary actions (How to perform / Swap / Note) into one chip-style meta row instead of stacked links. Layout only; all wiring preserved
- Plan restructure, a `PageTitle` + Generate header, then a single program-header card (phase + week notes + rationale, with an inline week stepper) replacing the separate phase card / WeekSelector / "Why this plan" blocks. Below it, a full-width column: a card holding the weekly schedule + a taller, two-label weekly-volume chart whose bars are clickable to jump to that week (`setActiveWeek`), then the per-session exercise breakdown
- Library unification, one shared toolbar and one row-card style across the Exercises, Routines, and Templates tabs, plus a `PageTitle`, so the three tabs read as one library. A shared `FilterChips` rail (Exercises categories + Templates equipment) stays a single row and scrolls, swipe/trackpad on touch, mouse-wheel-to-horizontal on desktop, so every chip is reachable without stacking into many rows on narrow screens
- Progress trends as cards, the trends grid (volume, e1RM, volume-by-muscle, streak, Best Lifts, Personal Records) is wrapped in `bg-pulse-surface` cards on a tighter gap, matching the card idiom the redesigned screens use
- Muscle priority + gender-aware generation, persistent `priority_muscle` on `profiles` (`glutes | legs | chest | back | shoulders | arms | balanced`, nullable). The generator tilts each session's emphasis toward the priority's movement patterns (front-loading them where the session already trains that muscle, never injecting into unrelated days) via a pure `tiltEmphasis`; the Progress weekly volume targets bump for that muscle (`priorityAdjustedTargets`) so the recovery nudges stay coherent. Gender now only seeds the default (female → glutes) via `genderDefault`, applied server-side at generation when no explicit choice exists; the old female-only `recommendStyle` bias is retired. Editable as a "Training priority" setting in Profile. (Deferred: an explicit priority step inside the generation flow, the Profile control + server-side seeding cover the default.)
- Template metadata + Templates-tab filters, `routine_templates` gain `goal` (`build_muscle | lose_fat | general_fitness`) and `gender_fit` (`any | female`) columns, seeded across the 17 templates (Full Body → general fitness, Full Body Tone → lose fat + female, Glute Focus → female, rest build-muscle / any). The Templates tab gains a shared filter model (`templateFilters.ts`, pure + unit-tested): Goal and Equipment as always-visible `FilterChips` rails, with Experience / Days-per-week / a "For you" gender-fit toggle behind a Filters expander (active-count badge); all AND-combined. "For you" keeps gender-neutral templates plus ones matching the user's gender
- CSV data export, a pure, unit-tested `buildWorkoutCsv` produces one row per logged set (Week, Exercise, Set, Weight (kg), Reps, RIR, PR, Drop sets) with RFC-4180 escaping; weights stay in canonical kg. The export moved up to `PulseProvider` so it can resolve exercise names (honouring week-scoped swaps) and PR flags, replacing the old nameless JSON dump. Its button moved out of the mobile header into a "Data" section in Profile, reachable on every device
- Accent colour selection, users can pick their accent from a preset palette (Coral default, Emerald, Sky, Violet, Amber, Rose) in Profile. The choice persists on `profiles.accent_color` (a preset key) and is applied by overriding the `--color-pulse-accent` / `--color-pulse-accent-dim` tokens at the document root, so it cascades to every accent usage (Tailwind classes + `var()`). `ACCENT_PRESETS` is one constant; null = the coral default. No new component beyond a swatch row.
- Program start-day selection + false-"behind" fix, fixed the adherence over-count where a session scheduled for *today* counted as overdue before the day passed (`computeProgramPosition` now counts only days strictly before today, matching `computeWeekAdherence`), so a mid-week start no longer reads as "behind". Added a user-settable program start date in Plan (a date input + "Today"), via a `setProgramAnchor` action/hook/context that updates `program_anchor`. The anchor drives only the calendar overlay, not completion-paced progression, so changing it never alters logged data. No migration.
- Onboarding day-count fix, the "which days" picker now caps selection at the chosen days-per-week (`MAX_TRAINING_DAYS`: 2–3→3, 4→4, 5–6→6), so picking 6 day-slots after answering "4 days" can no longer resolve a 6-session style. Stepping back to a lower frequency trims the over-selection. Fixes a routine-generation mismatch for new users.
- Auto-applied deload for stalled lifts, the plateau nudge becomes actionable instead of advisory. When a lift stalls (no e1RM gain in ~3 weeks via `computePlateau`) and isn't already rebuilding, its next set targets auto-deload: `SetLogger` prefills ~90% of the previous weight (rounded to the 2.5 kg grid) with reps reset to the bottom of the range, shown as a `↓ deload target`. Self-limiting via `recentDrop` (a ≥3% e1RM dip in the last ~3 weeks suppresses re-deloading), so it deloads once then lets the lift rebuild. Fully derived from logs (`shouldDeload`/`deloadTarget` in utils.ts), no new storage, action, or migration. The ExerciseCard "Stalled" card explains it. (Deferred: guided WorkoutModeScreen + SupersetCard deload prefill, a follow-up; the workout-mode redesign was visual-only.)
- Adaptive missed-workout regeneration, the program becomes **calendar-aware and completion-paced**. A routine gets a `program_anchor` (set on its first completed session; existing routines backfilled), and the user's `timezone` is captured silently. A pure engine (`adherence.ts`) attributes completed `workout_sessions` to program weeks ordinally, so missing a session just slides the timeline forward instead of forcing a restart; the calendar layer flags when you're behind or have lapsed. On Train, a non-destructive nudge (same pattern as the plateau nudge) offers a **ramp-back week** after a gap of `GAP_DAYS` (10), reduced volume + easier RIR, *inserted* so no real progression week is lost, or points you at a still-open session when you're just behind. Accept/dismiss persist in an append-only `program_adjustments` table (one decision per week, atomic upsert). The week stepper now defaults to and follows the completion-paced current week (with a jump-to-current control), and a fixed `clampWeek`-at-12 bug that broke the repeating-block / 16-week designs was corrected. All engine logic unit-tested; server-portable for a future adherence cron. (Deferred: multi-tier ramp-back for very long layoffs; push/email nudges; balance-aware re-sequencing.)
- Dynamic periodized programs, the static 12-week block becomes a **repeating block of a user-chosen length** (8/10/12/16; `program_weeks` on the routine, default 12). A pure `buildProgram(weeks)` defines each block (12 = the legacy tables); `getPhase`/`getRIR`/`volumeForWeek` are block-length-aware and wrap (`weekInBlock`) so the program repeats indefinitely, week 13 of a 12-week block = block 2 week 1, deloading at each block end. Plan gets a Program-length picker and reads the routine's block for the phase card / volume chart / week stepper; Train's header RIR + stepper and `SetLogger`'s target RIR follow the same length; the week steppers no longer cap (you advance into the next block). Stage 2: `computePlateau` flags a main lift with no e1RM gain over ~3 weeks and surfaces a non-destructive nudge on the exercise (suggested deload/swap, reusing the existing swap path). All pure logic unit-tested. (Deferred: strength-calibration test week; auto-applied deload that rewrites next week's targets, currently advisory.)
- Train header + day-strip redesign, the `/train` program status card and weekly day selector rebuilt in the bold "Focus" direction (frontend-design). A Big Shoulders Display week block (`01 /16` + phase · target RIR) replaces the text status line, with a gradient Start CTA derived from the themeable accent via `color-mix`. `DayTabs` becomes an elevated 7-day rail: per-day workout label plus a thin progress bar (track + accent fill + `done/total`), coral active day, dashed rest days, today dot. Added the self-hosted `Big Shoulders Display` display font (`--font-pulse-display`, no CSP change). All adaptive-regen chips (status, jump-to-current, ramp-back card) preserved.
- WorkoutModeScreen redesign, the guided workout screen rebuilt responsively (frontend-design). **Mobile = "Focus"**: bold Big-Shoulders exercise name + set-progress ring and per-step progress pips that fill to each step's real done/total. **Desktop = two-pane**: a left focus pane (hero + set rows) and a right 392px "This session" sidebar with overall pips, a clickable whole-session exercise track (progress spine, jump-to-step), and a docked rest timer + sets-logged / sets-left stats + actions. Set rows use an "editorial" `SetLogger` variant (hairline rows, "Set N" label, big display value) scoped to guided mode so the Train cards stay untouched; the rest bar stays `RestTimer`. Each exercise carries How-to-perform + Swap + Note (parity with the Train card), and primary actions use the app's standard coral button. Exactly one layout mounts via `useMediaQuery` (hardened against a missing `matchMedia`), keeping a single DOM and the full behavioural contract (auto-advance, superset-round gating, PR toast, mid-session swap, step clamp unchanged). Shipped after two adversarial review passes (AA-contrast, accessible naming) plus live-feedback polish. (Deferred: guided-mode deload prefill; a one-active-set focus that dims not-started rows; the mockup's green "done" token, intentionally kept coral to follow the themeable accent.)
- Offline-queue cross-user safety, queued offline writes (set logs + notes) now carry the owning `userId`; `flushQueue(userId)` replays only the current session's writes and leaves another user's queued writes untouched (never replayed under the wrong account, never dropped), so a shared device can no longer land one user's offline writes in another's account on the next sign-in. Sign-out drains the current user's queue while still authenticated, then clears the per-user SWR cache. Pre-public hardening alongside the live RLS audit (all user-data tables verified owner-scoped in prod). Unit-tested in `offlineSync`.
- Decimal comma input, weight inputs accept "2,5" as well as "2.5". A pure `parseDecimalInput` normalizes the comma before parsing (replacing bare `parseFloat`, which truncated "2,5" to 2 and silently corrupted the logged weight for European/NL keyboards), and the weight/reps inputs gain `inputMode="decimal"`/`"numeric"` for the correct mobile keypad. Pre-public input-correctness fix; unit-tested.
- Rest timer survives phone lock, the rest countdown is anchored to a wall-clock end time and recomputed on `visibilitychange`/`focus`, instead of a per-second `setTimeout` decrement that pauses when mobile suspends background timers between sets. The timer and its completion beep now stay accurate after the phone locks. Core-loop reliability fix; unit-tested (incl. a suspended-tab resume case).
- Conversational coaching copy, the everyday progression line now reads like a coach instead of a glyph. The Train card shows `Last 90 kg × 8` then `Go 95 kg × 8` (deload: `Back off to ...`); guided mode shows a spoken sentence ("Last time you hit 90 kg × 7. Go for 95 kg × 8." and a deliberate-backoff sentence on a deload). RIR stays on its own chip so the words never repeat it. The Plan "Why this plan" rationale now also names the muscle each session tilts toward (`buildRationale` takes the resolved priority), the one adaptation that was previously silent. Copy-only on the surfaces; aria-labels, logic, and tests preserved.
- Generation v2 Phase 0 #1, `muscleMap.ts`: the canonical weighted MovementPattern → muscle bridge (`PATTERN_MUSCLE_MAP`, the frozen §1 table, 15 patterns → the 10 categories, weights sum to 1.0) plus `muscleContributions` / `primaryMuscle`. Pure module, no migration, golden-tested (locks the frozen weights and the squat-vs-hinge glute-proportion invariant). Granularity decision A (keep the 10 categories; the 15 patterns stay the generation control layer). Spec: `docs/superpowers/plans/2026-06-06-09-48-10-muscle-map-phase0-1.md`.
- Generation v2 Phase 0 #2, exercise metadata + seed: six new columns on `exercises` (`secondary_muscles`, `unilateral`, `fatigue_cost`, `joint_stress`, `difficulty`, `substitution_class`) seeded for all 94 global exercises (`2026-06-06-11-28-49-exercise-metadata-fields-seed.sql`, idempotent `add column if not exists` + a fail-loud `do $` guard). Prerequisite: ~12 old ILIKE-seeded `movement_pattern` mis-tags corrected first (`2026-06-06-10-51-33-movement-pattern-correction.sql`) so the bridge attributes to the right muscles, with the three vocab-gap judgment calls (Leg Curl / Leg Extension / Rack Pull) science-validated.
- Generation v2 Phase 0, fractional per-muscle volume wiring: the muscle bridge now feeds the live Progress bars. `secondarySets` buckets each pattern's non-primary weights (≥0.20 → 0.5 set, 0.10–0.19 → 0.25, below dropped, excluding the exercise's own category to avoid double-count), and `accumulatePerMuscle` credits every logged set its primary 1.0 plus those fractions, wired through `computePerMuscleVolume`, `computeRecoveryFlags` (RIR averaged per touched category), and `historyBundle.ts` (the real Progress production path; `queries.ts` now selects `movement_pattern`). `VOLUME_TARGETS` retuned upward for the new secondary inflow (arms/shoulders most). UI rounds to half-sets (`roundSets`). Golden-tested in `muscleMap`/`utils`/`historyBundle`; on `feature/fractional-volume` pending review (changes visible Progress numbers).
- Guided set-row polish (Tier 1 #2, Editorial draft C, guided-only). Single-active focus via a new `active` prop on `SetLogger` (default true, so the Train `card` variant is untouched): `WorkoutModeScreen` marks the first unsaved set active and the rest render as dimmed "not started" previews. Inputs regrouped (balanced Weight/Reps boxes + Save in-row), logged row decluttered to value · PR · RIR · ✓ with Edit/delete de-emphasized (brighten on hover/focus, kept visible for touch), and the plate button dropped from the editorial logged row. Opportunistic a11y Q8: both guided overlay roots gained `role="dialog"` + `aria-modal` + label. Behavioural-contract tests stayed green; +2 tests for the active/idle behavior. 775 tests; on `feature/guided-set-row-polish`.
- Per-muscle volume in the desktop right rail (Tier 1 #1, completes the item). `RailMuscleVolume` fills the previously-sparse rail with this week's per-muscle volume vs target: all 9 targeted muscles, lagging-first, a thin bar filling toward the ceiling with a tick at the weekly floor, turning success-green once the floor is hit ("This week" heading). Reuses `computePerMuscleVolume(logs, activeRoutine.exercises, currentWeek)` + the priority-tilted `VOLUME_TARGETS` + `computeVolumeProgress`, so the numbers match the Progress view exactly; desktop-only (the rail does not exist on mobile). Design draft `docs/superpowers/designs/2026-06-06-17-10-50-rail-per-muscle-volume-draft.html`; 773 tests; on `feature/rail-per-muscle-volume`.
- Exercise-instruction content review, populated the 5 global exercises that had no instructions (Dumbbell Pullover, Dumbbell Shrug, Decline Dumbbell Press, Single-Leg Glute Bridge, Dumbbell Push Press) and corrected 9 existing entries: Deadlift / Rack Pull primaries relabeled to the posterior chain (Glutes, Hamstrings, Lower Back) with an explanatory lat-bracing cue, Plank glutes demoted to secondary, the Romanian Deadlifts gained Lower Back, and Chest removed from the four tricep-extension entries (it does not extend the elbow). Added one new display label, `Lower Back` (instruction muscle columns are free-text, rendered as plain chips, so no code change). Content from the Claude.ai science review, reconciled against code-truth (names, no em dashes, render-safety) and shipped as `2026-06-06-16-56-42-exercise-instructions-review.sql` (applied). Reusable coverage query at `docs/queries/exercise-instructions-review.sql`.
- Em-dash sweep, removed em dashes from the live application and maintained docs (`src/**` 114 occurrences, `CLAUDE.md`, `docs/roadmap.md`) per the no-em-dash copy rule. Spaced ` — ` became `, ` (template-name separators became ` - `); the lone `—` empty-value placeholder (a "no data" cell) was deliberately kept. Supabase: a content-cleanup migration (`2026-06-06-16-32-34-em-dash-content-cleanup.sql`) fixes the seeded `routine_templates` names/descriptions + `exercise_instructions.cues` (apply manually; en dashes in ranges like `45–75°` left alone), and `docs/queries/exercise-instructions-review.sql` is a reusable query to review instruction coverage + discover remaining em dashes. The historical `docs/superpowers/**` + `docs/audits/**` archives were intentionally left. On `feature/em-dash-sweep`; 771 tests green.
- Ramp-back / lighten copy honesty fix, the ramp-back nudge, the Train week banner, and the `decisionCopy` ramp_back/manual strings promised "reduced volume," but the volume cut is unconsumed (only the `+1 RIR` is felt). Reworded everywhere to promise only the easier RIR; the inert `RAMPBACK_VOLUME_FACTOR` is now flagged in `constants.ts` and the real volume-cut work is tracked as Tier 2 #7. Surfaced by the dose reconciliation with Claude AI; on `feature/ramp-back-copy-fix`.
- Lighten this week (Tier 1 #11) + priority-tilt why line (Tier 1 #1, non-visual half), autonomous batch on `feature/tier1-quick-wins`. **Lighten:** `manual_deload` `AdjustmentKind` + `manual` `DecisionTrigger`; `progressionInfo` lightens the week (`isRampBack`) without the re-entry offset (one-off easier week, progression continues); `lightenThisWeek` action writes the adjustment + dual-writes a `ramp_back`/`manual` decision event (so it lands in the Coach Timeline with distinct copy); kind-aware Train banner + a "Go easier this week" button; migration widens the `program_adjustments.kind` CHECK. **Priority why line:** pure `priorityFocusLine(priority)` caption above the per-muscle volume bars on Progress. Spec `docs/superpowers/specs/2026-06-06-15-43-44-...md`; migration `2026-06-06-15-58-32-manual-deload-adjustment-kind.sql`; 771 tests; second-opinion code review caught the (now-added) migration as a blocker. Deferred: manual-deload undo, button busy-state (idempotent + optimistic unmount make it low-risk).
- Coach Decision Timeline (Tier 3 #10, increment 1), first surfacing of the `DecisionEvent` log. Read path `loadDecisionEvents` → `/api/pulse/decisions` → `useDecisionEvents` → context `decisions`. Pure `decisionCopy(event, name)` maps each type to what-changed / why / what-next copy (progression branches weight-advance vs rep-advance off the stored magnitude); `groupDecisionsByWeek` for the headers. `CoachPanel` renders a compact card (desktop right rail as a plain block; Train inline as a surface card, `lg:hidden` so it doesn't double with the rail) and a full week-grouped overlay (`role="dialog"`, aria-modal, Escape + backdrop close). Progression uses the existing `pulse-success` token, others the themeable accent. `acceptReentryDeload` now also stamps `daysAway` into the ramp-back event magnitude so the copy can say "11 days since your last session". Unit-tested: `decisionCopy`/`groupDecisionsByWeek` (8) + `CoachPanel` (4). Draft `docs/superpowers/designs/2026-06-06-15-08-34-coach-decision-timeline-draft.html`; 759 tests; on `feature/coach-decision-timeline`.
- Generation v2 Phase 0 #3, session-linked logging + unified `DecisionEvent` log (closes Phase 0): `set_logs` gains `session_id` (nullable FK → `workout_sessions`, `on delete set null`) + `workout_date` (user-local day, computed client-side from `profile.timezone` and stamped at save so an offline write keeps the day it happened, not the day it flushes, threaded through `updateLog` → `runMutation` → `upsertLog` positionally, back-compatible with pre-change queued writes). New `decision_events` table (`type` ramp_back/deload/progression/swap, `trigger`, `affected_area` `''`-sentinel for program-wide, `week`, `magnitude` jsonb, `confidence`, idempotent `unique(user,routine,type,affected_area,week)`, owner RLS). Pure `decisionForExercise` (utils) mirrors what `ExerciseCard`/`SetLogger` already compute, deload wins over progression, progression only on a genuine advance, and the provider derives + fires it at the **set-save choke point** (`logSet`, success-gated dedupe ref), so components are untouched and it stays best-effort/re-derivable (not queued). Ramp-back dual-written from `acceptReentryDeload`. `recordDecisionEvent` action validates via `validateDecisionEvent` + `assertOwnsRoutine`. Migration `2026-06-06-14-30-53-session-linked-logs-decision-events.sql`; 747 tests; on `feature/decision-events-phase0`.
