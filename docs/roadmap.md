# Pulse, Product Roadmap

## Product vision & positioning

**Pulse is an adaptive strength coach that works on any device.** Build a plan, track your lifts, and it adapts when life gets in the way so you keep progressing for years, not weeks. The periodization, adherence, ramp-back, and deload logic are the mechanism behind that promise, not the pitch.

**The moat:**
- Completion-paced programs (you advance by finishing cycles, not by the calendar).
- Ramp-back weeks after a layoff, auto-deload on stalled lifts, adherence awareness (on-track / behind / lapsed).
- Deterministic, equipment-aware generation that is genuinely strong for home-gym, dumbbell-only, and travel setups.
- Cross-device and offline-first (any browser, no ecosystem lock-in), the gap Apple-only apps like Liftin cannot fill.

**Target user:** the training-first lifter (strength and hypertrophy), at home or in a gym, who wants structure without a spreadsheet. Not a social network, not a running / CrossFit / rehab app, not an AI chatbot. Expand to adjacent users (for example movement restrictions) through the generation engine, never by diluting the core.

**Design principle:** simple surface, ignorable intelligence. The app carries a lot of smarts; the UI stays calm and lets users ignore most of it until it matters.

**Not building** (focus guards, all evaluated): AI chatbot, LLM-generated workouts, heavy fatigue modeling, goal prediction, native apps and wearables until real traction (the native *how* is settled and the *whether* is held open under "Native client, deferred direction" below).

**Social -- hard-out permanently:** global leaderboards, competitive rankings, public follower counts, feed-first home screen (Train is always the home screen), viral challenges, engagement farming mechanics, likes or reactions on body or progress photos, network/comparison achievements (self-referential milestones -- your own PRs / streaks / volume bests -- are on-vision; only the comparative kind stays hard-out), DMs or in-app messaging, creator economy or paid content. (Achievements split into two lanes: self-referential milestones are post-launch-OK and on-vision; only the network/social kind stays hard-out, since it pulls toward the social-network anti-goal.)

**Social -- deferred, not ruled out:** lightweight training-community features (routine link sharing, friend activity, private accountability groups, public routine pages) are a documented long-term direction, not a near-term build. The distinction is "not social-first", not "not social". See "Social direction, deferred" below.

---

## Status & next session (2026-06-15)

**In progress:** Generation calibration round 2 (external-review-driven), `feature/generation-calibration-round2`. Spec `docs/superpowers/specs/2026-06-16-18-19-41-generation-calibration-round2-design.md`; driven by the 20-routine LLM review + code-grounded reconciliation in `docs/audits/2026-06-16-18-19-41-20-routine-review-reconciliation.md`.

**Designed + planned, ready to execute (gated on user "go"), Laldy rebrand + repo extraction:** the Pulse → **Laldy** rebrand and extraction of the app out of this personal-portfolio repo into its own `laldy` Turborepo monorepo (repo created at `/Users/christiaan/Documents/Workspace/laldy`). Full design in `docs/superpowers/specs/2026-06-15-14-48-46-laldy-rebrand-extraction-design.md`, phased executable plan in `docs/superpowers/plans/2026-06-15-laldy-rebrand-extraction.md` (codebase analysis + Perplexity/ChatGPT/Claude.ai loop, reconciled against code-truth). Locked: monorepo (pnpm, `nodeLinker: hoisted`); app at **`go.laldy.app`** (routes flattened to that subdomain's root), marketing reserved at apex `laldy.app` as a `noindex` static placeholder; two Vercel projects, same Supabase project (data carries over, config-only); **brand-neutral** internal rename (brand lives only in ~25 user-facing strings + manifest); the **dependency upgrade kept OUT of the cutover** (migrate + cut over on the current stack, soak ~1-2 weeks on real training, then upgrade as a separate fast-follow deploy, reversed the fold-in after a unanimous review-loop verdict: there's only one cutover either way, so folding in just stacks a Next major on the same middleware/CSP/cookie surfaces the migration disturbs). One migration sprint, one cutover. See the **monorepo-extraction** infra section below for the superseded prior plan. Replaces the old "flatten to apex root / `G:\Workspace\pulse-app`" decision.

**Shipped, context-sensitive exercise scoring (`feature/generation-context-scoring`, 2026-06-16):** replaces the flat `ISOLATION_QUALITY` selection score with a context-aware one, plus four related levers, all gated to no-op on nameless/metadata-absent pools so the byte-identity goldens stay byte-identical. (1) Four additive `exercises` columns (`quality`, `rep_min`, `rep_max`, `attributes`); `quality` migrates the `ISOLATION_QUALITY` map into a column (`isolationQuality` reads it, still feeds gap-fill's `qualityOf`). (2) A pure **`contextScore`** (`quality` + bounded style affinity + graded rep-fit bonus + saturating weekly-repeat penalty, returns a breakdown) replacing the ISOLATION_QUALITY comparator layer in `byPattern`, non-anchor patterns only. (3) Per-exercise rep windows doing double duty: a gross-mismatch SELECTION layer (all patterns, above `anchorRank`) + an assignment CLAMP (`clampRepsToWindow`), so Dumbbell Push Press loses selection to a real press and clamps to 3-5 on hypertrophy days, Step-Up never gets 3-6. (4) Style distinctiveness via a style-aware `anchorRank` (per-style `STYLE_PROFILES.canonicalReorder`; Bodybuilding leads incline/machine/cable primaries on chest/back/squat) + a bounded style affinity (objective `attributes`, not style labels); Balanced is the neutral identity (goldens byte-identical). (5) A weekly isolation-repetition soft cap (routine-wide `usedCount` -> saturating penalty) + a `back_iso` peer re-rank (Straight-Arm Pulldown above Pullover, a live inversion the #153 calibration left). (6) A warnings-only compound-carryover credit in `muscleCoverageGaps` (per-pattern `CARRYOVER_CREDITS`, default 0.0; squat/hinge/lunge credit nothing so hamstring gaps still warn); gap-fill stays direct-only. Spec-first through a two-round Claude.ai + Perplexity + ChatGPT loop (the per-pattern carryover map, replacing a dangerous flat 0.5 that would mask hamstring gaps, and the 3-pattern style reorder were the blocking round-2 fixes); ordering-invariant + anchor-guard tests lock the magnitude bands; catalogue names verified live before seeding. Migration `2026-06-16-15-56-45-exercise-scoring-metadata.sql` (hand-apply on merge, then `--refresh` the `scripts/gen-routine.ts` cache). Suite **1710**, typecheck clean; subagent-driven per commit-sized group, reviewed integration diffs + a whole-branch review pass (no critical/important findings). Spec `docs/superpowers/specs/2026-06-16-14-57-31-context-sensitive-exercise-scoring-design.md`; plan `docs/superpowers/plans/2026-06-16-context-sensitive-exercise-scoring.md`. Real-catalogue selection (which actual exercises land) is the post-migration smoke (synthetic goldens cannot confirm it).

**Shipped, generation calibration pass, Spec 3.1 (`feature/generation-calibration`, 2026-06-16):** an evidence-driven calibration of the slot-first generator after the Tier-2 muscle-coverage work, from a two-round ChatGPT + Perplexity review loop over 10 real-catalogue routines (`docs/audits/2026-06-16-12-25-04-generation-samples-for-review.md`). Six bounded changes, no new architecture: **(A)** `floorRepRangeForLoad` raises a dumbbell-only LOWER-BODY COMPOUND (squat/hinge/lunge) below 10 reps to 10-15 (goblet squat / DB RDL / split squat were getting un-loadable 6-8); name-gated like ISOLATION_QUALITY so synthetic goldens are byte-identical. **(B)** the flat `MUSCLE_COVERAGE_FLOOR` became a frequency-scaled `coverageFloor(muscle, dayCount)` (accessory floors climb at 4-6 days, capped at 8). **(C)** chest added to gap-fill as a conditional target with a flat low floor of 6 (catches the catastrophic lows; compounds carry it otherwise). **(D)** gap-fill Phase 2 restructured to distribute volume across the lowest-current-representation eligible sessions, with a per-exercise contribution cap (gap-fill-added sets <= the exercise's base, so an isolation never exceeds 2x base and never becomes mostly gap-fill). **(E)** catalogue re-tags: Dips -> compound chest press (`horizontal_push`), Straight-Arm Pulldown -> lat isolation (`back_iso`), with `deriveSeedPrimaryMuscle` aligned. Plus a correctness fix found in verification: `pickIsolationForMuscle` now filters `!is_compound`, so gap-fill never seats a compound for chest/hamstrings/glutes (bench/RDL/hip-thrust carry those as primary). Gated on muscle attribution + name, so all generation goldens stay byte-identical. Migration `2026-06-16-13-33-44-exercise-dips-straightarm-reclassify.sql` (hand-apply on merge, then refresh `scripts/.catalog-cache.json` to see the re-tags in `gen-routine.ts`). Suite **1685**, typecheck clean; reviewed per-piece + a whole-branch code-review pass. Spec `docs/superpowers/specs/2026-06-16-12-43-23-generation-calibration-design.md`; plan `docs/superpowers/plans/2026-06-16-generation-calibration.md`. (Context-sensitive exercise scoring shipped next, see the bullet above; then the Laldy rebrand.)

**Shipped, generation hypertrophy quality pass (`feature/generation-hypertrophy-fixes`, 2026-06-16):** a pre-validation quality pass on the slot-first generator from a Claude.ai-code-truth + ChatGPT + Perplexity loop over real-catalogue outputs (ChatGPT re-scored the baseline 6.5 -> 8.5/10), driven by a new committed diagnostic `scripts/gen-routine.ts` (runs the real `generateRoutine` against the real seeded catalogue from a config; the seed of a future deterministic generation gate). Shipped: #2 full-body Balanced heavy day floors to 6-8 (not a 3-6 power day); #4 push/pull validator drops the undifferentiated `shoulders` muscle from the press bucket (chest+triceps vs back+biceps); #1 full-body 30-min build-muscle session earns an isolation slot via supersets without dropping a compound; **#3 isolation-ranking quality** (a name-keyed `ISOLATION_QUALITY` score map wired into `byPattern` above the fatigue tiebreak on isolation patterns only, so the engine stops defaulting to Concentration Curl / Front Raise / Tricep Kickback; goldens byte-identical, real-catalogue effect confirmed via `gen-routine.ts`); **#4-refinement** the push/pull imbalance threshold is now **directional** (press-heavy flags at 1.5, the posture risk; pull-heavy tolerated to 2.0, since a slight back-lean is healthy and the aesthetic split is intentionally pull-favored). No migration. Suite **1642**, typecheck clean. Remaining (deferred): exercise-specific rep floors. Tier-2 muscle-volume work is now decomposed into 3 specs; Spec 1 shipped (see the next bullet). Plan + validated data: `docs/superpowers/plans/2026-06-16-generation-hypertrophy-quality.md`.

**Shipped, Tier-2 muscle-coverage warnings, Spec 1 of 3 (`feature/generation-hypertrophy-fixes`, 2026-06-16):** a warn-only per-muscle volume observability layer (measure before intervene). A new 13-value `Muscle` programming taxonomy (separate from the 10 reporting `ExerciseCategory` buckets) and a stored per-exercise `primary_muscle` (+ a fine `secondary_muscle_groups`), seeded by SQL CASE from `substitution_class` / `movement_pattern`. Pure `src/lib/pulse/muscleVolume.ts` tallies weekly DIRECT sets per muscle (effective sets computed too, diagnostic-only and non-normative) and flags muscles below the validated target bands; back is an aggregate target (lats + upper_back). Surfaced as a severity-sorted readout in the `gen-routine.ts` diagnostic and as one `muscle_coverage_low` validator warning (push/pull check kept, not replaced). Targets the 9 validated muscles; `front_delts` / `calves` / `core` are informational-only. A no-data guard keeps unattributed (synthetic) pools silent, so all generation goldens stay byte-identical; the validator's pool-scope guard only warns about muscles the user's usable pool can actually train (mirrors the `no_vertical_pull` philosophy). Migration `2026-06-16-09-58-39-exercise-primary-muscle.sql` (hand-apply on merge). Suite **1658**, typecheck clean. Spec `docs/superpowers/specs/2026-06-16-09-21-12-muscle-coverage-warnings-design.md`; plan `docs/superpowers/plans/2026-06-16-muscle-coverage-warnings.md`. (Spec 2 dropped + Spec 3 shipped, see next bullet.)

**Shipped, Tier-2 muscle-coverage gap-fill, Spec 3 of 3 (`feature/muscle-coverage-gapfill`, 2026-06-16):** the volume lever. A deterministic, capped, post-generation pass (`src/lib/pulse/gapFill.ts`, `applyCoverageGapFill`) that closes the small-muscle volume gaps the warnings exposed by appending ISOLATION work only (never compounds). **Spec 2 (variety scoring) was DROPPED on evidence:** an evidence sweep across 39 configs (13 styles x 3 equipment tiers, `scripts/muscle-sweep.ts` + `docs/audits/2026-06-16-10-49-42-muscle-coverage-evidence-sweep.md`) found same-pattern clustering in 0/39 configs (a variety reorder would fix a non-problem) while small-muscle VOLUME was under target in nearly every config. Gap-fill targets six accessory muscles (`side_delts, rear_delts, biceps, triceps, hamstrings, glutes`; chest/back/quads stay warning-only, the compounds cover them) in two phases: eliminate trainable zeros first, then nudge below-floor muscles toward `MUSCLE_COVERAGE_FLOOR` via set-bumps-before-inserts. Caps: +1 exercise/session, +4/routine, the 20-set ceiling, the pattern cap; a zero-kill may overflow the time band (honest `over_time`), a below-floor insert may not. Pure + import-cycle-free (reaches `ISOLATION_QUALITY` via a `qualityOf` callback); runs after the priority bump, before the duration guard; **gated on muscle attribution so synthetic pools no-op and all 255 generation goldens stay byte-identical.** Sweep result: rear_delts coverage 38 -> 69% (delt-head imbalance 12/39 -> 0/39), side_delts 52 -> 75%, biceps 58 -> 76%, triceps 59 -> 80%, hamstrings 72 -> 82%, glutes 67 -> 83%; chest/back/quads unchanged; sessions grow by at most the caps. No migration. Suite **1671**, typecheck clean. Reviewed per piece + a final pass. Spec `docs/superpowers/specs/2026-06-16-11-13-09-muscle-coverage-gapfill-design.md`; plan `docs/superpowers/plans/2026-06-16-muscle-coverage-gapfill.md`. Closes the Tier-2 muscle-volume track.

**Shipped, generation engine quality (merged #149, 2026-06-15):** the full pre-launch programme-quality pass on the slot-first generator, all of Phase 1 (5 launch blockers), Phase 2 (post-gen validator, coverage-aware backfill, demanding-week warning, estimator refinement, label-validity floor, log-timed-holds), and Phase 3 (experience/goal rep floor + difficulty filter, measurable priority muscle, Bodybuilding pump-range, PHUL identity across styles). Spec `…2026-06-14-21-51-23-generation-engine-quality-audit.md`, plan `…2026-06-14-generation-engine-quality-plan.md` (all items DONE). Four migrations hand-applied (restriction-tag-corrections, exercise-prescription-unit, set-logs-duration, leg-isolation-movement-patterns). Suite ~1,610 green, typecheck clean.

**Shipped, Library Routines tab redesign, Plan B core (`feature/routines-tab-redesign`, 2026-06-14):** the Routines tab rebuilt from a toolbar + inline editor into a card-based IA. `RoutinesTab` is now a thin orchestrator over `RoutineCard` (split chips via `routineSessionChips`, an Active badge + block-progress bar on the active card), a `NewRoutineChooser` ModalSheet (**Generate**, reusing `GenerateRoutineButton` via a new `children` slot so its onboarding/tune handoff is untouched, + **Ad-hoc** with an inline name), a `RoutineManageSheet` (Set active / Rename / Delete + a tappable session list for scheduled routines, an inline type-grouped editor with the type picker for ad-hoc), and a `RoutineSessionEditor` (back chevron; the variant-A icon row: conditional link icon left, then ↑ ↓ ✎ 🗑 with a red bin; within-session reorder via `reorderWithinSession`). `AddRoutineExerciseForm` gained a `fixedType` prop (drops the type select in a session); `RoutineExerciseRow`'s text actions became 34px icon buttons. Pure helpers `routineSessionChips` + `reorderWithinSession` in `library.ts` (unit-tested). No new server action, no migration. Built subagent-driven + live-verified in the running app; final whole-branch review clean. Suite **1568**. **Templates fold-in** (the chooser's "Start from a template") remains the follow-up. Spec/plan dated 2026-06-14-19-30-25 / 2026-06-14. Full detail in CLAUDE.md "Library redesign".

**Shipped, UX clarity + a11y (`feature/ux-clarity-a11y`, 2026-06-14):** two small fixes. (1) The password show/hide toggle is now keyboard-focusable (its `tabIndex={-1}` removed), so it sits in the tab order (field → toggle → submit) and can be operated without a mouse. (2) The Progress recovery tile's "Why" is now **state-aware**: the explain-layer `recovery` concept takes the readout's tone + driving muscles and returns a per-state title + cause + what-to-do (so "Watch" finally says which muscles and why), plus a **4-state colour-coded legend** (Fresh / Ready / Watch / Ease off) rendered by `WhyBody`, making the whole scale legible. No migration.

**Shipped, Library tab deep-links (`feature/library-routines`, 2026-06-14, stacked on `feature/modals-modalsheet`):** `/pulse/library` and `/pulse/library/routines` are now deep-linkable and back/forward-aware, matching Progress / Profile. Pure `libraryTabFromPath` / `libraryTabPath` in `navigation.ts`, a `[tab]` route segment, and a URL-driven `LibraryView` (local tab mirrors `usePathname`, `router.push` on change). No migration. The **Plan B RoutinesTab redesign** (cards + chooser + manage sheet + session editor) then shipped on `feature/routines-tab-redesign` (see the Shipped bullet above); only the **Templates fold-in** (the chooser's "Start from a template") remains deferred.

**Shipped, Modal consistency pass (`feature/modals-modalsheet`, 2026-06-14):** every Pulse modal now sits on a hardened shared `ModalSheet`. The shell gained focus management (trap + restore to the trigger), a `maxWidth` prop (default 560), a scrollable body region (header/footer pinned), an optional pinned `footer` slot, and a11y polish (`aria-labelledby` to the title, grip `aria-hidden`). All consumers adopted it: the action sheets (ExerciseFormSheet, ExerciseDetailSheet, MeasurementsCard, the mobile Why sheet) moved their primary buttons into the `footer`; the list/info sheets dropped their now-redundant inner scroll wrappers; ExerciseFormSheet widened to 640. The last two bespoke modals, `ExerciseSwapPicker` and `ExerciseInstructionModal`, were migrated onto `ModalSheet` (the how-to migration was a deferred Library follow-on). `TuneYourPlanPanel` was redesigned onto `ModalSheet` with a pinned footer and single-open progressive-disclosure rows, and gained two new tunables: **Priority muscle** (persisted via `updatePriorityMuscle` before the in-place regenerate, since the generate action reads it from the profile) and **Experience** (threaded through the regenerate `answers`). No migration. Suite **1557**, typecheck clean. Reviewed via code-review subagents per piece. Full detail in CLAUDE.md "Shared modal shell".

**Shipped, Modal consistency pass (`feature/modals-modalsheet`, 2026-06-14):** every Pulse modal now sits on a hardened shared `ModalSheet`. The shell gained focus management (trap + restore to the trigger), a `maxWidth` prop (default 560), a scrollable body region (header/footer pinned), an optional pinned `footer` slot, and a11y polish (`aria-labelledby` to the title, grip `aria-hidden`). All consumers adopted it: the action sheets (ExerciseFormSheet, ExerciseDetailSheet, MeasurementsCard, the mobile Why sheet) moved their primary buttons into the `footer`; the list/info sheets dropped their now-redundant inner scroll wrappers; ExerciseFormSheet widened to 640. The last two bespoke modals, `ExerciseSwapPicker` and `ExerciseInstructionModal`, were migrated onto `ModalSheet` (the how-to migration was a deferred Library follow-on). `TuneYourPlanPanel` was redesigned onto `ModalSheet` with a pinned footer and single-open progressive-disclosure rows, and gained two new tunables: **Priority muscle** (persisted via `updatePriorityMuscle` before the in-place regenerate, since the generate action reads it from the profile) and **Experience** (threaded through the regenerate `answers`). No migration. Suite **1557**, typecheck clean. Reviewed via code-review subagents per piece. Full detail in CLAUDE.md "Shared modal shell".

**Recently merged, Library redesign Plan A (#142, 2026-06-14):** the Exercises tab rebuilt into a findable catalog. Two solid `SegmentedTabs` (Exercises / Routines; Templates folded into a future Routines chooser, Plan B), free-text search, an advanced-filter control (Favorites / Fits-my-gear / Respects-my-restrictions / Show-hidden), a Muscle/Equipment/Type **Group-by** (persisted via `useLocalStorage`), a grouped list (favorites pinned, desktop 2-col grid incl. the filtered view), whole-card-click rows with human equipment labels, and detail + add/edit sheets on `ModalSheet` (numbered how-to with a CLS skeleton; reps as a from/to range with a freeform fallback; opt-in generation metadata). Tri-state **favorites** (`ExercisePreference` `'hidden' | 'favorite'`, mutually exclusive) through the data layer + the swap/add pickers; `create/updateExercise` extended for category + metadata. Shared **`ModalSheet`** now centers on tablet and has a mouse-draggable grip (app-wide). Reviewed via the Claude.ai + Perplexity loop (spec) + per-group code-review subagents + a clean final review; built subagent-driven and iterated against the running app. Migration `2026-06-14-12-00-00-exercise-preferences-favorite.sql` applied. Suite **1548**, typecheck + lint clean. **Deferred follow-ons:** Plan B Templates fold-in (the chooser's "Start from a template"; the Routines tab redesign itself shipped on `feature/routines-tab-redesign`); the Train/guided/Plan how-to migrated off `ExerciseInstructionModal` onto `ModalSheet` (shipped in the modal consistency pass); three minor review nits + an `EQUIPMENT_LABELS` completeness audit. Spec/plan dated 2026-06-14-11-00-00. Full detail in CLAUDE.md "Library redesign".

**Recently merged, Bug 6 per-session focus labels (#141, 2026-06-14):** a nullable `routine_schedule.label`, populated at generation from the session emphasis (`focusLabelForEmphasis`): quad emphases (`lower_quad` / `lower_lean`) → "Lower (Quads)", posterior (`lower_post`) → "Lower (Hamstrings & Glutes)", null for everything else (PHUL lowers and general days keep compact labels). Threaded through `ROUTINES_SELECT`, resolved at render by pure `sessionFocusLabel(schedule, type, variant)`, shown on the spacious surfaces (Plan accordion + next-session preview in `ProgramView`, desktop guided subtitle in `WorkoutModeScreen`); tabs and the mobile guided VARIANT chip keep their compact labels. Covers all five paired-emphasis styles (ul-classic-4, ul-aesthetic-4, ulppl-5, fb-ul-hybrid-5, ppl-x2-6). Forward-only: existing routines render unchanged until regenerated. +18 tests, suite **1447**. Migration `2026-06-14-10-30-00-routine-schedule-label.sql` applied 2026-06-14.

**Recently merged, Plan page redesign (#140, 2026-06-13):** `ProgramView` rebuilt to a new IA, synthesizing the page-depth Plan items into one redesign. A program **identity card** (name + `formatProgramStatus` pill + block-progress bar + RIR / next-deload / days-per-week stats + collapsible "Why this plan"), the next-session preview, a this-week strip, the **Training-block arc** (`BlockArc` from pure `buildBlockArc`: ascending phase tint, live-week full colour, deload marked, tap-to-inspect with per-phase descriptions + RIR/deload glossary via the explain layer), a **responsive `PlanSessionList`** (mobile/tablet chip selector, desktop single-open accordion, with `estimateSessionMinutes` + focus line + per-slot `exerciseReason`), and collapsed **Program settings** (length / start date / "Change split or days" → new routine, old kept). Generation duress warnings moved off the rationale to a keyed `workout_routines.warnings` column + dismissible `GenerationWarningNotice` (`WARNING_COPY`, localStorage-dismissed per routine). Desktop L4 sticky summary rail, kept alongside the app's coach rail; scrolls internally when taller than the viewport. Migration `2026-06-13-22-18-25-workout-routine-warnings.sql` applied. Reviewed via the Claude.ai + Perplexity loop + code-reviewer; live-verified (caught the arc-week async-sync bug). Suite **1429**, typecheck clean. Spec/plan/reviewer-prompt dated 2026-06-13-21-39-19; mockup `…-21-17-54-plan-page-redesign-v2.html`. Full detail in CLAUDE.md "Plan page redesign".

**Recently merged, recomp trend chart + generation 3.1 (#137/#138, 2026-06-13):**
- **Recomp trend chart (#137):** on Progress > **Overview** the recomp verdict card and a new `RecompTrendCard` sit side-by-side, **50/50 width + equal height** (the verdict card is the height reference; the trend card's two stacked area sparklines, bodyweight grey / strength score accent, flex to fill it via absolute-positioned SVGs). The verdict card gained a **status pill** (`recompStatus`: Recomping / Gaining / Cutting / Watch) and a **headline + description** split (`recompLines`; `recompDetail` strips the lead that restates the pill). Copy refined via the Claude.ai loop (Cutting/Watch reworded, Watch restructured); the best-case Recomping line is a user-authored affirmation, not a next-step. Pure helpers `computeRecompTrend` / `recompStatus` / `recompDetail` / `recompLines` (all tested), reusing `bodyweightLogs` / `strengthSeries` / `recomp`. No engine change, no migration.
- **Generation 3.1, compound-first guard (#138):** an `is_compound` term in `byPattern` (after the canonical-anchor rank, before the fatigue tiebreak) so a compound beats an isolation for the same slot. A defensive artifact live only for the two mixed patterns `squat` (Leg Extension) / `hinge` (Leg Curl), a no-op for the other 13; goldens byte-identical (+1 regression). A Phase 1 verification stood in for the loop and **debunked the stale 2026-06-10 audit's "knee+lower_back all-isolation leg day"**: `COMPOUND_FLOOR` (shipped 2026-06-11) protects the session level and the contraindication seed leaves safe lower compounds untagged, so the combo keeps a compound. Spec `docs/superpowers/specs/2026-06-13-19-12-33-compound-first-selection-guard-design.md`. No migration.
**Recently merged, coach-legibility quick-wins (#136, 2026-06-13):** three small diffs on one branch. (1) **Guided-mode deload prefill** (correctness): a stalled lift logged in guided mode now gets the ~90% deload target, matching the Train card (`WorkoutModeScreen`'s `ExerciseSetRows` computes the deload flag like `ExerciseCard`). (2) **e1RM chart stall/deload annotation**: a `Progressing` / `Stalled` / `Deload suggested` pill on Progress > Lifts, derived by the pure `liftTrend` (mirrors `computePlateau`/`shouldDeload`), with the `stalled`/`deload` `Why` affordance. (3) **Plan next-session preview**: a `NextSessionCard` showing the next scheduled session + the working weights Train will prefill (pure `computeSessionTargets`), with a "Start session →" CTA into the right Train tab. (Layout polish deferred to the Plan page redesign per user.) Note: the roadmap previously referenced a `docs/audits/2026-06-13-cls-data-fetch-audit.md` "ready to action", that file does not exist (stale reference, dropped).

**Recently merged, body-tracking + explain-layer sprint (2026-06-13):** four features.
- **Body-tracking live updates (#132):** logging a non-today body weight, setting/clearing a goal weight, and logging measurements now update the UI live. Root cause was cards calling server actions directly (or relying on a refetch) instead of the optimistic SWR hook; all three now insert the server-returned row into the cache.
- **Measurements rework (#133):** one row per `(user, date)` via a COALESCE upsert (+ `deleteBodyMeasurement`), all-four-at-once entry in a log sheet (matched-pair card with Body Weight), per-date delete, cm/in toggle inside the sheet, selected-metric trend chip. Migration above applied to prod.
- **Tab deep-links (#134):** `/pulse/progress/lifts`, `/progress/body`, `/profile/training` are URL-addressable (pure `resolveView` / tab-path helpers in `navigation.ts`, `[tab]` routes, URL-driven tabs that survive back/forward).
- **Explain layer:** a canonical reason registry (`explainCopy.ts`) + a shared on-demand `Why` affordance (portaled popover on desktop / `ModalSheet` on mobile; sentence-case, "Got it"/✕ close, focus-trap), `decisionCopy` parity-locked to it. Wired across deload/stalled, the progression target, the behind/lapsed status pills, and glossary terms (e1RM, warm-up, weekly volume, recovery, strength score). Spec `docs/superpowers/specs/2026-06-13-explain-layer-design.md`. **Deferred follow-ons:** BestLifts e1RM wrap (rows are buttons, needs a non-button affordance), the e1RM long tail (chart labels / SessionDetailModal / milestones), behind-pill word softening, strength-score Escape double-close, measurement write-side concurrency (a server-side COALESCE trigger), pre-fill on measurement edit.

**Recently merged (prior):** Mobile polish + Progress-overview rework (#131, 2026-06-13): the wife-test feedback batch (ModalSheet scroll-lock + swipe-to-dismiss + sticky-header `-top-px` + desktop `p-6`; shared `ModalGroupHeader`/`ModalIconBadge` unifying all list modals; Coach timeline on ModalSheet, week-grouped; training-prefs save toasts; e1RM decimals; top-bar "Week N + flame streak"; password show/hide; goal-weight Initial readout; Profile single-column) plus the Progress-overview rework (Sessions+Volume strip, Recomp-above-Program, equal-height Milestones/Coach pair, `computeWithinReach` forward hook, priority-muscle volume line, strength level on tile, tab deep-links, empty state). Progress page redesign (squash `554a5ca`, 2026-06-12); Profile restyle (#129), Profile + Progress IA redesign (#128), quick UI fixes (#127); self-serve auth lifecycle (#126); schema-in-VCS baseline (#125); exercise history while logging (#13, #124); PHUL + split rationale (#122/#123); live-test fixes (#121); P0 Group 2 seed (#120); `ppl-x2-6` A/B (#119); day-picker frequency (#118). Full detail per feature in **Shipped**.

**Billing, deliberately deferred (product decision, 2026-06-12):** auth shipped without billing. Prove the product is good before charging for it; the billing/cancel/refund lifecycle waits until validation earns it (it stays specified in "Launch floor" below for when that time comes).

**Test accounts seeded for validation (2026-06-12):** `christiaanve@hotmail.com` and `christiaanve@live.nl` carry rich controlled histories. hotmail (PHUL, anchor Apr 27, weeks 1-5 trained then a 14-day gap) reads **lapsed → ramp-back nudge** with a stalled Dumbbell Bench → auto-deload; live.nl (Classic U/L, anchor May 4, weeks 1-5 trained, week 6 open) reads **behind → catch-up nudge** with a stalled Barbell Bench → auto-deload. Both verified by running the real `computeProgramPosition` / `computeRegenSuggestion` / `shouldDeload` against the seeded rows. `decision_events` left empty on purpose so they fire live as you train (the actual validation).

**Generation engine quality track: CLOSED** (opened 2026-06-10, all five items + Issue 0 shipped by 2026-06-11; subsection 3.1, the compound-first selection guard, shipped #138 2026-06-13). No open generation-quality items. The next generation work is the deferred data-model item (`quad_iso`/`hamstring_iso` patterns), which would make the 3.1 guard a true no-op; spec-first, not in the launch path.

**Ongoing (continuous, not a discrete next-action):**
- **Validation block** (highest-leverage, gates the launch-readiness track): train Pulse with intent and run the deliberate missed-week test (skip a scheduled week to fire ramp-back), querying the `DecisionEvent` log via `docs/queries/decision-events-review.sql` (newest-first, today-only, ramp-back-only variants). Runs in parallel with build work; not a thing to "finish", a thing to keep doing until the adaptive promise is earned.

**Where things stand:**
- **Generation Phase 0 complete:** muscle bridge, exercise-metadata seed (94 exercises), fractional per-muscle volume, session-linked logging + the unified `DecisionEvent` log.
- **Tier 1 complete:** the #1/#2/#3 surfacing + polish layer, Coach Decision Timeline (#10), Lighten this week (#11), session-linked logging (#12), quick-start generation + Tune-your-plan (#15). All live-test findings shipped (see the validation record below).
- **Tier 2 moat complete:** the personalization-in-generation items all shipped, training style + variety + loading lean (#4), the GQ1-GQ3 ranking passes, movement restrictions (#5), equipment profiles (#6, both branches), and travel mode (#322). Exercise-history-while-logging (#13) shipped (#124). One non-personalization refinement remains in the Tier 2 table: reason-driven manual deload dose (#16).
- **Tier 3:** Coach Decision Timeline (#10), program pause / injury mode (#14), behavior-driven adaptation (#7, swaps/demote slice), and smart substitution v2 (#8) shipped; open: #9 (progress photos) and #17 (swap scope, designed 2026-06-11, deferred).
- **Launch floor:** auth lifecycle now shipped (signup, email verify, password reset, account delete; #126) on top of the earlier offline-queue safety + dead-letter hardening, decimal-comma parsing, rest-timer-survives-lock, live RLS audit, the 2026-06-06 code/security audit, and the schema-in-VCS baseline (#125). Billing deferred by product decision. The rest is still open, see "Launch floor".
- **Suite:** typecheck clean, **1548 tests**. Per-feature detail lives in **Shipped**; strategy + specs are frozen in the two 2026-06-06 design docs plus the equipment-profiles + travel-mode + behavior-adaptation + smart-substitution specs/plans.

**Next up, in order (converged 2026-06-10, pruned + re-cut 2026-06-12 after migrations applied + auth shipped):**

1. **Validation block** (now the top action, no longer blocked): the test accounts are seeded into ramp-back / catch-up / deload states, so train with intent and run the deliberate missed-week test, then read the `DecisionEvent` log via `docs/queries/decision-events-review.sql`. This gates the rest of the launch-readiness track and earns the adaptive promise.
2. **Operational infra**: error/crash tracking (Sentry or PostHog) + DB backups with a tested restore. Stands alone, can start now.
3. **Legal/compliance**: privacy policy, ToS, cookie consent. Can draft in parallel.
4. **A11y audit, then GDPR full export, then incident + rollback plan**, in that order.
5. **Auth hardening that was folded into "auth + billing":** rate limiting on auth + mutation routes, and production email deliverability (SPF/DKIM/DMARC + a transactional provider). Auth flows shipped (#126); these are the production-readiness companions, still open.
6. **Billing lifecycle** (deferred until validation proves the product): cancel / refund / transparency, built as carefully as signup. Not before validation earns it.

**Direct follow-ups from the 2026-06-10 generation engine bug-fix session (open items only; the resolved ones, the `lower_post` guard, the split rationale, and the PHUL suggested-split steering, are in Shipped):**
- ~~**Bug 6 relabel (user-chosen, next):** per-session labels so the posterior leg day reads correctly.~~ **Shipped (#141, 2026-06-14).** Built differently from this note after the wording/scope confirmation: final wording is muscle-named ("Lower (Quads)" / "Lower (Hamstrings & Glutes)", not "Quad/Posterior Focus"); scope widened from ulppl-5 to all five paired-emphasis styles; placement is the spacious surfaces only (Plan + desktop guided subtitle), so the label resolver hit ~3 surfaces, not ~8 (tabs + mobile guided chip deliberately keep compact labels). Mechanism is the `routine_schedule.label` column, populated at generation from the emphasis.
- **`anchor_rank` exercise column:** replace the name-keyed `CANONICAL_ANCHORS` map (silently degrades if a seed exercise is renamed; guarded today only by a catalog-consistency test) with a DB `anchor_rank` column next to `fatigue`, seeded by the same name-matched UPDATEs. Follow-up to generation Phase 0 #2 metadata.
- **PHUL suggestion in onboarding (open UX follow-up to the shipped #18 steering):** surface `suggestedStyleKey` in the onboarding `RoutineSetupFlow` picker too (today only `TuneYourPlanPanel` shows the "Suggested" badge). Deferred because the training-style signal is not set during quick onboarding; needs a product call on where it comes from.

**Own clean window (not interleaved with the above):** monorepo extraction (flatten-to-root decided now, executed as one isolated sprint, the whole near-term block must settle first), then the full dependency upgrade, hard-gated on extraction being live (low-risk group, then Next 15 to 16 in isolation, then the Supabase libs separately). See "Infrastructure" below.

**Later (post-launch or validation-gated):** progress photos (#9), reason-driven manual deload (#16, the volume-wiring prerequisite first), the behavior-adaptation v1.6 + smart-substitution v2 v1.6 follow-ons, Coach Timeline increment 2, i18n extraction, marketing-hero copy (needs a landing page that does not exist yet), PostHog, and the small UX items (guided landscape layout, computed starting-weight estimate). Detail in "Later".

**Deferred follow-ons from the Profile + Progress redesign (queued 2026-06-12, each its own spec):**
- **Write-side measurement parity:** independent per-metric same-date logging ("one value per metric per date"). Needs a `UNIQUE(user_id, measured_at)` migration on `body_measurements` (with a one-time dedupe/merge of existing same-date rows), a COALESCE upsert rewrite of `logBodyMeasurement` (currently a plain insert, so same-date rows duplicate today), a same-date merge policy, and a new `deleteBodyMeasurement` action + UI (none exists today). The redesign ships read-side parity only; this is the write half.
- **UI primitives consolidation:** an emerging shared layer already exists (`TabButton`, `ui.ts` style constants, `EquipmentSelector`, `PageSkeleton`/`SkeletonBar`, and the redesign's new `SegmentedTabs` + `MetricLineChart`), but recurring patterns (option cards, section labels, pills, settings rows, shimmer) are re-implemented inline in several places. A single consolidation pass extracts the common ones into `src/components/pulse/ui/` and adopts them. NOT a formal library / Storybook (premature at this scale). Run it as its own branch **after** the Profile/Progress and Setup/Tune redesigns land, so the patterns have stabilized first.
- **Progress richness (Liftin-inspired): SHIPPED in full.** Workout calendar + session-detail merged in the redesign (`554a5ca`); drill-in, milestones, strength trend, and recovery readout merged #130 (2026-06-13). Remaining backlog: expanded measurement metrics + progress photos (separate bullets below).
- **Expanded measurement metrics (backlog):** body-fat %, shoulders, neck, biceps, forearms, hips, thighs, calves on the Body tab. Needs a `body_measurements` schema migration (new nullable columns) plus the metric-picker / logging UI extended to cover them. Deferred until the migration is worth it.
- **App-shell icon pass:** the #129 restyle fixed the Profile-specific icons (chevrons / export to proper SVGs). The rest of the app's icons still want a once-over for consistency and quality, the left-sidebar (`DesktopLayout`) and bottom-nav (`BottomNav`) nav icons (Train / Plan / Progress / Profile / Library) and any other text-glyph or placeholder icons across views. Audit, standardize on one icon set/stroke weight, replace stragglers. Its own small pass.
- **Smaller:** sub-tab state persistence. (Library exercise list grouped by equipment: shipped as the Group-by, #142. History-modal month grouping: resolved, all history/list modals are month-grouped as of the modal consistency pass + milestones modal.)
- **Setup / Tune flow redesign** (from the same backlog): shrink the oversized `RoutineSetupFlow` desktop modal + redesign `TuneYourPlanPanel` to match `RoutineSetupFlow`. Its own design round.

**Deferred follow-ons from the Library redesign (Plan A, #142, 2026-06-14):**
- **Plan B: Routines tab restyle + Templates fold-in** (the New-routine chooser: Generate / Start from a template / Ad-hoc; routine cards with split chips + block-progress; a manage sheet). Its own spec/plan; the Templates fold-in is gated on the templates-role decision.
- **Migrate the how-to off `ExerciseInstructionModal`:** Train / guided / Plan still use the bespoke modal; move them onto `ModalSheet` (or the shared detail view) for full consistency.
- **Minor review nits:** detail-sheet network-error vs normal-404 message; `ExerciseFilterControl` toggle-JSX de-dup; custom-exercise sets-format validation; an `EQUIPMENT_LABELS` completeness audit.

**External page-depth review (2026-06-12, Claude.ai, five lists: Train / Plan / Progress / Profile / Library), reconciled against code.** Several findings were stale (already shipped: Progress drill-in + workout calendar, Train previous-sessions reference (`ExerciseHistoryPanel`, #13/#124), Profile re-run-setup row, Plan swap picker with `exerciseReason` captions; already roadmapped: Bug 6 relabel, "Today's focus" Train panel (Phase 2, and its Phase 0 prerequisite is now satisfied), year-in-review, expanded metrics, GDPR export, i18n, Library equipment grouping, exercise media, the templates-role decision gate) and one misdescribed the schema (no `fatigue_cost` / `joint_stress` / `difficulty` columns exist; the real displayable metadata is `movement_pattern` / `equipment` / `is_compound` / `substitution_class`). The review's Train meta-note is adopted as doctrine: on Train bias subtraction over addition, every add must be ignorable when it does not apply. The adopted kernels, none of which jump the validation-block / ops-infra queue:

- **Adopted, high value-to-effort (the "page depth" lane, each its own small diff):** (guided-mode deload prefill, Plan next-session preview, and the Progress e1RM stall/deload annotation shipped #136 2026-06-13; the recomp trend chart shipped #137; the **Plan page redesign** folded in the Plan items below, shipped #140 2026-06-13; the rest remain.)
  - ~~*Plan: block/phase arc view.*~~ **Shipped in the Plan redesign** (`BlockArc`: phase per week via ascending tint, deload marked, RIR per week + per-phase descriptions, tap-to-inspect; from `buildBlockArc`).
  - *Train: inline "why this target".* Absorbed into the **Explain layer** entry below (2026-06-12): it ships as that entry's progression-card-tappable item, through the canonical `explainCopy` registry rather than as an ad-hoc one-off.
  - *Train: "Today's focus" panel* (the roadmapped Phase 2 item, now unblocked): session intent + lifts due to progress + anything carried over, at the top of Train. Spec-first through the review loop; it is coach voice at the moment of action and must stay ignorable.
- **Adopted, backlog tier (valuable, not next):** Progress multi-week per-muscle volume trend (is a muscle's weekly volume climbing or falling); Progress weekly tonnage trend (workload proxy next to set counts); ~~Plan per-slot "why this exercise" line~~ (shipped in the Plan redesign, `exerciseReason` on every `PlanSessionList` row); ~~Plan estimated session duration~~ (shipped, `estimateSessionMinutes`); ~~Plan restructure entry points~~ (shipped, "Change split or days" → the generation flow, new routine, old kept); Profile program defaults (default program length / session length / days via the existing `param ?? profile ?? default` resolution); Profile notification-preference home (build WITH the first notification feature, not before); Profile About/version row (lands with the launch-floor legal pages); Profile data-export expansion (JSON full export is the launch-floor GDPR item; CSV exists); ~~Library findability bundle~~ (shipped in the Library redesign, #142: search + Fits-my-gear + Respects-my-restrictions + the Group-by); ~~Library exercise-card metadata surfacing~~ (shipped #142: row metadata + the detail sheet's Similar exercises); ~~custom-exercise generation-awareness~~ (shipped #142, the form's generation-metadata toggle); template preview-before-clone (gated on the templates-role decision like all Templates work, Plan B); ~~favorites/pinned exercises~~ (shipped #142, floated in pickers + swaps); **Train ergonomics bundle** (one polish diff: "Same as last" + prescription chips already live in the editorial SetLogger, bring the useful subset to the card variant; +/- weight steppers; RIR carried from the prior set; warm-up check-off/dismiss); Train rest-timer intelligence (type-derived default rest when `rest_seconds` is null, compound vs iso, plus an optional under-RIR extend nudge; the per-exercise `rest_seconds` column + timer wiring already exist); Train live this-session muscle readout (per-session volume via the muscle bridge, guided sidebar + mobile summary); **Train session-event trail** (spec-first, one design: per-exercise skip ("not today"), mid-session add/remove tracked as session events instead of conflating with permanent routine edits; fixes the known data-cleanliness gap AND makes skip detection trustworthy, which the behavior-adaptation v1.6 skip signal needs anyway); guided-mode `ExerciseHistoryPanel` parity (the Train card got #13; the guided focus pane did not); guided superset round-gating live-test pass (tighten, do not add set types).
- **Dismissed (decision record):** muscle heat-map silhouette (re-presents `MuscleVolumeBars`, asset cost over info gain); push/pull balance ratios (the per-muscle bars + targets already surface imbalance; 10-category taxonomy too coarse for honest ratios); coach-voice verbosity toggle (settings creep; "ignorable intelligence" is already the design principle); light theme (dark-by-design); e1RM main-lift table (Best Lifts already covers it; the one delta, PR dates, can ride along in the drill-in someday); calendar-date session pinning (fights completion-paced progression); concurrent active programs (state complexity with no current need); soft-delete routines (revisit at real user volume); first-day-of-week / date-format prefs (en-GB is hardcoded in 6 places; revisit with i18n extraction, same string layer); Plan equipment-profile switcher (TuneYourPlanPanel's equipment chips + regenerate already cover the mechanism; a Plan entry point can ride the restructure-controls item); print/share plan summary (hold; revisit on real demand); Library per-exercise history entry point (the shipped Progress drill-in is keyed by `routineExerciseId`, a Library exercise maps to many reIds across routines; an exercise-identity aggregation is real work for a duplicate surface, revisit if browsing-to-history proves a real path); account/security depth (active sessions, email change: standard post-signup work, queue with the auth-hardening launch-floor item); PWA install/cache status (the `PendingSyncBadge` already covers sync state; a cache panel waits for real stale-cache reports); Train rest-day / active-recovery screen (Train is always the home screen and always training-focused, hard guard); in-set form video / camera (storage-gated, and a different product than fast logging).

**Explain layer (queued 2026-06-12, from the explain-layer audit `docs/audits/2026-06-12-23-51-27-explain-layer-audit.md`; findings accepted).** Both entries file in the near-term lane **BELOW the validation block and the open launch-floor items**: this is the most moat-serving polish available and it de-risks i18n, but it is build-after-validation, not instead-of.

- **Canonical reason registry + shared "why" affordance** (one entry: mechanism + the top-3 audit items). The audit's core finding: the explain problem is concentrated drift, not absence. `decisionCopy` is the working canonical what/why/next backbone for its four dated event types, but the Train-time surfaces bypass it with independent strings (the auto-deload rule is phrased ~4 ways across `ExerciseCard` / `SetLogger` / `decisionCopy` / `FinishDebrief`). One mechanism, no second explanation path: a new `explainCopy.ts` (timeless rule concepts -> parameterized `{ headline, why, next? }`, the shape `decisionCopy` established) plus ONE shared on-demand `Why` affordance (tap a value -> popover on desktop, `ModalSheet` on mobile; zero chrome until invoked, so ignorable intelligence is preserved by construction), with `decisionCopy`'s four cases pulling their why-sentences from the matching `explainCopy` concept so exactly one canonical sentence exists per concept. **Ships with the top three audit items only:** deload copy unification, progression-target why on the Train card (the fix is "make the card's number tappable", NOT "make the card talk"; the card stays the dense at-a-glance surface), and behind/lapsed why. **Build order (deliberately not the audit's impact ranking):** mechanism first -> deload unification (pure plumbing, the copy exists and is good) -> progression-card tappable (pure plumbing, copy exists) -> behind/lapsed (NEW copy, written deliberately: it fires at the app's most anxiety-prone moment and must not be rushed) -> then the cheap one-liners as follow-on adds inside this entry, not separate entries (e1RM glossary, warm-up scheme, volume-target provenance, recovery drivers, strength-score methodology in the breakdown modal). **i18n dependency (binding): this entry precedes the queued i18n extraction** (see "Infrastructure, i18n foundation"), so the `coaching` namespace translates one canonical sentence per concept and the "translated copy drifted from English" bug class cannot exist. Continues the Phase 2 "trust & visibility" track (Coach Timeline + per-decision event explanations shipped; this generalizes the same voice to timeless rules). Accepted pushback stands: engine internals (anchor ranks, fatigue tiebreaks, caps/floors, the role model), equipment exclusions, and always-on behind/lapsed banners stay as-is. Absorbs the page-depth bullet "Train: inline why this target" above. Spec-first through the review loop.
- ~~**Plan rationale collapse + distinct generation warnings**~~ **Shipped in the Plan redesign (2026-06-13).** `ProgramView`'s rationale prose now collapses behind a "Why this plan" affordance (facts chips stay inline); the duress warnings became stable KEYS on a `workout_routines.warnings` column (no longer concatenated into the rationale) rendered by a dismissible `GenerationWarningNotice` (copy from `WARNING_COPY`, localStorage-dismissed per routine). The `rir` / `phase` / `deload_week` explain concepts were added to `explainCopy`.

---

## Validation block, live-test findings (resolved 2026-06-06)

The first live workout of the validation block surfaced ~20 UX/correctness findings, all triaged and shipped: the launch-quality bugs (bodyweight logging, end-of-session teardown, routine-scoped decision feed, week-1 anchoring, equipment-filter leak, plate-calculator close), the guided set-row + plate-calculator polish, the cross-surface screen tweaks, and the finish/debrief rework. Each is in **Shipped**.

**Durable decision, week-1 mid-week start (Runna model):** completion-paced means a week is an ordered set of N sessions, so past-calendar days read as "up next, never overdue", never missed (engine verified already correct), plus a friendly start-date prompt at creation.

**Still open (relocated to Later):** exercise "how to perform" media (storage decision, shares infra with Tier 3 #9 Progress photos), a height-aware guided layout for landscape / short viewports, and a computed starting-weight estimate (needs the strength model).

---

## Launch floor (pre-public)

The data-safety and account-lifecycle work that gates a public launch. These are not features, they are the floor: "don't lose or leak user data, don't trap the user." Features wait behind them. Sourced from the multi-app review sweep (Liftin', Runna), whose loudest one-star drivers were sync data-loss and billing opacity, cross-checked against the code. Nothing here jumps the floor or the validation gate; the action is verifying these failure modes are handled before strangers arrive.

- **Auth + billing lifecycle (the long pole).** **Auth shipped (#126, 2026-06-12):** signup / email verify / password reset / account delete are built, with an auto-create-profile trigger and account-deletion FK cascades. **Billing deliberately deferred** (prove the product before charging). The sweep extends this to **billing specifically**, cancellation difficulty, opaque billing, and charges after account deletion are the #1 complaint driver across Runna reviews. So the paid-tier seam must include a clean, honest cancel / refund / billing-transparency path built as carefully as signup, plus a real support contact (not a bot). **Positioning note (not a build):** a one-time / lifetime payment option is a real weapon against subscription fatigue and fits Pulse's no-dark-patterns stance, worth considering when designing the tier, or at minimum keep subscription terms transparent. **Cross-device session continuity (security boundary):** the safe version is already the default, Supabase persists an authenticated session per device (the SSR cookie session the middleware refreshes), so "logged in everywhere" needs no new mechanism. If you want lower sign-in friction, do it through owner-bound standard flows: single-use, short-TTL magic-link / OTP sign-in delivered to the account's own email, or a QR device-pairing approval from an already-authed device (the WhatsApp-Web model). **Never a link that *carries* authentication**, a forwarded or shared link that auto-logs-in whoever holds it is an account-takeover path, and stays out.

- **Accessibility (a11y) audit, sequenced AFTER the auth lifecycle, not before.** A full a11y sweep, not yet done. The code audit (2026-06-06) surfaced one symptom, the guided `WorkoutModeScreen` overlay missing `role="dialog"`/`aria-modal` and some unlabeled icon-buttons (audit Q8), and one flagged modal almost always means the same patterns recur elsewhere, so this needs a dedicated pass, not a spot-fix. Treated as a launch-quality **floor, not deferred**, because the launch audience is EU/NL, where the European Accessibility Act (in force 2025) makes basic digital accessibility a legal consideration, not only a quality one. **Triage like the general audit:** fix the floor (WCAG 2.1 A + AA essentials, keyboard operability, focus management on modals/overlays, accessible names, semantic structure, contrast) before launch; file richer ARIA/live-region/reduced-motion as opportunistic Bucket 2; drop/defer the AAA-and-edge tail. **Highest-priority surface: the core logging loop** (`ExerciseCard`, `SetLogger`, guided `WorkoutModeScreen`), a keyboard or screen-reader user must be able to log a set start-to-finish. **Pulse-specific checks the standard sweep would miss:** (1) verify contrast against **every accent preset** (coral/emerald/sky/violet/amber/rose), not just default coral, since a user-picked accent multiplies the contrast surface by six; (2) part-manual, not tooling-only (automated scans catch ~⅓–½; the manual VoiceOver/keyboard "log a set without looking" core-loop test is the highest-value part); (3) test on a real mobile device with VoiceOver (iOS) / TalkBack (Android) + touch-target sizing, since installed-PWA mobile is the primary surface. **Preliminary contrast already computed** (so the eventual sweep starts from data): the bright accent-as-foreground passes AA on the dark theme for all six presets (6.4–11.4) and dark-label-on-accent buttons pass, so that fear is defused; the real AA gaps are accent-independent, `pulse-muted` body text fails the 4.5 floor on bg/surface/surface-2 (4.21/3.89/3.61, large-text-only) and the dim-accent foregrounds fail on most presets (≈3.3–4.4). Confirms this is a genuine defect to fix, not a theoretical one. **Sequencing rationale:** signup / login / reset / verification are net-new, high-stakes a11y surfaces that don't exist yet (a screen-reader user who can't complete signup never reaches the app); auditing now would miss the most important new front-door surface and force a re-audit. **Exception that jumps the queue:** fix the known Q8 modal issues opportunistically if already inside `WorkoutModeScreen` for the guided-set-row polish (Tier 1 #2), that is the opportunistic-fix discipline, not running the formal sweep early.

- **Operational infrastructure (the "operating it in production" floor).** These are the gaps a feature-focused solo dev overlooks because at two users *you* are the monitoring, support, and error reporting, which silently stops being true at launch. Triage: the first four are floor (regret-launching-without), the rest are confirm-don't-assume.
  - **Error / crash tracking (floor).** Today a production runtime error is invisible until it surfaces as a one-star review ("it glitched and lost my workout", a real Runna/Hevy review pattern). Need production error capture with stack + route context. Sentry (free tier ample at this scale) or PostHog error tracking (one fewer vendor if PostHog is adopted later, see the analytics decision record above). Pairs with the offline-queue dead-letter work, a dead-lettered write should surface to *you* (the `deadLetteredCount` already exists), not just sit in IndexedDB. Launching blind to production errors is launching without a smoke detector.
  - **Database backups + a tested restore (floor, confirm/upgrade).** The product promise is "your training history, for years," so DB loss is existential and a botched restore loses a user forever. Confirm the Supabase plan's backup cadence, recovery window (point-in-time recovery), and *actually test a restore once*. Free-tier Supabase backup retention is limited, if so, this is a reason to upgrade the tier before launch. "There are backups" ≠ "I've restored one."
  - **Rate limiting / abuse protection (floor, build into the auth lifecycle).** No rate limiting on write paths or auth today; fine at two trusted users, not public. Two surfaces matter: auth (signup/login, credential-stuffing, signup spam, verification-email abuse) and the mutation routes (a runaway/buggy client flooding the DB). Needn't be elaborate, Supabase has some built-in auth limiting; a basic per-user/per-IP limit on mutations covers the rest. Build it *into* the auth-lifecycle work rather than bolting it on later.
  - **Production email deliverability (floor, part of the auth lifecycle).** Signup verification, password reset, and transactional email depend on email actually arriving (SPF/DKIM/DMARC, sender reputation, not landing in spam). Supabase's default email sending is dev-grade and rate-limited; production signups likely need a transactional provider (Resend / Postmark / SendGrid) wired to Supabase Auth with domain email auth configured. A reset email that bounces or spam-folders is a locked-out → dead user. Part of the auth-lifecycle build, not separate, it's the part people forget until reset emails fail.
  - **Confirm-don't-assume (lower effort, mostly near-free on Vercel + Supabase):** uptime/health monitoring (a free pinger on a health endpoint, so "is it down" isn't answered by a user); a staging/preview environment with a non-production Supabase so destructive migrations are never tested against the only users' real data (Vercel preview deploys give most of this free); migration safety/rollback discipline (hand-written migrations against prod data get higher-stakes once it's irreplaceable, test against a copy, know how to roll back); CI on push (the full test suite + typecheck running automatically so a regression can't reach prod silently, Vercel can gate deploys on it).
  - **Explicitly NOT needed yet (don't over-build):** CDN beyond Vercel's default, a separate caching layer (Redis), container orchestration, load balancing, multi-region, a dedicated logging pipeline beyond error tracking. All scale infrastructure for a user base that doesn't exist; the Vercel + Supabase stack handles this class of concern precisely so a solo dev doesn't reach for it early.

- **Legal / compliance pages (floor, named deliverable, not just an implied mention).** Privacy policy, terms of service, and a cookie/consent mechanism with a clear data-processing basis. Currently referenced only in passing (the analytics GDPR note, implied in the auth lifecycle) but not owned as a concrete item. Required floor for an EU/NL launch handling health-adjacent data (bodyweight, measurements, training history), same tier as account deletion. Not a big build, but a GDPR-compliant privacy policy for health-adjacent data is not a fill-in-the-blank template, and it's the kind of task that's nobody's until it blocks launch day, so name it now. Shares the consent surface with the analytics decision record above. **Caveat (not lawyer-able from here):** these roadmap items mean "make sure this is handled," not "this is now legally sufficient", for health-adjacent EU body data, a one-time actual legal check (even a single consultation on the privacy policy + consent basis) is worth more than any roadmap wording. Treat the obligations named here as the checklist, not the clearance.
- **Incident + rollback plan (floor, lightweight, extends migration safety).** The connective tissue between backups and error tracking: the deliberate answer to "what do I do when production breaks with real users," not a heavy runbook (solo dev). Three concrete cases: (1) roll back a bad deploy (Vercel is ~one click, confirm the procedure is known); (2) roll back a bad *migration*, the real one, since migrations are hand-written and applied to prod data, so it is the higher-stakes case and overlaps the migration-safety confirm-item above; (3) a stated tolerance for "app is down while I'm asleep." The value is having thought it through once, before it happens at 2am on live user data.
- **Account data export (floor, GDPR portability, pairs with account deletion).** GDPR's data-portability right. The shipped CSV export partially covers this for workout logs; the action is to confirm whether that satisfies the portability obligation or whether the account-deletion floor item should also carry a full-account export companion (deletion and export are the two halves of "the user controls their data", same surface). Likely small or already mostly covered; confirm sufficiency rather than assume new build.

**Floor already shipped (do not re-audit):** offline-queue cross-user safety (per-user replay, never dropped/leaked) **and poison-pill / dead-letter hardening** (a permanently-failing write no longer stalls the queue; auth-expiry stays transient), live RLS audit (all user-data tables owner-scoped in prod), decimal-comma input parsing, rest-timer-survives-lock reliability, and the **2026-06-06 code/security audit** (server-action authz, input validation, secrets, CSP all verified, see `docs/audits/`). See Shipped.

**Still open on the floor:** the **billing lifecycle** (deferred until validation proves the product), the a11y audit (below), the **operational-infrastructure** items above (error tracking + tested backups stand alone; rate-limiting + email deliverability are the production-readiness companions to the now-shipped auth flows), the **legal/compliance pages** (privacy policy + ToS + consent) and **incident/rollback plan**, and confirming the CSV export covers GDPR portability. **Closed 2026-06-12:** the auth lifecycle shipped (#126); the schema-in-VCS baseline (`0000-baseline-schema-reconstruction.sql`, #125) supplies the base `CREATE TABLE` for the four pre-migration tables (`profiles`, `set_logs`, `exercise_notes`, `bodyweight_logs`) and is applied to prod (no-op via `IF NOT EXISTS`), closing the reproducibility gate.

---

## Roadmap (re-tiered 2026-06-06)

Ordered to deepen the adaptive-coach moat. Tier 1 is cheap and makes existing intelligence visible; Tier 2 is the real personalization differentiation; Tier 3 builds on it.

### Tier 1, now (cheap, high-trust, on-vision)

**Complete.** All items shipped (2026-06-06 → 06-08); full detail in **Shipped**.

| # | Feature | Status |
|---|---------|--------|
| 1 | Surface adaptation decisions + fill the desktop right rail | Shipped 2026-06-06 |
| 2 | Guided set-row polish (Editorial draft C) | Shipped 2026-06-06 |
| 3 | Positioning copy (login + onboarding live surfaces) | Shipped 2026-06-06 |
| 11 | Lighten this week (user-initiated deload) | Shipped 2026-06-06 |
| 12 | Session-linked logging + `DecisionEvent` log (Phase 0 #3) | Shipped 2026-06-06 |
| 15 | Quick-start generation + Tune-your-plan panel | Shipped 2026-06-08 |

### Tier 2, next (the moat: personalization in generation)

**Personalization complete** (all generation-preference items shipped 2026-06-07 → 06-11). The one open row is #16, a non-personalization refinement.

| # | Feature | Notes |
|---|---------|-------|
| ~~GQ1~~ | ~~Generation quality, exercise ordering + pattern guardrails~~ | Shipped 2026-06-07. |
| ~~GQ2~~ | ~~Wire `fatigue` metadata into backfill priority~~ | Shipped 2026-06-07. |
| ~~GQ3~~ | ~~Role-aware exercise ranking~~ | Shipped 2026-06-08. The weekly-structure repair pass (the other half of the original title) is deferred pending a real reproduction. |
| ~~4~~ | ~~Training-style + variety + loading-lean preferences~~ | Shipped 2026-06-07 (#85 / #86 / #91). |
| ~~5~~ | ~~Movement restrictions~~ | Shipped 2026-06-08 (#94). Ranked multi-priority stays deferred to generation Phase 3 (a single `priority_muscle` covers ~80% of users). |
| ~~6~~ | ~~Equipment profiles~~ | Shipped 2026-06-09 (Branch A #99 storage + manager; Branch B #100 generation wiring). Travel mode (#322), the `expires_at` overlay on the same table, also shipped. See **Shipped**. |
| 16 | Reason-driven manual deload dose | **Refinement of the shipped Lighten-this-week (#11), validation-informed.** (Renumbered from #7 to clear the collision with the shipped Tier 3 #7 behavior-driven adaptation.) Today a manual lighten reuses the post-layoff ramp-back ease (0.6 volume, +1 RIR). That is a mild modeling error (Claude AI, science): a post-layoff ramp-back rebuilds a *detrained* lifter (a bigger cut is justified), whereas a mid-program "go easier" manages accumulated fatigue in a *fully-trained* lifter (less reduction is appropriate; over-cutting needlessly drops stimulus). So the manual dose should be **meaningfully gentler than the layoff dose**, and ideally **reason-driven**: add a reason on the lighten control (e.g. sick/run-down vs slammed-schedule, the latter is really a missed-volume week, not a fatigue event) and map each reason to its own ease factor, all gentler than ramp-back. Rough direction (not a defensible exact number, calibrate by use): ~0.7-0.8 volume, +0.5 to +1 RIR. **Code-truth gate (do this first):** `rampBackPrescription`'s volume cut has **no consumer** today, so the only *felt* ease is the integer `+1 RIR` (LogView), and the stored volume factor is inert. A "gentler" dose is therefore not expressible until actual volume reduction is wired into set rendering (and RIR can't go below +1 without being no ease at all). Sequence: wire real volume reduction → then reason→dose with manual < ramp-back. **Calibrate during the validation block:** if the cut feels too heavy for ordinary fatigue in real use, that is better evidence than the reasoning. **Copy already made honest (2026-06-06):** the ramp-back / lighten / manual copy now promises only the easier RIR (the overclaimed "reduced volume" was removed everywhere), so this item is purely the engine work, not a copy fix. |
| ~~13~~ | ~~Exercise history context while logging~~ | Core shipped 2026-06-11 (merged #124): pure `computeExerciseHistory` + `ExerciseHistoryPanel` in the expanded `ExerciseCard`. See **Shipped**. **Deferred:** thread the notes map into `ExerciseCard` so `previousNote` shows (currently fed `{}`); guided-mode panel parity (also in the page-depth backlog tier); a batched aggregator if one screen needs many; visual polish. |
| ~~18~~ | ~~PHUL (Power Hypertrophy Upper Lower) 4-day style~~ | Shipped 2026-06-11 (merged #122; "Suggested split" steering followed in #123). 4-day powerbuilding split, pure additive `EMPHASES`/`STYLES` data, every other style byte-identical. Known limitation: no `quad_iso`/`hamstring_iso` patterns, so leg curls/extensions are approximated (future Phase 0 #2). Spec: `docs/superpowers/specs/2026-06-11-12-53-42-phul-program-style-design.md`. See **Shipped**. |

**Foundations first:** the 2026-06-06 audit found generation's bottleneck is data richness, not the algorithm. **Phase 0** (muscle-attribution bridge, exercise-metadata expansion, session-linked `set_logs`) is complete and preceded #4 / #5. See "Routine generation v2, engine direction" below.

### Tier 3, then

| # | Feature | Notes |
|---|---------|-------|
| ~~7~~ | ~~Behavior-driven adaptation (v1.5)~~ **SHIPPED 2026-06-09 (swaps/demote slice; merged #111)** | Learn from logged behavior and bias future generation. **v1.5 shipped the swap signal:** demote-only (soft-deprioritize exercises the user repeatedly swaps away from), non-anchor patterns only (compounds protected), recency-bounded. **v1.6 follow-ons:** promote (swap-to), skips, added/cut volume -> emphasis, and permanent-swap capture, most safely gated behind reason-tagged swaps (#8) so the engine can tell preference from constraint. See **Shipped**. |
| ~~8~~ | ~~Smart substitution v2~~ **SHIPPED 2026-06-09 (merged #112)** | Reason-tagged swaps (pain / no equipment / crowded; null = preference) offering ranked same-stimulus suggestions, on top of the existing swap. **Shipped:** optional reason capture, a pure tiered `rankSubstitutes` (same `substitution_class` dominates, then a reason term, then name), reason chips in the swap picker (LogView only), and the #7 preference-only filter. See **Shipped**. v1.6 follow-ons: contraindication-aware pain across profile restrictions, no_equipment -> equipment profiles, cross-pattern same-stimulus, substitution.ts extraction. |
| 9 | Progress photos | (Promoted from Later.) Date-stamped photos alongside measurements, paired with the recomp dashboard. Outcomes beat metrics for most users. Needs file upload + a Supabase storage bucket (RLS) + a CSP `img-src` / `connect-src` update. Only item here with real infra cost. |
| 17 | Swap scope: temporary or permanent from the session | **Designed 2026-06-11, deferred (pick up later).** Give the session swap a scope choice so a disliked exercise can be replaced for good without going to Plan. Decisions (all confirmed): (1) scope lives IN `ExerciseSwapPicker` (an `allowScope` prop renders a "This week only" (default) / "From now on" row; `onSelect` widens to `(exerciseId, reason, permanent)`; the Revert row hides under permanent scope; LogView + guided mode pass `allowScope`, ProgramView keeps permanent-only but adopts the reason chips). (2) Permanent swaps feed behavior learning, the #7 v1.6 "permanent-swap capture": `swapRoutineExercisePermanently` extends to `(reId, newExId, week?, reason?)`, snapshots the old exercise_id, updates `routine_exercises`, and upserts the week's `exercise_swaps` row (`from_exercise_id`, reason, `permanent = true`, fresh `created_at`), which simultaneously captures the signal, carries the reason for the #7 constraint filter, and clears any stale temp override for the week; `loadSwapHistory` / `analyzeSwapBehavior` pick the rows up with zero engine change. (3) Reason chips show for both scopes. Migration: `exercise_swaps.permanent boolean not null default false` (future weighting; unused by the analyzer in v1). Ride-along hardening: `swapRoutineExercisePermanently` currently never validates the new exercise id as global-or-owned (the temp path does); add the check. Rejected: a separate capture table (needless surface), capturing without the flag (kills future weighting). ~5 files + migration + tests; no engine diff. |
| ~~10~~ | ~~Coach Decision Timeline~~ | Shipped 2026-06-06 (increment 1): read path + pure `decisionCopy` mapper + `CoachPanel` compact surface + full timeline overlay. See **Shipped**. **Deferred (increment 2):** derived events the log does not yet store (program created, block transition, PRs, priority change), layered into the same timeline with no schema change. |
| ~~14~~ | ~~Program pause / injury mode~~ | **SHIPPED 2026-06-09 (merged #109).** See **Shipped**. The anchor-semantics rethink resolved to a **dedicated `program_pauses` table** (a date span, `resumed_at IS NULL` = active, one active per routine), NOT a new `AdjustmentKind` (a pause spans many weeks; the week-keyed `program_adjustments` unique constraint would collide). No `decision_events` dual-write in v1 (deferred: a date-based pause does not dedupe cleanly in the week-keyed log; the Coach Timeline pause entries + an injury/illness/travel reason picker are the v2 follow-on). |

### Routine generation v2, engine direction

A full code-grounded architecture audit (2026-06-06; both AI reviews converged) is captured in `docs/superpowers/designs/2026-06-06-00-30-19-generation-engine-v2-audit.md`. Conclusion: **the generator is already well-architected and should not be rewritten. The next bottleneck is data richness, not the algorithm.** The shipped pipeline already is the layered model both reviews proposed: profile → training archetype (`STYLES` by day count) → movement-pattern slots (`EMPHASES`) → equipment-filtered selection → progression.

Single most important finding: generation is purely **slot-first** (fill session slots, weekly volume emerges). `VOLUME_TARGETS` / `priorityAdjustedTargets` exist but feed only the Progress / recovery analytics, never generation, and the two systems speak different vocabularies (15 `MovementPattern`s vs 10 `ExerciseCategory`s) with no bridge. That missing bridge is the foundation everything else depends on.

Phased plan (do in order, foundations before features; "build trust before more intelligence"). A research-consensus draft of the Phase 0 content (muscle map, metadata, contraindications, volume landmarks, training-style definitions, strength standards, adaptation copy), with reconciliation notes against Pulse's actual vocabulary, is captured in `docs/superpowers/designs/2026-06-06-00-54-52-phase0-source-material.md`.

- **Phase 0, data foundations (highest leverage, no visible feature) = "make coaching decisions computable":** build the weighted MovementPattern ↔ muscle bridge (reconcile the draft map to Pulse's real 15 patterns; the 6 isolation patterns are already muscle-specific so attribution is near 1:1, the real work is the ~7 compounds' secondary muscles; keep it a data table so more patterns can be added later without a rewrite, 7 compounds is enough to ship Phase 0-2, not the final model). **Decide muscle granularity first: keep the existing 10-category taxonomy and collapse only the muscle target side of the map into it; the 15 movement patterns stay the generation control layer, separate from the reporting categories (see the three-layer rule in Product decisions). Expand to quad/ham + delt splits only if specialization later demands it.** Expand exercise metadata (primary/secondary muscles into the generation path + backfill, add `fatigue_cost`, `unilateral`, `joint_stress`, `difficulty`, and a substitution class for equivalence grouping); add `session_id` + a workout date to `set_logs` so skip detection / adherence / behavior learning become trustworthy. **Define a standard `DecisionEvent` schema** (`type` = ramp_back / deload / progression / swap; plus `trigger`, `affected_area`, `magnitude`, `confidence`) and log every adaptive event through it, ramp-back already persists in `program_adjustments`, but auto-deload and progression do not, so the Phase 2 timeline is a computable system log, not a cosmetic journal. Optionally tune `VOLUME_TARGETS` toward the research landmarks (the draft's MAV runs higher than Pulse's current bands for back / glutes / shoulders). **(Status, 2026-06-06: Phase 0 COMPLETE. The muscle bridge (#1), the metadata seed + movement-pattern correction (#2), the fractional volume wiring + `VOLUME_TARGETS` retune, and now the session-linked logging + unified `DecisionEvent` log (Tier 1 #12) all shipped; `exercise_swaps.week` cap raised 12 → 52.)**
- **Phase 1, personalization (= Tier 2 generation work, all clean slot-first hooks):** training style (general / bodybuilding / strength / powerbuilding) → session `bias` + `repRange`; injury restrictions → pool filter (use the contraindication map); equipment preference (prefer barbell / dumbbell / machine, distinct from owned) → `byPattern` secondary sort; variety preference → avoid-set strictness. Generate fast, expose these as optional refinement (see Product decisions below). **(Status, 2026-06-13: COMPLETE. Training style + variety + loading lean shipped as Tier 2 #4, movement restrictions as #5.)**
- **Phase 2, trust & visibility (surface the intelligence already built, before building more):** the Coach Decision Timeline (Tier 3 #10), per-decision "what changed / why / what next" explanations (the adaptation copy is drafted), and a forward-looking "Today's focus" panel on Train. Requires the Phase 0 event / session logging. **(Status, 2026-06-13: Timeline + per-decision event copy shipped (`decisionCopy`, #10). Open: the "Today's focus" panel (adopted in the page-depth lane, now unblocked) and the explain-layer entry (near-term lane), which continues this track by generalizing the same voice to timeless rules.)**
- **Phase 3, behavior learning:** trustworthy skip tracking (needs Phase 0 session linkage), behavior-driven adaptation (= Tier 3 #7), smarter substitution, and an optional one-tap post-workout session rating. *Session rating is deferred here on purpose:* there is no session-level field today (RIR is per-set only), so it is genuinely new signal, not a duplicate, but average session RIR is already derivable from the logged sets as a cheap proxy, and it has a hard dependency on Phase 0 #3 (no generic event/session table exists to hang a rating on until then, `recordDecision` is ramp-back-only). Not near-term, not foundational. **(Status, 2026-06-13: #7 v1.5 swap slice + smart substitution v2 (#8) shipped. Open: skips (pairs with the Train session-event trail in the page-depth backlog), the #7/#8 v1.6 follow-ons, session rating.)**
- **Phase 4, advanced programming (only when ranked specialization genuinely demands it):** ranked multi-priority + the volume-first planner, built on the Phase 0 bridge. A single `priority_muscle` already covers ~80% of users, so this is deliberately last.

### Product decisions & principles (2026-06-06)

- **Gender is no longer a programming input, and is now fully optional.** Its only generation role today is `genderDefault` (female to glutes priority), a weak proxy. Replace it with a direct "what do you want to prioritize?" question (the deferred explicit-priority step). **Gender no longer gates the Strength Score (shipped 2026-06-06):** with a gender set it uses the gender-specific standards; without one it scores against a neutral gender-midpoint standard (`NEUTRAL_STANDARDS` / `standardsFor`) and flags the result `approximate`, so the user can skip the question and still get a number, refined later by setting gender. Net: gender stays for analytics + score precision, stops guessing the program, and is never required. **Science-validation candidate:** the midpoint as the "unspecified" baseline is a defensible engineering choice, not a researched standard, worth a pass through the Claude.ai loop if it matters.
- **Generate fast, refine later (onboarding-friction guard).** Every Phase 1 preference input is another onboarding step on top of the existing 6-7. Keep onboarding minimal and generate immediately, then expose style / restrictions / equipment-preference / variety as an optional post-generation "refine your plan" step (Profile or a tune-it screen), not as new required onboarding questions. Decide this before building Phase 1 so the funnel does not bloat.
- **Templates vs generation, pick a role.** 17 hand-authored templates and the generator coexist, and `recommendTemplate` is now largely vestigial, so two systems are drifting. Decide deliberately: either templates stay as the "named program I trust" anchor (5/3/1, PPL) with a distinct purpose, or they fold into generation over time. Otherwise they are maintenance debt. This is a decision to make before extending either path, not a feature.
- **Three-layer architecture (keep the layers separate).** Movement layer (the 15 `MovementPattern`s + equipment + slots) is the generation control system; Exercise layer (per-exercise muscles / fatigue / substitution class) is the mapping; Outcome layer (per-muscle volume, progression, adherence, recovery, the 10 categories) is reporting. **The outcome layer must never feed generation directly, only through the mapping layer.** This is already true (analytics never touch the generator) and must stay true when volume-first generation arrives: the planner reads targets through the bridge, it does not import the analytics. It is also what keeps `MovementPattern` (control) and the 10 categories (reporting) from collapsing into one another. The real moat this protects is traceable, explainable adaptation: every program change answers what changed, why, which rule fired, and what happens next.

### Infrastructure, i18n foundation (next-intl)

Deliberate infrastructure, chosen early. **Not a launch blocker, and must not block or delay the auth-lifecycle or comma-parsing floor work**, it runs as a parallel track on an isolated branch (large diff expected; pure extraction, review carefully). The goal: adding a second language later is a drop-in message file with zero refactor of the string layer, so we set up a real, App-Router-native i18n library now rather than a hand-rolled JSON convention that would itself need migrating later (that would defeat the purpose).

**Sequencing dependency (2026-06-12): the explain-layer entry (canonical `explainCopy.ts` reason registry + shared "why" affordance, see the near-term lane) lands BEFORE this extraction.** Centralizing collapses the drifting adaptive-voice strings (~4 deload phrasings, 2 progression variants, and the rest) into one canonical sentence per concept, so the `coaching` namespace extracts one string per concept instead of several drifting ones, and the "translated copy drifted from English" bug class cannot exist.

- **Library: `next-intl`**, built for Next.js 15 App Router + React Server Components, native ICU MessageFormat. Chosen over react-i18next (client-first, awkward in RSC), next-i18next (Pages-Router era), and Lingui / Paraglide (fine but not materially better here, less App-Router-native).
- **Mode: without i18n routing.** No `[locale]` route segment and no next-intl routing middleware. Configure `getRequestConfig`, wrap the tree with `NextIntlClientProvider` (in `PulseLayout`), and use `getTranslations` (server) / `useTranslations` (client). This avoids restructuring every route under `app/[locale]/` and any collision with the existing `/pulse` Supabase-session middleware (`src/middleware.ts`); no CSP change. A real second locale later is a small, localized add (detection + switcher), not a re-extraction.
- **Structure:** `messages/en/<namespace>.json` composed in `getRequestConfig`; a single `en` locale; no UI switcher. Namespaces by surface: `train`, `progress`, `plan`, `library`, `profile`, `common` (buttons, nav, generic labels), and `coaching` (all adaptive-voice strings: progression targets, stalled / deload copy, ramp-back nudges, priority-tilt rationale).
- **Extraction scope:** move every current user-facing hardcoded string into the right namespace. A move, not a rewrite, English text stays byte-identical.
- **Interpolation contract (strict):** all formatting and unit conversion happen in TS before the value reaches the message layer; pass display-ready named variables (weights arrive already unit-converted). No conversion functions (`toDisplay`) or unit logic inside message files. Conditional clauses use ICU `select` / composed messages, never string concatenation of fragments. Canonical case: `You stalled around {prevWeight} {unit} × {prevReps}, so back off on purpose. Drop to {targetWeight} {unit} × {targetReps}{deloadClause} to reset.`, `deloadClause` is an ICU-conditional message, not a glued string. The just-shipped `deloadTankClause` / `rirClause` (currently TS string-concat in `SetLogger`) are the first conversions and the proof of the pattern.
- **Double-duty strings (cannot use the React hook):** pure lib functions that emit user-facing strings, notably `buildRationale` (a pure function called from a server action, builds the priority-tilt sentence), plus the regen / ramp-back nudge copy, `strength.ts`'s `reason`, and the recommendation reasons. Restructure to return keys / structured data and translate at a layer that has a translator (`getTranslations` in the server action), or pass a translator in. Do not leave them hardcoded.
- **Verify:** rendered app visually and textually identical to before across all views (zero copy changes). Deliverable: a summary of the namespace structure and any strings that were doing double duty or needed special interpolation handling.

### Infrastructure, typed domain vocabulary (magic strings → const-objects/unions, domain-grouped types)

**Bucket 2 (maintainability), file-opportunistic, NOT launch floor.** A refactor with no user-facing payoff; it must not compete with the launch floor or the validation gate, and must not jump them. Do it **domain-by-domain, opportunistically, when already in a domain for feature work**, the same incremental discipline as the i18n extraction above. A one-pass type-reorg is a large diff touching nearly every import, exactly the churn to avoid near the floor (breakage risk when stability matters most). Each domain moved keeps typecheck + the full suite green.

- **Goal:** eliminate scattered hard-coded domain strings (movement patterns, categories, adjustment kinds, biases, etc.) in favor of centralized, typed, single-source-of-truth values, and organize shared type declarations by domain.
- **Group by DOMAIN, not by language construct.** Do **not** create `/types`, `/interfaces`, `/enums` folders, folder-by-construct fights how the code changes (a domain's types/interfaces/values change together, so the split forces every conceptual change across three directories). Instead group shared declarations by domain area under `lib/pulse/types/` (e.g. `exercise.ts`, `generation.ts`, `program.ts`, `adherence.ts`), each holding the types, interfaces, AND domain value-constants for that domain together. One barrel `index.ts` if clean imports help.
- **Do not split `type` vs `interface` into separate locations**, the distinction is a local implementation choice in TypeScript, not an organizing principle; both are shape declarations and belong together.
- **Co-locate vs centralize (decide per type, don't force one bucket):** centralize the cross-cutting shared vocabulary (`MovementPattern`, `ExerciseCategory`, the core domain unions/values used by many modules) in `lib/pulse/types/`; keep module-private types co-located with their module.
- **No TypeScript `enum`.** Use the const-object + derived-union pattern (`as const` + `type Foo = typeof Foo[keyof typeof Foo]`). TS `enum` has runtime quirks and `const enum` has known problems with bundlers / isolated-modules (relevant to the Next/SWC build); the const-object gives the same named-constant ergonomics, autocomplete, and exhaustiveness with none of the footguns. **Reframe the work:** much of the vocabulary already exists as union types (`MovementPattern`, `ExerciseCategory`, `AdjustmentKind`, `Bias`, and now `DecisionEventType`/`DecisionTrigger`), so for most code this is "centralize the canonical values and make scattered inline strings reference the union/const," not "introduce enums", pair each union with a const object of its values where logic needs the runtime values.

### Infrastructure, monorepo extraction then full dependency upgrade

Two sequenced tasks; the **order is hard: extract first on current versions, verify and deploy, only then upgrade.** Both are isolated jobs, not feature work. The extraction now **rides together with the Pulse → Laldy rebrand** (one sprint, one cutover); do i18n after the extraction, in the new repo, so two large diffs never collide.

**Task 1: rebrand + extract.** Fully designed in `docs/superpowers/specs/2026-06-15-14-48-46-laldy-rebrand-extraction-design.md` (codebase analysis + Perplexity/ChatGPT/Claude.ai loop, reconciled against code-truth). The earlier "flatten to apex root in `G:\Workspace\pulse-app`" plan is **superseded**; the corrected, locked decisions are:
- New repo **`laldy`**, a Turborepo monorepo: `apps/web` (the PWA), `apps/www` (apex marketing placeholder), empty `packages/{lib,ui}` reserved for when the Obytes Expo app arrives (gated behind the native-reopen bar; don't pre-factor the brain before then). **pnpm** with `nodeLinker: hoisted` (Metro/Expo-compatible).
- App at **`go.laldy.app`**, routes flattened to that **subdomain's** root (not the apex). Marketing reserved at apex `laldy.app` as a `noindex` static page. Two Vercel projects (different root dirs).
- **Same Supabase project** reused: data carries over (keyed by account, not origin), config-only change (Site URL + redirect allow-list). Auth is email/password only, cookies already host-scoped, redirects use dynamic `${origin}`, so the auth migration is small.
- **Brand-neutral** internal rename (`App*`, `useApp`, `--color-app-*`, `src/components/app/`): the brand "Laldy" lives only in ~25 user-facing strings + the manifest, so a future rename never re-touches ~1,200 token usages.
- Sequence: provision DNS/SSL early → scaffold (pinned to current versions) → move on current names+routes (green) → flatten routes + infra (split into route/manifest/middleware/CSP sub-commits, each gated) → neutral internal rename → user-facing brand sweep → Vercel-preview rehearsal → cutover (drain offline queues to provable zero, keep old origin as rollback, both users reinstall the PWA; no tombstone build at this user count) → soak ~1-2 weeks → **(separate fast-follow deploy) full dependency upgrade** (codemod pre-pass → low-risk group → Next 15→16 incl. `middleware.ts`→`proxy.ts` → Supabase libs, verifying `@supabase/ssr` on Next 16 first). Discrete verified commits.

**Task 2: Full dependency upgrade (a separate fast-follow deploy after the migration soaks on real training ~1-2 weeks, NOT folded into the cutover, review-loop-confirmed 2026-06-15).** The brief fold-in was reversed: there is only one config/DNS/origin cutover either way, so deploying the upgrade to the already-live `go.laldy.app` afterward is routine and keeps the Next major off the surfaces (middleware/CSP/cookies) the migration already disturbs. Add a `@next/codemod` pre-pass; **Next 16 renames `middleware.ts` → `proxy.ts`** (so the CSP/session middleware moves) and flips caching/Turbopack defaults; verify `@supabase/ssr` supports Next 16 before bumping it. Still three isolated passes, each green:
- Produce an upgrade plan table first (dep / current / latest / risk) before touching anything.
- **Target: fully current stack.** Next 15 → 16 (official codemods, not hand-migration; watch caching defaults, async request APIs, Supabase SSR cookie handling, PWA service worker behaviour). React 19 is already current. Tailwind v4 is already current.
- **Upgrade in three isolated passes, each verified green before the next:**
  - Pass 1 (low-risk group): `swr`, `vitest` (2.x → 3.x is a major; config may shift), `eslint` + `eslint-config-next` (sync to the Next version), `typescript`, `prettier`, `prettier-plugin-tailwindcss`, `react-icons`, `@types/*`, `jsdom` (pin consistently), `postcss`, Tailwind v4 minors.
  - Pass 2 (Next major): Next 15 → 16 as its own isolated commit using official codemods. Verify core flows: auth, log a set, generation, progress, PWA install/offline. Report any behavioural changes, especially caching and RSC behaviour.
  - Pass 3 (Supabase): bump `@supabase/ssr` and `@supabase/supabase-js` against the upgraded Next. Never stack this with the Next major bump. `@supabase/ssr` is pre-1.0, treat a minor as potentially breaking; read its changelog and verify auth + data flows end-to-end.
- Output: plan table first, then the upgraded project with codemods handled and anything deliberately not upgraded (and why). User soak-tests on real training before merging. Upgrades only -- no features, no copy changes, no refactors beyond what an upgrade strictly requires.

### Native client, deferred direction (Expo + react-native-web)

A decision record, not a planned task. This settles **how** Pulse would go native **if** it ever does; **whether** it does is genuinely open and gated behind two gates, not one. Most likely it stays "not yet" for a long time, and that is a fine place for it to stay: the PWA already is the cross-platform app.

- **Unbundle "App Store presence" from "rewrite the client."** They are separate decisions. Store presence is testable cheaply by wrapping the existing PWA with Capacitor plus one genuine native feature (push) to clear Apple's guideline 4.2 (thin web wrappers get rejected). **The wrap tests discovery and trust only, not native feel**, so a positive wrap result is evidence for *staying wrapped*, not for rewriting. Don't let a good wrap justify the expensive bet. **iOS caveat (price it as a small project, not a config flag):** Capacitor renders in WKWebView, which does not reliably run service workers, so the SW-based offline *shell* needs Capacitor's native cache equivalent. The IndexedDB write queue (the data-safety layer) works fine in WKWebView, so offline writes survive the wrap, only the SW piece is re-homed.
- **The traffic motivation argues for the wrap, against the rewrite.** App-store discovery for an unknown solo app in a category owned by Hevy / Strong / Fitbod is effectively zero without paid UA or an existing audience; the store is a conversion-and-trust surface for traffic you drove there, not a discovery channel. That benefit is almost entirely captured by the cheap wrap. The rewrite buys native feel + watch, which is a *different* goal, confirm you actually have it before paying.
- **If the gates are cleared and native feel / watch is genuinely needed: route is Expo + react-native-web** (one codebase to iOS / Android / web), **not native-only RN**, RNW keeps a web target, so it protects the cross-platform moat instead of trading it away. Viable for Pulse specifically (audited): the data layer is already client-SWR (read path ports; supabase-js runs in RN; only the server-action mutations need rework), 14 of 20 `lib/pulse` files are web-independent (the brain ports verbatim), and NativeWind carries the Tailwind tokens. Cost: a full presentation-layer rewrite (App Router, service worker, server actions, all CSS do not port).
- **RNW keeps a web app, so the product stays linkable / usable-in-browser**, shareability is *not* lost (and the app is already behind login today). The real, smaller loss is web-app polish / SEO vs bespoke Next, handled by **keeping the Next marketing + landing site as the real-web SEO surface** (app screens are authed, never needed SEO). Escape hatch if the RNW web app proves too weak for the app itself: Next-for-web + RN-for-native sharing the brain (two UI layers, the maintenance cost RNW avoids), a build-time fork, not a now-decision.
- **Credit the no-install edge to the web target, not to deeplinks.** The indie distribution advantage is the *absence of an install step on the conversion path*: a link opens the running web app with zero friction, while a deeplink to a native app still resolves, for an *uninstalled* prospect, to an App Store download (the conversion cliff). Deeplinks only serve installed-user continuity. So the web target is what preserves cold-prospect conversion. **Do not drift to native-only on the theory "deeplinks handle sharing"**, that drops the no-install path, and deeplinks cannot recover it. Keep the web target and deeplinks are a bonus on top.
- **Knock-on effects on already-queued work:** the monorepo question reopens only at the point of committing to Expo (Expo universal *is* a monorepo: shared `packages/app` consumed by `apps/native` + `apps/web`), informed by two real apps rather than guessed at. The queued standalone-repo extraction should keep `lib/pulse` cleanly importable so it becomes that shared package with no re-extraction. The launch-floor offline-queue poison-pill fix doubles as separating queue *policy* from *storage*, which makes the eventual IndexedDB → SQLite/MMKV swap a swap, not a rewrite.
- **Reopen bar (a real gate, not a formality):** a committed active-user number, **plus** the validation gate cleared (completion-paced adaptive coaching proven to work for real users, native is downstream of the product being worth distributing wider), **plus** specific evidence the PWA itself is the wall: watch-centric lifters refusing to switch, iOS PWA storage eviction, or App Store being the channel you actually need.
- **Native-fidelity exploration (deferred, explore when native reopens; noted 2026-06-15):** if/when the Expo app is built, explore going beyond a plain NativeWind app toward a genuinely native iOS feel. Reference point: the mijndelta-app (also Obytes-starter) feels "decent, not exceptional" on iOS, so aim higher. Levers (verify current at build time, the space moves fast): iOS 26 **Liquid Glass** via `expo-glass-effect` / `GlassView` plus native tab/nav bars (Expo Router native tabs or `react-native-bottom-tabs`, which adopt the glass material automatically), native menus (`zeego` / `@react-native-menu/menu`), SF Symbols (`expo-symbols`), native sheets/blur/haptics, and `expo-ui` (experimental SwiftUI bindings) for real native controls. Working pattern: **native components for system chrome (auto-glass), NativeWind for content.** Reachable ceiling is roughly 80-90% of native feel; the last bit (day-one new components, pixel-perfect SwiftUI fidelity, bespoke native interactions) needs native modules. **Tension to weigh:** every iOS-26-native component is iOS-specific UI that does not serve web/Android, eroding the one-codebase / web-target rationale above, so scope deliberately rather than chasing pixel-parity with native-only apps like Liftin. Gated behind the same reopen bar.

### Social direction, deferred (not social-first)

A decision record, not a planned task. Pulse is a training coach, not a social network. This section documents the long-term direction so the distinction between "hard-out permanently" and "deferred, not ruled out" is clear and explicit, and so the right design constraints are captured before any social surface is built. Nothing here is scheduled work.

**The reframe.** "Not social" was always a shorthand for "not social-network-first, not engagement-optimised, not comparison-driven." It was never a permanent ban on features that help people train together, share useful training work, or hold each other accountable. The honest framing is: Pulse is not social-first. The adaptive coach is the product. Social features, if and when they arrive, support training adherence -- they do not replace it.

**The test for any proposed social feature.** Does it make training better, or does it only make the app more sticky? Features that only do the latter are out. A friend seeing your PR is useful accountability. A leaderboard ranking your PR against strangers is engagement farming.

**The four phases, as a documented direction.**

- **Phase 1: Routine link sharing.** A user can share a routine via a URL. The recipient sees the routine's structure and can install it. On install, Pulse adapts the routine to their equipment, session length, and preferences via the existing generation machinery. No social graph required. No account needed to view. No follower mechanics. Closest analogy: an export that someone else can run. This is the smallest useful first step and the foundation for Phase 4. *Reopen bar:* validation gate cleared, real users exist, and routine sharing has a clear observed use case (users asking how to share their program).
- **Phase 2: Friend activity.** Users can add friends (mutual accept required -- no follow-without-consent). Friends see a simple activity stream of training events: workout completed, new PR, program completed, streak milestone, first workout back after a gap. No engagement actions beyond a single congratulate tap (no like counts displayed). Activity is a secondary surface, never the home screen. *Reopen bar:* Phase 1 shipped, enough active users that friend activity feels alive for at least some of them.
- **Phase 3: Private accountability groups.** Small closed groups (2-20 people) for friends, family, or gym partners. The group sees the same Phase 2 activity events scoped to members. No public discovery of groups, no public member lists, no rankings within the group. *Reopen bar:* Phase 2 shipped, evidence that bilateral friend activity isn't sufficient for the accountability use case.
- **Phase 4: Public routine pages.** A user can make a routine public, which generates a browsable page showing the split, exercises, equipment required, program length, and copy count. No public user profile. No follower count. Light signals: copy count (not likes), optional 1-5 rating on install. Users browse without an account; installing requires one. A content moderation plan is required before this ships. *Reopen bar:* Phase 1 shipped, real user base exists, evidence that routine discovery is a meaningful acquisition channel.

**Hard-out list (permanent, not deferred).** Global leaderboards or competitive rankings. Public follower counts displayed anywhere. Feed-first home screen. Viral challenges or streak wars. Likes or reactions on body or progress photos. Network/comparison achievements. DMs or in-app messaging (high moderation burden, low training value). Creator economy or monetised profiles. Follow without mutual consent. Public profiles that surface personal body data.

**Domain model to consider when the time comes (not schema yet).**

- Phase 1: `routines.visibility` ('private' | 'link_only' | 'public'), `routines.share_token` uuid, `routines.slug` text, public route `/r/[share_token]`.
- Phase 2: `user_connections(id, requester_id, addressee_id, status, created_at)` status: pending | accepted | blocked. `activity_events(id, user_id, event_type, payload jsonb, visibility, created_at)` -- event_type: workout_completed | pr_achieved | program_completed | streak_milestone | first_back_after_gap. Visibility: friends_only (default) | group_only | private.
- Phase 3: `groups(id, name, created_by, created_at)`, `group_members(group_id, user_id, role, joined_at)`. Activity events gain optional `group_id` scoping.
- Phase 4: `routine_installs(id, routine_id, installed_by, installed_at)` for copy count. `routine_ratings(id, routine_id, rated_by, rating smallint, created_at)`, one per user per routine.
- Never: a general `followers` table (asymmetric follow moves toward the Twitter model), engagement metrics stored as columns (view counts, like counts -- these optimise for the wrong thing), personal body data joined to any public-facing table.

**Privacy and safety constraints to carry from the start.** Private by default on every new social object. Friend requests require mutual acceptance. GDPR erasure cascades to all social graph data (connections, activity events, group memberships). Account deletion removes all of it. No body photo sharing in any social context. Activity events are friends-only by default, never public by default. Phase 4 moderation plan (routine names and descriptions are user-generated text, even if benign) must exist before public routines ship. No username search / user discovery until Phase 2, and even then prefer invite-by-link over open search.

**What does not change now.** Auth surface, account profiles, visibility settings, and the Train screen are all unchanged. Phase 1 (routine sharing) is the only phase that touches existing surfaces (adding a share button to a routine and a public-facing read-only route). Phases 2-4 each add new surfaces rather than modifying existing ones, which keeps the blast radius of each phase contained.

**Risks.** (1) Social gravity: each phase makes the next easier to justify. The hard-out list above is only useful if it is actively defended at each phase gate, not just written once. (2) Network effects in Phase 2: friend activity needs a cluster of users training simultaneously to feel alive. If the user base is small when Phase 2 ships, the feature will feel empty. Phase 1 has standalone value; Phase 2 does not. (3) Moderation cliff in Phase 4: user-generated text at scale requires a plan before it ships, not after. (4) Auth surface expansion: friend-adding requires user discovery. Username/handles or email-based invites are both new surfaces with non-trivial privacy and abuse implications. Phase 1 avoids this entirely.

### Monetization philosophy, decision record (not a planned task)

A decision record, capturing the principle that should govern pricing once the auth + billing lifecycle (launch floor, above) actually gets built, so choices made under launch pressure don't drift from what the product already promises. The launch-floor bullet above names the *mechanics* (a clean cancel / refund / billing-transparency path, the positioning note that lifetime pricing fits the no-dark-patterns stance); this names the *principle* behind them, so the mechanics have something to answer to.

- **The test.** Premium pays for the adaptive coaching, personalization, generation, adaptation, the Coach Decision Timeline, the actual moat, never for the basic logging loop. A free tier that can log a workout, run one program, and watch real progress is the floor; paywalling the core loop turns a training partner into a toll booth and contradicts "an adaptive coach, not a logger" at the business-model layer, not just the marketing layer. Define this free / paid line on purpose, before any paywall gets built, not as a side effect of "whichever surfaces were easiest to gate in code."
- **Carried from the launch floor (restated for the record, not new policy):** no ads, no selling user data (matches the "calm, private, no surveillance" stance already stated for analytics); no dark patterns, no confusing cancellation, no charges after account deletion (already named the #1 complaint driver across the Runna review sweep in the launch-floor bullet above); a one-time / lifetime option carries real weight as a subscription-fatigue antidote and a trust signal even if the eventual call stays subscription-with-honest-terms; account deletion and billing cancellation read as one clean flow, not two separate frustrations to survive.
- **New to this record.** The free / paid line itself (logging, one program, and basic progress free; generation personalization, the Coach Decision Timeline, and deeper analytics as the premium surface is the natural cut, matching what the product is actually for) and the pricing structure (subscription vs lifetime vs hybrid) are product decisions with real consequences, not implementation details to default on. Decide them at the same table as the billing-lifecycle build, not earlier (pricing decided pre-validation calcifies around assumptions the validation block may overturn) and not as an afterthought once the billing code already exists.
- **Reopen bar.** This stays a principle, not a build, until the auth + billing lifecycle item is actually scheduled, that is the moment the free / paid line and the pricing structure stop being a decision record and become a real build decision that needs answering before the first paywall ships.

### Infrastructure, product analytics (PostHog, EU), post-launch

A backlog decision record, not a launch-floor item. Instruments the **public** phase, once there are strangers to learn from; it does **not** gate launch and must not delay it.

- **Tool: PostHog, EU Cloud region.** Chosen over Vercel Analytics / GA because the need is **product analytics** (event-level user behavior), not web analytics (pageviews / traffic). The **EU region is required, not optional**: Pulse is NL-based, privacy-positioned (no ads, no surveillance, a calm space), launching to an EU audience, data stays in the EU and the GDPR story stays simple and honest.
- **Posture: instrument for learning, not surveillance.** Do **not** track-everything-figure-it-out-later, that is the surveillance default the positioning rejects, and a solo dev won't analyze undirected event noise anyway. Define a small, deliberate event set (~a dozen), each tied to a question already on the validation gate: **adaptive events firing** (ramp-back offered, deload triggered, behind/lapsed flagged); **override behavior** (deload followed vs dismissed, ramp-back accepted vs ignored, lighten-this-week used); **core-loop completion** (workout started vs finished, sets logged per session); **feel-coached surfaces** (Coach Timeline opened, adaptation explanations seen). Nothing beyond that set.
- **GDPR / consent is a hard requirement, the same legal floor as account-deletion + a privacy policy.** Behavioral analytics needs a truthful privacy-policy disclosure and a consent story (cookie/consent banner, with the privacy-preserving default selected). Analytics does not skip the legal floor; those legal pages are themselves launch-floor work.
- **Sequencing, the two-user validation gate does NOT need PostHog.** The gate is covered by the `DecisionEvent` log (Phase 0 #3 / Tier 1 #12), which captures the adaptive events as first-class rows in our own Supabase, no third party, no consent story for our own data, so validation runs off `DecisionEvent` + direct DB queries on our own training. PostHog is therefore deferred until real users exist whom you can't eyeball in the DB. Don't build it pre-launch.
- **Guard: analytics tells what, not why.** The deepest validation question, does completion-paced coaching actually keep someone progressing, is a judgment from watching real training, not a dashboard number. Instrument to *support* that judgment, never to replace it.

---

## Later

The Hold bucket from the re-tiered roadmap, in detail. Web-native moderate-value items first, then data-gated work, then native-platform and scale-gated items last. (Progress photos was promoted to Tier 3 #9 on 2026-06-06; CSV export and Muscle priority shipped earlier.)

| Feature | Notes |
|---|---|
| Expand template library | Evaluate and add high-demand named programs (Starting Strength, StrongLifts 5x5, PHUL, PHAT, PPL variants, Juggernaut Method, etc.) from the top-30 list reviewed 2026-06-07. Gated behind the "Templates vs generation, pick a role" product decision (see above) -- if templates are staying as the "named program I trust" anchor, this is the content work that populates that lane. Do not build before that decision is made. |
| Supersets (advanced) | Tri-sets, giant sets, AMRAP tracking. After basic superset support (shipped). Niche. |
| Exercise "how to perform" media | Image / gif / video in the instruction modal (text-only today). Needs a storage + content decision and shares infra with Tier 3 #9 Progress photos (Supabase storage bucket + RLS + a CSP `img-src` / `connect-src` update); scope the two together. Surfaced in the 2026-06-06 live test. |
| Guided screen landscape / short-viewport layout | In landscape (phone sideways, where `useMediaQuery('(min-width: 1024px)')` is still false so the single-column Focus layout renders into a short viewport), the hero, title, "next exercise", and "Finish early" stack too tall and push the set inputs + rest timer below the fold. Needs a height-aware layout that compresses the hero (smaller title, inline meta, shrink/drop the progress ring) so the logging area stays visible. The 2026-06-07 title-size clamp only helps the portrait case; scope a proper redesign, not font patches. Surfaced in the 2026-06-06 live test. |
| Computed starting-weight estimate | The first-time "start light, leave N in the tank" cue shipped; the actual computed starting weight needs the strength model. Scope separately. Surfaced in the 2026-06-06 live test. |
| Achievements + gamification | The self-referential milestones FEED shipped 2026-06-12 (`milestones.ts`: PRs, streak records, weeks completed, session counts, on Progress Overview). This row now covers only badges/celebration beyond the feed; implement after real usage data, badges only land well at milestones users actually reach. (also in: Jefit, Boostcamp) |
| Year / period in review | Shareable annual and monthly recap of volume, PRs, streaks, and milestones. Retention and organic reach. Needs a year of data to be meaningful. (also in: Hevy, Jefit, Boostcamp) |
| AI workout generation (v2) | Rule-based generation from onboarding is shipped. v2 adapts split, volume, and exercise selection based on actual logged performance. Needs months of logged data to not feel random, premature at current scale. Superseded for now by Tier 3 #7 (behavior-driven adaptation), which gets most of the benefit without the data requirement or the black-box feel. (also in: Fitbod, Jefit, Boostcamp, Alpha, Setgraph) |
| ExerciseCard memo effectiveness | `ExerciseCard` is wrapped in `React.memo`, but the save path still passes the whole `logs`/`prMap` objects (new refs on every save) so all cards re-render anyway. Slice per-card data upstream in `LogView` (own set entries + savedCount + per-exercise PR) so unchanged cards keep stable props and a save touches one card. Refactor of the hottest screen, defer until it actually hurts at scale. |
| Apple Health / Google Fit sync | Import bodyweight / activity from wearables and calorie trackers; export workouts. Native-gated, HealthKit is iOS-native (no web API) and Google has been moving Fit to Android-native Health Connect, so this means going off the web (Capacitor/native). Low ROI at two users. (also in: Hevy, Strong, Fitbod, Jefit, Caliber) |
| Wearable integration | Garmin, Apple Watch, Whoop. Heart rate during sets, auto rest timer from HRV. Native-platform integration, same off-the-web cost as Health/Fit sync; far off. |
| Social / sharing | Friends feed, likes, follow. Requires critical user mass. Not before traction. |
| Myoreps / rest-pause set type | A distinct set structure, not a `drops` variant (work a set close to failure, rest 3-5 breaths, repeat short clusters to a rep target), on MacroFactor Workouts' set-type list; Pulse ships drop sets + a failure tag but not this. **Code-truth:** there is no `set_type` concept today, set kinds are inferred (a non-empty `drops` jsonb array means a drop set, `rir = 0` means failure, warmup sets are computed client-side and never persisted), so this is genuinely new structure, not a `drops` extension: a set-type field, a multi-segment rest-pause UI/timer in `SetLogger`, and rep-attribution changes in the progression / volume math (deciding which reps count toward the working total and the e1RM). Niche, evidence-based-hypertrophy-segment signal that the target user skews toward; low priority, scope properly when picked up rather than bolting onto `drops`. |
| Per-side (bilateral asymmetry) tracking | MacroFactor Workouts logs left and right separately for unilateral lifts, useful for users training around an imbalance or a return from injury. Pulse already tags `unilateral` on exercise metadata (seeded across all 94 exercises) but the field is purely descriptive today, generation has exactly one comment referencing it and no behavior keys off it. **Code-truth:** zero per-side infrastructure exists, no left/right columns on `set_logs`, no UI, no aggregation. Adding it safely (per-side logging + a read-only asymmetry report) is moderate work; wiring per-side data into progression (which side sets next week's target?) is high-risk and not recommended as a first cut. Low priority for the current segment, becomes genuinely relevant on the movement-restrictions / injury-mode path (#5, #14) where imbalance tracking earns its keep, scope to logging + reporting only if it is ever picked up. |

---

## Deprioritized (evaluated 2026-06-04, not planned)

Mostly from a ChatGPT feature audit (2026-06-04), plus later additions. Real ideas, but low value at current scale or trust-risky, revisit only if the rationale changes.

Ordered by revisit-likelihood: small/standalone ideas first, then those gated on a future v2 adaptive engine, then the trust-risky or heavy-modeling ones last.

| Feature | Why not |
|---|---|
| Expand exercise library (94 to 200-250) | The 6-field metadata schema is stable, so there is no day-one-metadata decay cost to adding exercises later, and 94 metadata-complete exercises are already "enough to ship Phase 0-2" (engine-direction notes). A bulk 94-to-250 tagging pass is tedious, error-prone, and gates nothing at current scale. Revisit as targeted adds only if P0 1.x / 2.x expose a specific thin pattern (a few cable / dumbbell / vertical-pull options), not a bulk pass. (Moved here from Later 2026-06-10.) |
| 4-week program block | Too short to hold an accumulation → intensification → deload arc; it is a different program *type* (minicut / travel / peak), not a periodization length. Serve later as a short-block template if needed, not via `buildProgram`. |
| 24-week program block | Redundant with the repeating-block design: running the 12-week block twice yields two deloads (better spacing than one long ramp), and most users never finish a 24-week monolith. Program lengths stay at 8 / 10 / 12 / 16, default 12; each is already a distinct phase table (not a stretched copy). |
| Recovery warnings (e.g. legs Mon+Tue) | Schedule is user-chosen and small; low value. Smallest standalone rule if ever revisited. |
| Program coherence score (X/100) | Decorative for a deterministic generator that's already correct by construction (slot-based, equipment-filtered, volume floors). It can't emit an incoherent program. Only earns its keep guarding a fuzzy v2 adaptive engine. |
| Dynamic volume management | Needs the v2 adaptive engine + logged data. Bundle with v2 if pursued. |
| Exercise recommendation AI | Vague; rule-based selection + the shipped preferences/swap already cover the real need. |
| Goal forecasting ("86 kg in 12 weeks") | Easy to get wrong; a missed prediction erodes trust more than it motivates. |
| Muscle recovery / fatigue modeling | Fitbod's signature, but heavy modeling for marginal payoff at two users and risks feeling pseudo-scientific. |

---

## Generation quality & setup flow, backlog (added 2026-06-10)

Audit of `generation.ts`, `actions/routines.ts`, the `EMPHASES`/metadata data, and `RoutineSetupFlow.tsx`. Every finding below was verified against the live tree on 2026-06-10. **Key audit result: all six setup-flow findings (subsection 4) were already shipped before this audit ran**, so the only open work is the generation-quality track (subsections 1, 2, 3). Each shipped entry is kept with its verification note rather than dropped, so the discrepancy is on the record.

Recommended implementation order (open items only):
1. Entries 1.1 + 1.2 + 1.3 + 1.4 together (one `EMPHASES` edit pass, one branch). **Implemented 2026-06-10 on `feature/generation-emphasis-fixes`** (in review, see Shipped); the 1.1 composition is clean separation (quad days drop `hinge`, posterior day drops `squat`).
2. Entries 2.1 + 2.2 + 2.3 together (one idempotent seed migration).
3. Entry 3.1 (compound-slot ranking guard, after 1.x and 2.x are merged).

Subsection 4 (setup flow): findings 4.1 through 4.6 are **already shipped**, verified in live code. No action. The original proposed order (4.1, then 4.6, then 4.2, then 4.3+4.4+4.5) is retained below per entry, each marked shipped.

### Subsection 1: generation quality, emphasis data bugs

Pure data fixes to `EMPHASES` in `generation.ts`. No logic change. Highest leverage: they touch every generated routine.

#### 1.1 Lower and Legs are structurally identical (`ulppl-5`, `ul-classic-4`)

**Status:** Backlog (open, verified)
**Track:** Generation quality
**Prerequisites:** None
**Decision needed:** Slot-count composition (see Fix). The task brief called this "none", but reaching the 6-exercise target without a primary hinge/squat needs one explicit composition choice.

**Root cause:** `generation.ts:51-58`, `lower_quad` = `[squat, lunge, hinge, glute_iso, calf, core]` and `lower_post` = `[hinge, glute_iso, lunge, squat, calf, core]` are the **same six patterns reordered**. Both are used together by `ulppl-5` (`generation.ts:281` lower_quad, `:284` lower_post) and `ul-classic-4` (`:234` lower_quad, `:235` lower_post). Under `varietyPreference = 'consistent'` the anchor map (`pick`, `generation.ts:734-756`) pins one exercise per `COMPOUND_ANCHOR_PATTERNS` slot (squat, hinge, ...) across the week, so the same squat and same hinge recur in both sessions: Lower becomes "Legs with the leftovers."
**Symptom:** users on the default 5-day or 4-day split see near-identical Lower and Legs sessions.
**Fix:** make `lower_quad` quad-led (primary `squat`, `lunge`; no `hinge` as a primary slot) and `lower_post` posterior-led (primary `hinge`, `glute_iso`; no `squat` as a primary slot; `lunge` may stay as a secondary compound). Keep `calf` + `core` as accessories in both. **Constraint to resolve:** dropping hinge from `lower_quad` and squat from `lower_post` leaves each at 5 patterns, but `volumeFor('45–60 min', 'intermediate')` targets 6 (`generation.ts:380-384`). Either add a sixth accessory slot per emphasis (e.g. a second posterior accessory in `lower_post`) or allow the dropped compound as a trailing non-leading backfill slot. Pick one before editing so backfill does not re-introduce the duplicate it is meant to kill.
**Test stub:** generate `ulppl-5` and `ul-classic-4`; assert Lower and Legs share at most 1 exercise by id. Assert no `lower_post` session leads with a `squat` compound. All golden identity / `consistent`-determinism tests stay green.
**Verified:** confirmed exactly as described in live code.

#### 1.2 `hinge` slot in `pull` emphasis puts a deadlift/RDL on Pull day

**Status:** Backlog (open, verified)
**Track:** Generation quality
**Prerequisites:** None (sequence with 1.3/1.4 within the same edit pass)
**Decision needed:** None

**Root cause:** `generation.ts:98-101`, `pull` = `[horizontal_pull, hinge, back_iso, shoulder_iso, biceps_iso]` carries a `hinge` slot. In `ulppl-5` hinge is then demanded by Lower (`lower_quad`), Pull, and Legs (`lower_post`): **3 of 5 sessions**. Pull is a horizontal-pull / vertical-pull / biceps day; hinge does not belong there architecturally.
**Symptom:** a heavy deadlift or RDL surfaces on Pull day, which reads as incoherent. By the third hinge demand the hinge compound pool is thinned by the routine-wide avoid-set + substitution-class dedup (see 2.2).
**Fix:** remove `hinge` from `pull`; replace with `vertical_pull` (coordinates with 1.3). If the slot count still falls below the `volumeFor` target after the swap, add `back_iso` as well.
**Test stub:** generate any routine with a Pull session; assert zero exercises with `movement_pattern = 'hinge'` in that session.
**Verified:** confirmed exactly. `pull` contains `hinge`; `ulppl-5` demands hinge in 3 of 5 sessions.

#### 1.3 `vertical_pull` absent from every emphasis slot list

**Status:** Backlog (open, verified)
**Track:** Generation quality
**Prerequisites:** None (pairs with 1.2)
**Decision needed:** None. No-op safety confirmed (below), so no prerequisite blocker.

**Root cause:** `vertical_pull` appears nowhere in any `EMPHASES` slot list (grep: it occurs only in `COMPOUND_ANCHOR_PATTERNS` `generation.ts:474`, `patternTier` `:556`, `PULL_PATTERNS` `:819`, and the omission comment `:28-30`). The comment states it was dropped globally to protect dumbbell-only users (no pull-up bar = no usable option). That protection belongs to the equipment filter, not the slot list.
**Symptom:** Gym users with cables / lat-pulldown never get any vertical pulling exercise in any generated routine.
**Fix:** add `vertical_pull` to `pull` (covered by 1.2) and to `upper_general` (and any equivalent upper-back emphasis). Home users with no `pull_up_bar`/`machines` simply no-op the slot; Gym users get a Lat Pulldown / Pull-Up.
**No-op safety (verified):** `pick` calls `byPattern(slot)` then `if (candidates.length === 0) return false` (`generation.ts:713-714`), so an empty pattern silently no-ops and the backfill loop (`:780-801`) covers the gap from other patterns. No crash, no empty session. **No prerequisite blocker.**
**Test stub:** Gym profile, routine with a Pull session, assert at least one `vertical_pull` exercise. Home profile (no pull-up bar / machines), same routine, assert zero `vertical_pull` and no error.
**Verified:** confirmed absent; no-op-and-backfill safety confirmed in `pick`.

#### 1.4 `push` and `pull` have 5 slots but `volumeFor` targets 6 at 45-60 min

**Status:** Backlog (open, verified)
**Track:** Generation quality
**Prerequisites:** Sequence after 1.2 and 1.3 (so the post-edit `pull` slot count is known)
**Decision needed:** Whether `pull`'s sixth slot is `back_iso` or `vertical_pull`, decided once 1.2/1.3 land.

**Root cause:** `push` (`generation.ts:94-97`) and `pull` (`:98-101`) each list 5 slots; `volumeFor('45–60 min', 'intermediate')` returns `{ exercises: 6, sets: 3 }` (`VOLUME` `:380-384`, `volumeFor` `:392`). So backfill (`:780-801`) fires on every Push and Pull session at this length. Backfill revisits covered slots in emphasis order (uncovered-first then covered), so the sixth exercise is whichever pattern yields a fresh candidate first, deterministic per pool but undesigned and pool-dependent.
**Symptom:** Push/Pull sessions get an unintentional sixth exercise (a doubled pattern) instead of a deliberate sixth slot.
**Fix:** add an explicit sixth slot. `push`: `chest_iso` if absent else `vertical_push` (placed after compounds, before isolation, so `patternTier` order holds). `pull`: `back_iso`, or `vertical_pull` if 1.3 is in.
**Test stub:** generate a Push session at 45-60 min; assert the sixth exercise matches the intended sixth-slot pattern, not a backfill artifact. Compare before/after.
**Verified:** confirmed, both `push` and `pull` have exactly 5 slots; `volumeFor` returns 6 exercises for intermediate at 45-60 min.

### Subsection 2: generation quality, metadata bugs

One idempotent seed migration (`UPDATE`s scoped to `user_id IS NULL`). **Status: SHIPPED 2026-06-11** as `docs/migrations/2026-06-11-10-19-55-p0-group2-exercise-metadata-corrections.sql` (merged #120, **applied to prod and verified 2026-06-12**). All three entries below re-verified against the seed files at authoring time; kept as decision records.

#### 2.1 Chest isolation exercises have no `substitution_class`

**Status:** SHIPPED 2026-06-11 (in the Group 2 migration above)
**Track:** Generation quality
**Prerequisites:** None
**Decision needed:** None

**Root cause:** in `2026-06-06-11-28-49-exercise-metadata-fields-seed.sql`, `Cable Fly` (line 68), `Chest Fly` (line 69), and `Pec Deck` (line 76) all have `substitution_class = null` (no later migration overrides them). The `byPattern` freshness term only fires on non-null classes (`generation.ts:671-673`: `a.substitution_class !== null && usedSubstitutionClasses.has(...)`), so the three flies are treated as unrelated and the cross-session dedup never sees them as equivalent.
**Symptom:** two near-identical chest fly variations can co-occur in one routine.
**Fix:** `UPDATE exercises SET substitution_class = 'chest_fly' WHERE name IN ('Cable Fly','Chest Fly','Pec Deck') AND user_id IS NULL AND substitution_class IS NULL;` (exact names verified in the seed).
**Test stub:** after the migration, generate a Gym routine with a `chest_iso` slot on two variant sessions; assert A and B do not both contain a `chest_fly`-class exercise.
**Verified:** confirmed, all three are null-class; freshness term is exactly at `generation.ts:671-673`.

#### 2.2 Leg curl / leg extension family have no `substitution_class`

**Status:** SHIPPED 2026-06-11 (in the Group 2 migration above)
**Track:** Generation quality
**Prerequisites:** 1.2 (the three-times-demanded hinge is the upstream amplifier)
**Decision needed:** None

**Root cause:** `Leg Curl` (seed line 93), `Leg Extension` (line 94), and `Dumbbell Leg Curl (Lying)` (line 89) all have `substitution_class = null`. Patterns/compound flags (from `2026-06-06-10-51-33-movement-pattern-correction.sql`): `Leg Curl` -> `hinge`, `is_compound = false` (`:47-48`); `Dumbbell Leg Curl (Lying)` -> `hinge`, `false` (set in `2026-06-04-exercise-equipment-correction.sql:233-235`); `Leg Extension` -> stays `squat`, `is_compound = false` (`:52-53`). Because their class is null, the `byPattern` freshness term never deprioritizes them. With hinge demanded 3 times (1.2), once the real hinge compounds (RDL/Hip Thrust, class `hinge_pattern`/`glute_pattern`) are consumed elsewhere, the null-class leg curl reads as "fresh" and **is selected to fill the hinge slot** on the remaining leg session.
**Symptom (corrected):** the task brief said "Leg Curl appears as the **first** exercise on Legs day." That is inaccurate: `patternTier(hinge, isCompound=false)` returns tier 4 (`generation.ts:549-562`), so a non-compound hinge is **presented last** (with calf/core), not first. The real defect is at **selection**: the session's hinge volume is filled by an isolation instead of a compound. Same for `Leg Extension` in the `squat` slot.
**Fix:** same migration as 2.1. `UPDATE exercises SET substitution_class = 'leg_curl' WHERE name IN ('Leg Curl','Dumbbell Leg Curl (Lying)') AND user_id IS NULL AND substitution_class IS NULL;` and `UPDATE exercises SET substitution_class = 'leg_extension' WHERE name = 'Leg Extension' AND user_id IS NULL AND substitution_class IS NULL;`. **Name correction:** the brief proposed `'Dumbbell Leg Curl'`, but the actual global row is `'Dumbbell Leg Curl (Lying)'` (no plain `'Dumbbell Leg Curl'`, and `'Leg Curl Machine'`/`'Leg Extension Machine'` are template-only names with no global exercise row). 1.2 is a prerequisite: the class fix reduces but does not eliminate the problem while hinge is demanded 3 times.
**Test stub:** generate `ulppl-5` with a Home profile; assert no session fills its first compound-capable slot with an `is_compound = false` exercise when a compound exists for that pattern in the usable pool. (Phrase the assertion on the selected slot, not on presentation order.)
**Verified:** null classes confirmed; patterns/compound flags confirmed; brief's exercise name and "first exercise" symptom both corrected.

#### 2.3 Smith Machine Calf Raise has wrong equipment tag

**Status:** SHIPPED 2026-06-11 (in the Group 2 migration above; re-tagged `{machines}`, no bench needed for a calf raise)
**Track:** Generation quality
**Prerequisites:** None
**Decision needed:** None

**Root cause:** `2026-06-04-exercise-equipment-correction.sql:88-97` lumps `Smith Machine Calf Raise` into the `ARRAY['barbell','bench']` bench-press group (line 96), a copy-paste error. Its `movement_pattern` was later corrected to `calf`, `is_compound = false` by `2026-06-06-10-51-33-movement-pattern-correction.sql:31-32`, but the equipment was never re-tagged. So pattern is right, equipment is wrong.
**Symptom:** invisible to users but silently narrows the calf pool, the exercise is unavailable to Gym users who own machines but no barbell.
**Fix:** same migration as 2.1/2.2. `UPDATE exercises SET equipment = ARRAY['machines'] WHERE name = 'Smith Machine Calf Raise' AND user_id IS NULL;`
**Test stub:** assert `Smith Machine Calf Raise` is available in any equipment pool that includes `machines`.
**Verified:** confirmed, equipment is `{barbell,bench}`; movement_pattern was corrected by a prior migration but equipment was not (exactly as the brief stated).

### Subsection 3: generation quality, ranking guard

#### 3.1 Isolation exercises can win a primary compound slot when the pool is thin

**Status:** SHIPPED #138 (2026-06-13). Spec `docs/superpowers/specs/2026-06-13-19-12-33-compound-first-selection-guard-design.md`. A Phase 1 verification (prompted by a reviewer challenge sourced from the stale 2026-06-10 audit) stood in for the loop: confirmed `COMPOUND_FLOOR` exists and protects the session level, the audit's knee+lower_back "all-isolation" claim does NOT reproduce (safe compounds are left untagged in the seed + the floor backstops), and 3.1 is a within-pattern tiebreaker for the two mixed patterns (`squat`/`hinge`) only. The `is_compound` term sits after the canonical-anchor rank, before the fatigue tiebreak; goldens unchanged (1379) + 1 regression. NOT the same item as Bug 2.
**Track:** Generation quality
**Prerequisites:** 1.2 and 2.2 (they reduce how often this fallback is reached); both shipped as of 2026-06-11.
**Decision needed:** Exact sort placement and golden-test handling (see below). This is **not** purely additive.

**Root cause:** the `byPattern` comparator (`generation.ts:654-682`) never considers `is_compound`: its terms are demote, loading-lean, substitution-class freshness, front-delt suppression, fatigue, id. So within a pattern an isolation can sort above a compound (on freshness or fatigue) and be **selected** for a compound-pattern slot. `patternTier` (`:549-562`) only reorders **presentation** after selection, so it cannot stop an isolation from occupying the slot.
**Symptom:** under pool pressure (thin equipment, restrictions, or a pattern demanded more times than it has compounds) an isolation fills the session's main compound slot. The brief's "machine leg curl becomes the **primary movement**" is a selection problem, not a display one: `patternTier` already presents non-compound hinge/squat among the tier-4 finishers.
**Rejected alternative (2026-06-10):** making the lower-day 6th slot a designed squat/hinge repeat that seats Leg Extension/Leg Curl was NOT taken; the max-2-per-pattern cap (Bug 4/7) plus the quad/posterior split keep the lower day diverse instead.
**Fix:** add an `is_compound` (compound-first) term to `byPattern`, placed **after** the front-delt-isolation term (`generation.ts:674-676`) and **before** the fatigue tiebreak (`:677-679`), so user-intent signals (loading lean, freshness) still win and fatigue stays the tiebreak. The brief's "only when filling the session's first compound-capable slot" is hard to express in the stateless per-pattern comparator; a simpler equivalent is a global compound-first term (a no-op for pure-isolation patterns, where all candidates share `is_compound = false`).
**Spec findings (2026-06-13, corrects the two flags below):** (a) **Measured byte-identical**: adding the `is_compound` term to `byPattern` (after front-delt, before fatigue) leaves all 1379 goldens green, the earlier "not byte-identical" worry was wrong. (b) **Blast radius is narrow**: a full catalog audit found patterns are compound/isolation-segregated EXCEPT `hinge` and `squat`, which hold `Leg Curl` / `Leg Extension` (`is_compound=false`) because no `quad_iso`/`hamstring_iso` pattern exists. So the term is live only for those two patterns, and only in a thin pool where no *named* anchor compound survives (canonical rank already protects named compounds, which is why goldens don't move). (c) **Root cause**: the durable fix is adding `quad_iso`/`hamstring_iso` patterns (separate, larger item); the `is_compound` guard is a cheap, safe rail. See the spec for the placement, test plan, and review-loop questions.
**Test stub:** fixture with one compound and one isolation for the same pattern; assert the compound is selected for the session's first compound-capable slot, and the isolation can still be selected for a subsequent same-pattern slot.
**Verified:** confirmed, no compound-first guard exists in `byPattern`/`pick`; added presentation-vs-selection nuance and the golden-test caveat.

### Subsection 4: setup flow bugs, all already shipped

Every finding here was implemented before this audit. Kept for the record with verification. **No action.**

#### 4.1 First session lands on wrong training day

**Status:** Already shipped (verified in live code)
**Track:** Setup flow
**Prerequisites:** n/a
**Decision needed:** Resolved (default anchor = today's weekday)

**Root cause (historical):** an ascending weekday sort pinned session A to the lowest weekday number.
**Fix as shipped:** `orderTrainingDays(trainingDays, anchorDow)` exists and is exported (`generation.ts:938-940`) with the exact modular sort `((a-anchorDow+7)%7) - ((b-anchorDow+7)%7)`; `GenerationInput.anchorDow?: number` exists (`:904`); `generateRoutine` uses `orderTrainingDays(trainingDays, input.anchorDow ?? 1)` (`:948`). `cloneTemplate` derives `anchorDow` from `startAnchor` and calls `orderTrainingDays` (`routines.ts:379-380`); `generateAndSaveRoutine` derives `anchorDow` (`:452`) and passes it (`:546`).
**Tests:** already present, `describe('orderTrainingDays')` and `describe('generateRoutine anchor-aware schedule')` (`__tests__/generation.test.ts:451-512`), including the Wed-start regression.
**Verified:** **DISCREPANCY, already fixed.** The brief assumed `anchorDow` was not yet a parameter; it is, with tests.

#### 4.2 Setup flow auto-opens after deleting the last routine

**Status:** Already shipped (verified in live code)
**Track:** Setup flow

**Fix as shipped:** `PulseProvider.tsx` has `const hadRoutines = useRef(false)` set true once `routines.length > 0` (`:286-289`), and `showOnboarding = onboardingOverride ?? (!loadingRoutines && routines.length === 0 && !hadRoutines.current)` (`:292`), exactly the proposed guard. The known reload-resets-the-ref limitation still applies.
**Verified:** **DISCREPANCY, already fixed** (file path `PulseProvider.tsx` was correct in the brief).

#### 4.3 No way to close the setup flow mid-way

**Status:** Already shipped (verified in live code)
**Track:** Setup flow

**Fix as shipped:** the `FlowFrame` wrapper (`RoutineSetupFlow.tsx:117-166`) renders a persistent close (✕) in the header on every step (`:151-156`), an Escape handler (`:370-378`), and a "discard while dirty" confirm (`requestClose`, `:366-369`). Full-screen layout via `SCREEN = 'fixed inset-0 z-50 ...'` (`:100`).
**Verified:** **DISCREPANCY, already fixed.**

#### 4.4 Modal card resizes between steps

**Status:** Already shipped (verified in live code)
**Track:** Setup flow

**Fix as shipped:** `FlowFrame` uses fixed chrome, `h-14` header (`shrink-0`), `flex-1 overflow-y-auto` body, `shrink-0` footer (`RoutineSetupFlow.tsx:134-163`); the code comment (`:98-99`) calls out that this replaced the resizing centered card.
**Verified:** **DISCREPANCY, already fixed** (same `FlowFrame` work as 4.3).

#### 4.5 Start date step shows labels without actual dates

**Status:** Already shipped (verified in live code)
**Track:** Setup flow

**Fix as shipped:** each start option carries a resolved date via `sub={START_DATE_FMT.format(startDateFor(...))}` (`RoutineSetupFlow.tsx:1110-1130`); "Pick a date" calls `dateRef.current?.showPicker?.()` (`:1137`) on a visually hidden `<input type="date">` (`:1141-1156`); "Next Monday" is strictly next via `(1 - d.getDay() + 7) % 7 || 7` (`:94`).
**Minor delta:** `showPicker()` is called directly in the click handler, not wrapped in `requestAnimationFrame` as the brief sketched; substance is equivalent.
**Verified:** **DISCREPANCY, already fixed.**

#### 4.6 Exercise numbering inconsistent between Library and Plan

**Status:** Already shipped (verified in live code)
**Track:** Setup flow

**Fix as shipped:** `RoutineExerciseRow` takes a `displayNumber?: number` and renders `{displayNumber ?? index + 1}` for the badge while `onMove(index, ...)` keeps the global index for reorder (`views/library/RoutineExerciseRow.tsx:25-29,78,102,109`). `RoutinesTab` passes a per-session counter as `displayNumber` (`views/library/RoutinesTab.tsx:216,245,378`) while keeping the global `index`. Plan numbers per section (`views/ProgramView.tsx:296`).
**Path correction:** the files are at `src/components/pulse/views/library/RoutinesTab.tsx` and `.../RoutineExerciseRow.tsx`, not `src/components/pulse/` as the brief stated.
**Verified:** **DISCREPANCY, already fixed**, plus a file-path correction.

### Generation engine quality track (post-bug-fix backlog)

Five confirmed generation-engine quality issues from systematic testing plus a three-way review (internal audit + ChatGPT + Perplexity), opened 2026-06-10 after the generation bug-fix work (#115 + the anchor/unilateral follow-ups). Confirmed against the live tree, not speculative. Distinct from subsections 1-3 above (those are the `EMPHASES` / metadata / ranking data fixes); this track is the next layer found after them. Supporting detail: `docs/audits/2026-06-10-21-30-25-generation-engine-input-coverage-audit.md`.

**Status (2026-06-11):** Items 1, 2, 3, and 4 shipped (merged #117); Issue 0 shipped (merged #118); Item 5 shipped (merged #119). **The track is closed.**

#### Issue 0, day-picker redesign (exact weekly frequency)

**Status:** SHIPPED 2026-06-11, merged #118. Spec: `docs/superpowers/specs/2026-06-11-08-27-32-day-picker-redesign-design.md` (review decisions in its Section 10). Detail in Shipped.
**Problem:** the days-per-week answer is a coarse three-bucket selector (`'2-3' | '4' | '5-6'`) and the quick flow (the only live entry point) seeds `SUGGESTED_DAYS['5-6']` = 5 days, so `STYLES[6]` (`ppl-x2-6`) is unreachable and Item 5 cannot be tested end-to-end. The `'2-3'` bucket always seeds 2 days, so a 3-day quick path does not exist either.
**Fix:** replace the bucket union with an exact numeric `WeeklyFrequency` (`2 | 3 | 4 | 5 | 6`) in a new `src/lib/pulse/weeklyFrequency.ts` (type, options, runtime guard, rekeyed `SUGGESTED_DAYS` / `MAX_TRAINING_DAYS`); the quick flow's days step becomes a five-option exact selector that seeds `SUGGESTED_DAYS[n]` (6 -> Mon-Sat). Keys 2/4/5 stay byte-identical; 3 (`[1,3,5]`) and 6 are new paths. `recommendTemplate` rekeys; `recommendStyle` / `STYLES` / the engine are untouched; no migration (`days` is transient).

#### Item 1, Powerbuilding pull patterns: set/rep mismatch

**Status:** SHIPPED 2026-06-10 (`feature/generation-engine-quality-track`). `horizontal_pull` + `vertical_pull` added to `POWERBUILDING_HEAVY_PATTERNS`; the accessory unit test updated, +2 tests (heavy-pull unit + a PPL-pull end-to-end asserting 3-6 on the primary row). The "conservative pulls" variant remains a future per-pattern-bias-override option, not built.
**Problem:** `POWERBUILDING_HEAVY_PATTERNS` (squat / hinge / horizontal_push / vertical_push) excludes `horizontal_pull` and `vertical_pull`. Under powerbuilding a pull session gets the hypertrophy range (8-12) on every compound, but because `effectiveBias` resolved to `strength` the first compound still takes the strength +1 set bump. Net: the main row reads 4 sets × 8-12 and the back is never trained heavy under a style marketed as powerbuilding. An internal inconsistency, not a deliberate design choice.
**Fix:** add `horizontal_pull` and `vertical_pull` to `POWERBUILDING_HEAVY_PATTERNS`. Update the `repRange` / `resolveRepRange` unit tests and add an end-to-end test asserting a powerbuilding pull session produces 3-6 reps on the primary row compound.
**Note:** a "conservative pulls" variant (pulling stays moderate, 6-10, to manage joint stress) is a legitimate coaching philosophy, but it should be an explicit future option, not the silent default. Defer until the training-style system supports per-pattern bias overrides.
**Sequencing:** no dependencies; can ship in the next commit.

#### Item 2, Minimum-compound guard for restriction-emptied sessions

**Status:** SHIPPED 2026-06-10 (`feature/generation-engine-quality-track`). After `selectForSession`, a session with zero compounds seats one safe compound from any pattern (cross-pattern fallback, dropping the lowest-priority isolation so the count holds); if no compound survives anywhere in the safe pool, a non-blocking `RoutineBlueprint.warnings` notice is appended to the routine rationale (the "why this plan" copy). Never hard-rejects. Code-reviewed by subagent (no blocking findings). +3 tests. The warning surfaces via the rationale rather than a transient panel toast (zero ripple to the generate return contract, and persistent). The intent-matched substitution (a safe posterior compound for an emptied hinge, a safe quad for an emptied squat) remains the v1.5 follow-on below.
**Problem:** the restriction filter is purely subtractive with no floor. When combined restrictions empty a session's compound pool (e.g. knee + lower_back on a posterior lower day removes both the hinge and the lunge compounds), the engine silently generates an all-isolation session. A lower day of glute_iso + calf + core is a failure state, not a valid training session. knee + lower_back is common in real users and is the highest-severity untested combination in the current engine. (Generalizes the existing `lower_post` minimum-compound follow-up in the Status block, which covers only the equipment-emptied hinge case, to the restriction-driven case across all session types.)
**Fix:** after equipment + restriction filtering, before session construction, check the available compound count per session type. If compounds fall below a floor of 1 for any session, run a fallback search across all patterns in the safe pool (not just the session's emphasis patterns) for any available compound exercise; insert the first one found as the leading slot before backfill. If no compound exists after exhaustive search, generate the session anyway but surface a clear generation-time warning: "Your movement restrictions removed all compound options for one or more sessions. These sessions use accessory work only. Consider adjusting your restrictions or equipment." Do not hard-reject generation, an informed accessory-only session beats nothing.
**Dependencies:** uses existing `substitution_class` metadata (already seeded). Does not require the full substitution logic system.
**Follow-on:** a fuller pattern-aware fallback ranked by movement intent (a safe posterior compound when hinge is out, a safe quad compound when squat is out) is the next restrictions enhancement after this guard ships. Spec it separately as "movement restrictions v1.5, substitution logic."
**Sequencing:** no hard dependencies; can be implemented alongside Item 1.

#### Item 3, Remove `fb-emphasis-3` from the 3-day split options

**Status:** SHIPPED 2026-06-10 (`feature/generation-engine-quality-track`). `fb-emphasis-3` removed from `STYLES[3]` (now `fb-3` / `ppl-3` / `ulf-3`); the three orphaned emphases `fb_chest_back` / `fb_legs` / `fb_delts_arms` are kept as `EMPHASES` Record keys (removing them is a coupled `EmphasisKey` type change, and they are earmarked for the future replacement style). +1 test. The true "full-body with emphasis" replacement remains unbuilt (Future, below).
**Problem:** the 3-day style "Full Body - Emphasis Days" (`fb-emphasis-3`) is a body-part split that trains each muscle group roughly once per week: `fb_chest_back` (zero leg work), `fb_legs` (zero upper work), `fb_delts_arms` (minimal compound work). It appears alongside genuinely full-body 3-day options under a "Full Body" label; a user choosing it for full-body frequency gets the opposite, the lowest-frequency option in the catalog and inferior to every other 3-day option for the target user (intermediate lifter, hypertrophy + general strength).
**Fix:** remove `fb-emphasis-3` from `STYLES[3]`. The 3-day picker then offers three sound options: `fb-3` (full body, 3x frequency, recommended default), `ulf-3` (upper/lower/full body, ~1.5x frequency), and `ppl-3` (push/pull/legs, 1x frequency, clearly positioned for users who want specialization and understand the tradeoff).
**Future:** a true "full-body with emphasis" 3-day style, where each session trains all regions but one is prioritized, is the right long-term replacement for `fb-emphasis-3`. Spec it before building: every session must include at least one lower compound, one upper-push compound, and one upper-pull compound, with one pattern group front-loaded as the day's emphasis.
**Sequencing:** no dependencies. Remove now, spec the replacement separately.

#### Item 4, Squat/hinge adjacency: interim fix and long-term role model

**Status:** SHIPPED 2026-06-10 (`feature/generation-engine-quality-track`). Landed in two steps: first the interim `interleaveLowerCompounds` pass, then the **full exercise-role model** (`assignRole` / `orderByRole`) that replaced it and the old tier sort (the role model below is now built, not just the interim). +14 tests net (3 interim, re-homed; 8 role-sequence; 3 `assignRole` unit), code-reviewed by subagent twice (no blocking findings).
**Problem:** the tier sort assigns both squat and hinge to Tier 1, guaranteeing they always appear as exercises 1 and 2 in any session containing both. Every full-body session, combined leg day, and PPL legs session opens with two maximally fatiguing lifts back to back, the single biggest coach-quality issue in the output. Established programming convention (5/3/1, GZCLP, most general-population templates) separates heavy lower compounds with an upper compound between them on combined sessions, or places squat first and hinge later as a secondary lift on dedicated leg days.
**Interim fix:** after the tier sort, run a post-sort pass. If exercises 1 and 2 are both Tier 1 lower-body compounds (squat and hinge patterns) and the session contains at least one Tier 2 upper compound, insert the first Tier 2 upper compound between them (squat → bench → RDL → row instead of squat → RDL → bench → row). On dedicated leg sessions with no Tier 2 upper compound to interleave (`lower_post`, `lower_quad`, PPL legs day) the adjacency is acceptable and no reorder fires, squat-then-hinge on a leg-only day is standard programming.
**"Generation engine, exercise role model (architectural)", SHIPPED 2026-06-10** (spec `docs/superpowers/specs/2026-06-10-22-41-05-exercise-role-model-design.md`, reviewed + amended via Q1/Q2/Q3, then implemented as `assignRole` / `orderByRole`): replaced the pattern-tier model with an exercise-role layer, Primary Lower → Primary Upper → Secondary Lower → Secondary Upper → Isolation → Finisher. This encodes how coaches think about session structure, naturally separates heavy lower and upper compounds, and provides a clean hook for compound-floor validation (Item 2), rep-scheme assignment by role, and future volume-first generation. It is the same architectural conclusion reached independently by both external reviewers. Spec-first: do not build without a written spec defining role assignment rules for all 15 movement patterns and all session types. It gates the volume-first generation planner (Phase 4 in "Routine generation v2, engine direction" above; the source brief referenced it as Phase 3). The role-model spec should be written before that planner work begins.
**Sequencing:** the interim adjacency fix can ship with Items 1-3; the role-model spec is written before Phase 4 work begins.

#### Item 5, `ppl-x2-6` A/B session differentiation

**Status:** SHIPPED 2026-06-11, merged #119. Spec: `docs/superpowers/specs/2026-06-11-09-40-57-ppl-x2-6-differentiation-design.md` (review decisions in its Section 9). Detail in Shipped.
**Problem:** the 6-day PPL×2 style (`ppl-x2-6`) uses identical emphasis definitions for both push days, both pull days, and both leg days. Both leg days use the same slot list (squat, hinge, lunge, glute_iso, calf, core) with no quad/posterior differentiation and no heavy/volume contrast. Under `consistent` variety both leg days share the same squat and hinge anchors, producing near-identical sessions across the week, the 6-day equivalent of the cross-focus collapse already fixed in `ulppl-5`.
**Planned fix (spec-first):** apply the quad/posterior split `ulppl-5` already uses to the 6-day leg days, and add heavy/volume contrast to push and pull:
- Leg A: squat + lunge led (quad).
- Leg B: hinge + glute_iso led (posterior).
- Push A: bench-led, strength bias. Push B: OHP-led, hypertrophy or balanced bias.
- Pull A: horizontal-pull-compound emphasis, strength bias. Pull B: vertical-pull emphasis, hypertrophy or balanced bias.

The new session definitions (six emphasis definitions, the updated `STYLES[6]` entry, updated focus strings) must be written as a spec and reviewed before implementation; each new emphasis definition requires a passing golden test.
**Sequencing:** write the spec after Issue 0 ships (so the 6-day path is testable end-to-end); implement alongside or just after Issue 0.

---

## Reference & archive

_Dated market/strategy context and the full Shipped history. Everything forward-looking is above this line._

---

## Competitive analysis (2026-06-03)

Reviewed 8 trackers: Hevy, Strong, Fitbod, Jefit, Boostcamp, Alpha Progression, Caliber, Setgraph. _Historical reference, all five "biggest gaps" below have since shipped; kept for the differentiation framing._

Biggest gaps versus the field:

- Live PR detection. Every serious tracker (Hevy, Boostcamp, Caliber, Strong) flags a personal record the moment you hit it. Pulse only surfaces PRs after the workout. Pulse already computes E1RM, so this is a small add with a big motivation payoff.
- Per-muscle weekly volume. Hevy, Fitbod, Jefit, Boostcamp, and Strong all show sets per muscle group per week, often as a body-diagram heat map. Pulse has a 10-category taxonomy and a volume-by-type chart already, so it can derive this from exercise tags.
- Plate calculator. Hevy, Strong, Boostcamp, Setgraph, and Caliber all ship one. It is a pure function with near-zero backend cost and removes real friction at the rack.
- Rich set types. Drop sets and failure sets are standard in Hevy, Strong, and Boostcamp. Pulse only has warmup and working sets.
- Mid-workout exercise swap. Boostcamp, Fitbod, Alpha, and Caliber let you swap a busy machine for a similar exercise and carry your weights across. High friction reducer for real gyms.

Differentiation opportunities:

- Recovery-aware volume nudges. Combine per-muscle volume with the existing RIR data to flag under- and over-trained muscles. Fitbod and Boostcamp do versions of this, but none pair it tightly with a fixed 12-week progressive-overload plan the way Pulse can.
- A single Strength Score. Caliber and Boostcamp prove a 0-100 composite metric drives engagement. Pulse already has E1RM PRs to compute it from, and it gives non-experts a legible headline number.
- Stay private and fast. Strong and Setgraph win on being distraction-free with no social noise. Pulse can lean into solo progressive overload as a positioning choice and skip the heavy social race that needs user mass.

---

## Strategic review (2026-06-06)

Two external product reviews (ChatGPT), cross-checked against the code. Net: the engine and feature depth are already competitive. The next work is deepening the adaptive and personalization moat and sharpening positioning, not adding generic tracker features.

**Validated direction:** completion-paced progression, ramp-back, auto-deload, adherence awareness, and staying private/fast are the right bets. The reviews' "do not build" lists (AI chatbot, social, leaderboards, wearables, native, fatigue modeling, goal prediction) match the Deprioritized / Later sections.

**Already shipped (reviews listed these as "future", do not rebuild):** exercise dislikes / hide (`user_exercise_preferences`, generation filters them out); same-movement-pattern swap with weight carryover; generation `rationale` ("why this plan"); gender-agnostic split selection with a `priority_muscle` tilt (the reviews' "use priorities, not gender" mistake is the exact thing Pulse already fixed); schedule-to-split mapping by day count (`STYLES`, `recommendStyle`); movement-pattern slots filled by equipment (`EMPHASES`). One review explicitly called the movement-pattern-slot design a strength.

**Taken into the plan:** lead with the one-line promise + cross-device / home-gym wedge; close the personalization gap in generation (training style, variety, multi-priority, restrictions); surface the adaptation decisions; build behavior-driven adaptation as the pre-AI personalization step.

---

## Shipped

- **Library redesign, Plan A (Exercises tab)** (2026-06-14, `feature/library-redesign`, merged #142): the Exercises tab rebuilt into a findable catalog. `LibraryView` is two solid `SegmentedTabs` (Exercises / Routines; Templates folded into a future Routines New-routine chooser, Plan B, with `TemplatesTab.tsx` left unimported). The Exercises tab (`views/library/`): free-text search (name / category / equipment), an `ExerciseFilterControl` (popover desktop / `ModalSheet` mobile: Favorites / Fits-my-gear via `hasEquipment` over the travel-aware effective set / Respects-my-restrictions via `isContraindicated` / Show-hidden), a Muscle / Equipment / Type **Group-by** (persisted via `useLocalStorage`), a grouped list (favorites pinned, desktop 2-col grid incl. the filtered view), `ExerciseRow` (whole-card click target, favorite star, human equipment labels), `ExerciseDetailSheet` (metadata badges, Targets, numbered How-to with a CLS skeleton, "Similar exercises" via `rankSubstitutes`, favorite / hide / edit) and `ExerciseFormSheet` (add / edit; reps as a from/to range with a freeform fallback; an opt-in "use in auto-generated routines" toggle capturing `movement_pattern` / `equipment` / `is_compound`; editable category with a history-aware confirm). Pure logic in `src/lib/pulse/library.ts` (`filterExercises` / `groupExercises` / `parseRepRange`+`composeRepRange` / `floatFavorites`). **Favorites:** `ExercisePreference` widened to `'hidden' | 'favorite'` (mutually exclusive, one row per `(user, exercise)`; migration `2026-06-14-12-00-00-exercise-preferences-favorite.sql` applied), loaded as `{ hidden, favorite }` via `usePreferences` (`favoriteExerciseIds` + `toggleFavorite`), floated in `ExerciseSwapPicker` + `AddRoutineExerciseForm`; `create/updateExercise` extended for category + generation metadata. **Shared `ModalSheet` (app-wide):** centered on tablet+desktop (`md:`), bottom sheet only on phones, and the grip drag-to-dismiss now works with mouse as well as touch. Built subagent-driven (Group 1 data layer, Group 2 UI), each group code-reviewed, spec reconciled via the Claude.ai + Perplexity loop, iterated against the running app, final review clean. Suite **1548**, typecheck + lint clean. **Deferred follow-ons:** Plan B (Routines tab restyle + Templates fold-in, its own spec/plan); migrate the Train / guided / Plan how-to off the bespoke `ExerciseInstructionModal` onto `ModalSheet`; three minor review nits + an `EQUIPMENT_LABELS` completeness audit. Spec `docs/superpowers/specs/2026-06-14-11-00-00-library-redesign-design.md`; plan `docs/superpowers/plans/2026-06-14-library-redesign-plan-a-exercises.md`.
- **Bug 6, per-session focus labels** (2026-06-14, `feature/ulppl-session-labels`, merged #141): a nullable `routine_schedule.label`, populated at generation from the session emphasis via pure `focusLabelForEmphasis` (quad emphases `lower_quad` / `lower_lean` → "Lower (Quads)", posterior `lower_post` → "Lower (Hamstrings & Glutes)", null otherwise incl. PHUL lowers). Disambiguates the two paired leg days on the five styles that pair these emphases (ul-classic-4, ul-aesthetic-4, ulppl-5, fb-ul-hybrid-5, ppl-x2-6). Threaded through `ROUTINES_SELECT`, resolved at render by pure `sessionFocusLabel(schedule, type, variant)`, shown on the spacious surfaces only (`ProgramView` Plan accordion + next-session preview; the desktop `WorkoutModeScreen` subtitle via a `focusLabel` prop computed in `LogView`); tabs and the mobile guided VARIANT chip keep their compact type+variant labels. Forward-only (existing routines render unchanged until regenerated; no backfill). Wording/placement/scope confirmed via AskUserQuestion; code-reviewed (no runtime bugs; the review added the fb-ul-hybrid-5 + guided-subtitle tests). +18 tests, suite **1447**, typecheck + lint clean. Migration `docs/migrations/2026-06-14-10-30-00-routine-schedule-label.sql` (additive nullable column) applied 2026-06-14. Full detail in CLAUDE.md "A-D variants".
- **Plan page redesign** (2026-06-13, `feature/plan-page-redesign`, merged #140): `ProgramView` rebuilt to a new IA that synthesizes the page-depth Plan backlog into one redesign. A program **identity card** (routine name + `formatProgramStatus` status pill + block-progress bar + RIR / next-deload / days-per-week stats + a collapsible "Why this plan" where the rationale facts stay inline and the prose collapses), the next-session preview, a this-week strip, the **Training-block arc** (`BlockArc`, fed by pure `buildBlockArc`: one bar per block week, ascending faint phase tint with the live/selected week full colour, the deload week marked, tap-to-inspect updating a caption + per-phase description, RIR/deload glossary via the shared `Why`/`explainCopy`; tracks the live week via `picked ?? currentWeek`), a **responsive `PlanSessionList`** (mobile/tablet chip selector, desktop single-open accordion; a shared exercise row with sets×reps, `exerciseReason`, equipment chips, swap + how-to; header shows `estimateSessionMinutes` + set count + focus line), and a collapsed **Program settings** group (length / start date / "Change split or days" → `GenerateRoutineButton`, new routine, old kept, no data loss). Desktop is an L4 sticky summary rail beside the scrolling sessions, kept alongside the app shell's coach rail (user-confirmed); the rail scrolls internally when taller than the viewport. **Generation duress warnings moved off the rationale prose**: the generator emits stable KEYS (`limited_variety` / `no_compound`) into a `workout_routines.warnings text[]` column (migration `2026-06-13-22-18-25-workout-routine-warnings.sql`, applied), `WARNING_COPY` holds the display copy, and a dismissible `GenerationWarningNotice` renders them (localStorage-dismissed per routine). Per-phase descriptions live in `PHASE_DESCRIPTIONS` (`explainCopy.ts`), data-accurate to the volume ramp (the review loop caught the original copy contradicting the curve). Reviewed via the Claude.ai + Perplexity loop (phase-copy-vs-data fix, warnings-column, status-mapper reuse, restructure split, arc inspection-only) + code-reviewer on the warnings + rebuild diffs; live-verified (caught the arc-week async-sync bug). Suite **1429**, typecheck clean. Spec/plan/reviewer-prompt dated `2026-06-13-21-39-19`; mockup `…-21-17-54-plan-page-redesign-v2.html`.
- **Progress richness + modal consistency** (2026-06-12, merged #130 on 2026-06-13): completes the progress-richness scope. **Modal consistency pass:** all six Pulse modals unified on a shared `ModalSheet` shell (single source of modal spacing, p-6 edges, standardized header with optional back chevron + subtitle), counts on every list modal (entries / lifts / workouts / milestones / per-exercise sets), back-navigation from `SessionDetailModal` / `ExerciseDetailModal` to the list modal they were opened from (close still fully dismisses; direct opens show no chevron), and the Body-tab Log CTAs flipped to the accent fill. **Recovery readout:** the Overview tile's "N flags" replaced by pure `recoveryReadout` (worst-meaningful-state: Fresh / Ready / Watch / Ease off / No data) rendered by `RecoveryTile` with a status dot (new `--color-pulse-warn` amber token); "under volume" no longer reads as an alarm, only real fatigue goes amber/red. **Strength trend:** pure `computeStrengthScoreSeries` (cumulative best e1RM per week, bodyweight deliberately held current to isolate lifting progress) + `strengthDeltaLabel` drive a tile delta ("▲ N this cycle") and a Score-trend line in `StrengthBreakdownModal`. **Milestones:** new pure `src/lib/pulse/milestones.ts` with four detectors, PR (the SAME `calcE1RM`-per-`routineExerciseId` rule and key as `computePRMap`/`isSetPR`, parity test-locked; baseline-skipped so a routine regenerate never fires a false PR), streak records (computeStreak's program-week bucketing, emit once per all-time record, undated records omitted never epoch-dated), week-completed (via new `completedWeekBoundaries` in `adherence.ts` reusing `matchSlot`, block-wrapped titles with a "· Cycle N" suffix past cycle 1), and every-10-sessions; fully backfilled on load (derived, nothing persisted), surfaced as `MilestonesCard` (top-4 + month-grouped Show-all `ModalSheet`). Spec went through the external review loop twice (6 spec findings + 4 plan findings amended pre-build, incl. the one-PR-definition reconciliation and the 1970-epoch streak-dating fix). Subagent-driven build, per-piece spec + quality review, verified live in the browser against real data. No migration, no server-action change, engine untouched. +27 tests, suite **1313**, typecheck + lint clean. Spec: `docs/superpowers/specs/2026-06-12-22-01-15-progress-richness-design.md`; plan: `docs/superpowers/plans/2026-06-12-progress-richness.md`.
- **Progress page redesign** (2026-06-12, `feature/progress-page-redesign`): completes the Progress side of the Profile+Progress redesign, the mockup-fidelity visual pass (#129 did Profile only) plus the surfaces the IA round designed but #128 never built. Six reviewable commits, subagent-driven with per-task adversarial review. Delivered: a shared month-grouped `MetricHistoryModal` (bottom-sheet mobile / centered desktop, optional `format` for weight+length units) behind every Body "Show all"; **Best Lifts + Personal Records merged** into one card (top set + e1RM, grouped by workout type, standalone PR card removed); a `SessionsCalendar` + `SessionDetailModal` (pure `sessionsByDay` / `localDateKey` / `buildMonthCells`; sessions placed by `started_at`; week-keyed rows carry the per-exercise breakdown, calendar-day clicks are header-only in v1 since set logs are not client-linked to a session id); a lean-glance **Overview** (metric strip with the real 0-100 strength score tappable to `StrengthBreakdownModal`, `ProgramStatusCard` from pure `formatProgramStatus` over `computeProgramPosition`, block-relative via `weekInBlock` so cycle 2 reads "Week 1 of 12", recomp verdict, `CoachActivityTimeline` mirroring `CoachPanel`'s wiring); a Body Goal-weight-50% + `RecentChangeCard` summary row (top on mobile) with aligned Body-weight/Measurements columns; chart value readouts + per-muscle volume card + full restyle to the Profile token vocabulary. `workoutSessions` now exposed on `PulseContext`. Two adversarial reviews caught and fixed a tz today-ring bug, a latent test-fixture gap, the block-repeat week label, and a hardcoded-kg coach overlay. Suite **1273** green, typecheck clean, **no migration, no server-action change**. Spec `docs/superpowers/specs/2026-06-12-15-39-42-progress-page-redesign-design.md`, plan `docs/superpowers/plans/2026-06-12-progress-page-redesign.md`, mockup `docs/superpowers/designs/2026-06-12-15-39-42-progress-redesign-mockup.html`. **Open follow-ons:** app-shell nav icon pass; write-side measurement parity; expanded metrics; progress photos (#9); calendar-click per-exercise breakdown (needs set-log session linkage).
- **Self-serve auth lifecycle** (launch floor; 2026-06-11, `feature/auth-self-serve`): the login-only app gains the full get-in lifecycle so other people can sign up without hand-created Supabase accounts. New routes, all mirroring the login page via a shared `AuthShell` + `authStyles` + `PasswordFields` + `AuthSubmitButton`: `/pulse/signup` (email + password, a `useActionState` "check your email" state that holds the address for resend, no enumeration), `/pulse/forgot-password` (always the same neutral confirmation), `/pulse/reset-password` (session-gated new-password form), `/pulse/auth/confirm` (Route Handler verifying the email `token_hash` via `verifyOtp` for signup + recovery, the SSR-correct flow; validated `next`, no open redirect), and `/pulse/account-deleted`. The login page gains signup + forgot links and friendly expired/invalid-link messages. Middleware now gates on a pure `isPublicAuthPath` (own module + tests). Profile gains a change-password control and a type-DELETE-to-confirm danger zone (`AccountSecuritySection`). Account deletion is a server action: the user's own `signOut({ scope: 'global' })` then `admin.deleteUser(user.id, false)` via a `server-only` service-role client (`src/lib/supabase/admin.ts`, lazily imported so it never enters the client bundle or the test graph; the id comes only from the session, so a user can only delete themselves). The fragile lazy profile-insert in `login/actions.ts` is replaced by an `on_auth_user_created` trigger. Code-reviewed by subagent (its one Critical was a false positive about Next `redirect()` semantics, dismissed with reasoning; the rest confirmed sound). +27 tests, suite **1175**, typecheck + lint clean. **Migrations (applied to prod 2026-06-11, verified):** `2026-06-11-18-50-41-profiles-auto-create-trigger.sql` and `2026-06-11-18-50-42-fk-cascade-auth-users.sql`. The cascade migration was audited against the live schema (pg_catalog): FKs to `auth.users` already exist and are mostly `ON DELETE CASCADE`; only `profiles.id`, `set_logs.user_id`, `bodyweight_logs.user_id` were `NO ACTION`, so the migration flips exactly those three (now verified CASCADE). Required for deletion to work at all, the `profiles.id` FK would otherwise block `deleteUser`. The trigger is verified live on `auth.users`. **Remaining hand-offs (not DB):** add `SUPABASE_SERVICE_ROLE_KEY` and `NEXT_PUBLIC_SITE_URL` (canonical origin for auth email links) to the deploy env; wire a transactional email provider (Resend) SMTP + SPF/DKIM/DMARC; add the app origin to Supabase Auth's redirect allow-list; set the email templates to the `token_hash` format pointing at `/pulse/auth/confirm`. **Security-reviewed** (`/security-review`, after fixing a broken global `gpg.format` that blocked it): no exploitable vulnerabilities; two defense-in-depth fixes applied, `getOrigin` pinned to `NEXT_PUBLIC_SITE_URL` (auth email origins never trust request headers) and `validateNext` path-normalized against traversal. Billing, legal pages, and CAPTCHA are deferred. **Local verification (real app + Supabase) caught a dev-only blocker:** the strict `/pulse` CSP blocked Next's dev `eval()` bundle from hydrating, so forms did native no-JS POSTs and hit a Next 15.1 server-action `cookies()` regression (broke login + signup in dev only); fixed by allowing `'unsafe-eval'` in the dev CSP only (prod stays strict, prod was never affected for JS users). Otherwise verified PASS: middleware gating, all routes, real-backend `verifyOtp`, render, hydration, and the signup happy path. Spec: `docs/superpowers/specs/2026-06-11-17-34-21-auth-self-serve-signup-design.md`.
- **Live-test fixes: compound floor, quick-flow day grid, lower_lean bias** (2026-06-11, `feature/live-test-fixes`): three fixes from live testing a 4-day aesthetic upper/lower (advanced, build muscle, dumbbells + bench). **Issue 1 (critical):** the dumbbell-only quad day collapsed to 2 compounds + 2x calf + 2x core. Fixed twofold in `selectForSession`: a per-focus **minimum-compound floor** (`COMPOUND_FLOOR`: lower/legs/full_body 2, upper/push/pull 1) runs between the first slot pass and backfill, seating compounds from the session's own body region only (follow-up fix, same day: lower/legs/full_body search squat > hinge > lunge, upper/push/pull the four upper compounds; **never cross-region**, and the Item 2 zero-compound guard is region-restricted the same way for lower/legs, its "seats an upper compound on a restriction-emptied legs day" test rebaselined to expect honest accessory work + a warning; full_body keeps the any-region fallback) under every existing cap (pattern, heavy-dedup, unilateral, avoid-set), never relaxing or rejecting; an unsatisfiable floor surfaces `LIMITED_VARIETY_WARNING` ("Some sessions have fewer compound exercises than recommended due to your equipment or movement restriction settings.", copy upgraded in the same follow-up) via the rationale (zero-compound sessions stay with the Item 2 guard so warnings never stack). Plus a **finisher deflection** in backfill: lower-type sessions about to REPEAT a calf/core pick first try a lower-bucket pattern missing from the emphasis (the quad day reaches for a Dumbbell RDL). Deep pools never reach a finisher repeat, so golden outputs are byte-identical (the Item 5 goldens pass unmodified); the quad/posterior purity softens only under thin equipment, the trade-off chosen in review. The old `lower_post` TODO is resolved by the same floor. Note: the dictated floor of 2 alone could not fix the reported session (it already had 2 compounds); the deflection is the agreed addition, with its trigger refined from "backfill exhaustion" (which never occurs, finishers keep providing) to "before a finisher repeat". **Issue 2 (high):** the quick flow had no which-days control. Its days step is now a **combined frequency + Mon-Sun day grid** (`DayGrid`, extracted, aria-pressed): picking a frequency seeds `SUGGESTED_DAYS[n]`, the grid is adjustable and capped at the frequency, changing frequency resets the selection, and Next requires the selection to match the frequency exactly (pre-seeded, so accepting the default costs nothing). Still 6 steps; the full flow is unchanged. **Issue 3 (medium):** `lower_lean` bias pump -> hypertrophy so the aesthetic quad day's compounds train 8-12 (was 12-15, accessory-level for an advanced build-muscle user); lunge-first slot order kept and documented as deliberate (unilateral-led aesthetic character; the role model still presents a seated squat first); `lower_post` was already hypertrophy. Code-reviewed by subagent (no findings). +9 tests across both passes, suite **1112**, typecheck + lint clean, no migration.
- **P0 Group 2, exercise metadata corrections** (2026-06-11, `feature/p0-group2-seed-migration`): one idempotent apply-by-hand seed migration (`docs/migrations/2026-06-11-10-19-55-p0-group2-exercise-metadata-corrections.sql`) closing roadmap subsection 2. **2.1:** Cable Fly / Chest Fly / Pec Deck seeded as one `chest_fly` substitution class (they were null-class, so the cross-session dedup never saw them as equivalent). **2.2:** Leg Curl + Dumbbell Leg Curl (Lying) seeded `leg_curl`, Leg Extension seeded `leg_extension` (null-class isolations could read as "fresh" and fill a compound slot at selection time; the class fix feeds the freshness/dedup layers). **2.3:** Smith Machine Calf Raise re-tagged `{machines}` (a 2026-06-04 copy-paste left it `{barbell,bench}`, hiding it from machine-only gym pools). All three re-verified against the seed files before authoring; the 2.1/2.2 UPDATEs guard on `substitution_class IS NULL` so a drifted live row is left alone. Docs-only, no src change, no new tests (the engine's non-null-class behavior is already unit-locked by the GQ3 suite; the migration carries its own verification SELECT). **Apply by hand AFTER the four pending merged-feature migrations** (same `exercises` rows). Suite unchanged at **1103**, typecheck clean.
- **ppl-x2-6 A/B differentiation (Item 5)** (2026-06-11, merged #119): the 6-day PPL x2 style's near-clone A/B sessions become a designed heavy/volume week. Four new emphases (pure data, `EmphasisKey` +4): `push_heavy` / `pull_heavy` (strength bias, compounds 3-6 with the +1 set bump, the doubled isolation slot trimmed to one per Q4 since the heavy day already carries the bump) and `push_volume` / `pull_volume` (hypertrophy, doubled `triceps_iso` / `back_iso` kept, the vertical compound leads the slot list so it gets selection freshness and backfill priority; display order stays role-model-owned per Q5). `STYLES[6][0]` is now `push_heavy` / `pull_heavy` / `lower_quad` (A) then `push_volume` / `pull_volume` / `lower_post` (B), reusing the proven quad/posterior pair (share by contract with `ul-classic-4` / `ulppl-5`, commented at the definition); both leg days stay hypertrophy (Q2, avoids three consecutive heavy days). Accepted Q4 consequence, documented and test-locked: the 5-slot heavy emphases backfill their 6th pick onto the lead compound pattern at 45-60 min (a 2nd horizontal push/pull at strength reps). Existing `push` / `pull` / `legs` emphases untouched, so every other style is byte-identical, locked by three hard-coded pre-change blueprint goldens (ppl-3 / ul-classic-4 / ulppl-5). Four legacy tests premised on legs-x2 shared slots migrated to push-x2 at `~30 min` (loading-lean fresh-vs-used, GQ3 anchor fresh-wins, GQ3 substitution-class dedup, Bug 1 consistent same-focus anchor, the last strengthened to assert both push compounds' anchors). Known taxonomy artifact (out of scope, Phase 0 #2): `shoulder_iso` spans front and rear delt moves, so Front Raise can seat on pull days. Spec-gated implementation, code-reviewed by subagent (no findings). +10 tests, suite **1103**, typecheck clean, no migration. Closes the generation engine quality track. Spec: `docs/superpowers/specs/2026-06-11-09-40-57-ppl-x2-6-differentiation-design.md` (decisions in Section 9).
- **Day-picker redesign (Issue 0)** (2026-06-11, merged #118): the coarse three-bucket days-per-week answer (`DaysPerWeek` = '2-3' | '4' | '5-6') is replaced by an exact numeric **`WeeklyFrequency`** (`2 | 3 | 4 | 5 | 6`) owned by the new `src/lib/pulse/weeklyFrequency.ts` (type, `WEEKLY_FREQUENCIES`, an `isWeeklyFrequency` runtime guard used by the generate-action validation, and the rekeyed `SUGGESTED_DAYS` / `MAX_TRAINING_DAYS`, moved out of `constants.ts`). The quick flow stays exactly six steps; its days step is now a five-option exact selector, so picking 6 seeds Mon-Sat (`[1,2,3,4,5,6]`) and resolves `STYLES[6]` (`ppl-x2-6`), previously unreachable from any live path, and picking 3 seeds a real 3-day routine (the '2-3' bucket only ever seeded 2 days, so a 3-day quick path did not exist). Seeds for 2/4/5 are byte-identical to the old buckets; `MAX_TRAINING_DAYS` is the identity map (the full-flow cap is the stated frequency). `recommendTemplate` rekeyed (beginner or <=3 full-body, 4 upper-lower, else ppl; behavior-identical per count; note it has no production callers today); `recommendStyle` / `STYLES` / the engine untouched, and `generation.ts` needed zero diff (numeric `days` interpolates identically in `buildRationale`). No migration (`days` is transient, never persisted). Audit-driven plan (hidden-coupling sweep found the spec's consumer list over- and under-counted); code-reviewed by subagent (no blocking findings). +16 tests, suite **1093**, typecheck + lint clean. Unblocks Item 5 (`ppl-x2-6` A/B differentiation). Spec: `docs/superpowers/specs/2026-06-11-08-27-32-day-picker-redesign-design.md` (decisions in Section 10).
- **Generation engine, exercise role model (Item 4)** (2026-06-10, `feature/generation-engine-quality-track`): replaced the `patternTier` sort + the interim `interleaveLowerCompounds` with a role layer. `assignRole(pattern, isCompound, categoryRankWithinSession, isLoneLowerCompound)` (pure, exported) + `orderByRole` order each session **PRIMARY_LOWER → PRIMARY_UPPER → SECONDARY_LOWER → SECONDARY_UPPER → ISOLATION → FINISHER**, ranking two coarse buckets (Lower {squat, hinge, lunge} by pattern priority squat>hinge>lunge then canonical → fatigue → id; Upper {the four push/pull patterns} by canonical → fatigue → push-before-pull → id), so each session has one Primary Lower and one Primary Upper, the two heaviest compounds lead and are separated by the opposite category (squat → bench → hinge → row). Lone-lunge promotion (no squat/hinge present → the first lunge becomes Primary Lower); position 0 stays a compound so the strength set-bump lands on the primary lift. Generalizes the interim squat/hinge fix to every multi-compound shape (e.g. hinge + lunge, two presses). Resolved through the pre-implementation verification gate (Q1 lower pattern priority, Q2 upper push-before-pull tiebreaker, Q3 coarse buckets for roles vs three fine categories for the planner/reporting); spec amended + code-reviewed (no blocking findings). **Zero existing order-assertion rebaselines** (the role model preserved every invariant; the three interim tests were re-homed, comments only). Pure logic, no migration. +11 tests, suite **1077**, typecheck clean. Spec: `docs/superpowers/specs/2026-06-10-22-41-05-exercise-role-model-design.md`.
- **Generation engine quality track, Items 1 / 2 / 3 / 4-interim** (2026-06-10, on `feature/generation-engine-quality-track`): four confirmed fixes from the post-bug-fix quality track (internal audit + ChatGPT + Perplexity). **Item 1:** `horizontal_pull` + `vertical_pull` added to `POWERBUILDING_HEAVY_PATTERNS`, so a powerbuilding pull session trains its primary row/pulldown heavy (3-6) instead of getting hypertrophy reps on every lift while the first compound still took the strength +1 set bump (a 4-set x 8-12 main row). **Item 2:** a minimum-compound guard after `selectForSession`, a session whose compound patterns were all filtered out (knee + lower_back emptying squat/hinge/lunge) seats one safe compound from any other pattern (cross-pattern fallback, count held by dropping the lowest isolation) rather than shipping accessory-only; if no compound survives anywhere, a non-blocking `RoutineBlueprint.warnings` notice is appended to the routine rationale. Never hard-rejects. **Item 3:** `fb-emphasis-3` removed from `STYLES[3]` (a ~1x-frequency body-part split mislabeled "Full Body"; the 3-day picker is now `fb-3` / `ppl-3` / `ulf-3`); its three emphases are kept as `EMPHASES` Record keys for the future true full-body-with-emphasis replacement. **Item 4 interim:** a post-tier-sort `interleaveLowerCompounds` pass slots the first upper compound between squat and hinge when a session leads with both (squat, then an upper compound, then hinge), so the two highest-fatigue lifts no longer open back to back; leg-only sessions (no upper compound) and position 0 (the set-bump target) are untouched. Items 2 and 4 code-reviewed by subagent (no blocking findings). Pure logic, no migration. +9 tests, suite **1066**, typecheck clean. (Item 4's interim interleave was then replaced by the full exercise-role model, see the bullet above.) Still open in the track: only Item 5 (`ppl-x2-6` A/B, spec-first, gated on Issue 0). Track detail in "Generation engine quality track" above; supporting audit `docs/audits/2026-06-10-21-30-25-generation-engine-input-coverage-audit.md`.
- **Generation quality, EMPHASES data fixes (P0 Group 1)** (2026-06-10, on `feature/generation-emphasis-fixes`, pending merge): four pure-data edits to the `EMPHASES` table in `generation.ts`, no engine logic change. **1.1 Clean Lower A / Lower B separation:** quad days (`lower_quad`, `lower_lean`) drop `hinge`, the posterior day (`lower_post`) drops `squat`, so the `consistent` variety anchor map can no longer pin the same squat + hinge to both leg days; each compound is still trained once across the week (each lower emphasis is always paired with its opposite). `legs` / `lower_general` / `fb_legs` are deliberately left alone (single leg days, or the same emphasis used twice where identical main lifts under `consistent` is intended). **1.2** `pull` drops its `hinge` slot (no deadlift on Pull day). **1.3** `vertical_pull` added to the back-focused upper/pull days (`pull`, `upper_general`, `upper_chest_back`, `upper_aesthetic_a`); the omission was over-broad, dumbbell-only users no-op the slot at the equipment filter (backfill covers the gap, byte-identical for them), gym users get a lat pulldown. **1.4** deliberate 6th slot: `push` -> 2nd `triceps_iso` (balances chest 2 / shoulders 2 / triceps 2), `pull` -> 2nd `back_iso` (posterior-shoulder work, structural-balance deficit on a push-heavy program; chosen over a symmetric 2nd biceps via science review). The three back-focused upper emphases become 7 explicit slots; at the 6-cap (45-60 min) the trailing isolation drops for gym users (a deliberate, tested behavior). Spec reviewed by Claude.ai (science lens; lower-frequency, vertical_pull breadth, and the pull-6th `back_iso` change all from that loop) + a code-reviewer pass (no blocking findings). Pure data, **no migration**. +12 tests (leg-day separation, no-hinge-on-pull, vertical_pull presence + no-op safety, push/pull 6th-slot counts, the upper_general 7-slot drop). Suite: typecheck clean, **1038 tests**. Sets up P0 Group 2 (2.x seed migration) and P0 3.1 (compound-first ranking guard). Spec: `docs/superpowers/specs/2026-06-10-10-41-58-generation-emphasis-fixes-design.md`.
- **Smart substitution v2** (Tier 3 #8; 2026-06-09, merged #112): reason-tagged swaps + reason-aware ranked same-stimulus suggestions. A user can optionally tag **why** they swap (`pain` / `no_equipment` / `crowded`; null = preference), stored in a new nullable `exercise_swaps.reason` (CHECK-constrained). A pure **`rankSubstitutes(original, candidates, reason)`** (utils.ts) reorders the same-pattern candidates by an **explicit tiered total order so same-stimulus always wins**: Tier 1 = same `substitution_class` as the original (dominates every reason, so no reason floats a worse-stimulus lift up), Tier 2 = the reason term (preference = equipment overlap desc; no_equipment/crowded = overlap asc, prefer gear you can skip; **pain = fewest `contraindications` flags**, the catalog's gentlest option, replacing the original draft's unsafe machine/isolation guess after the science review), Tier 3 = name. `DbExercise` + `EXERCISES_SELECT` + the routine-embedded `exercise:exercises(...)` projection were enriched with `substitution_class` + `contraindications` (existing columns; additive). The shared `ExerciseSwapPicker` gained a `captureReason`-gated chip row + a "Suggested" top-3 with an `exerciseReason` why-caption (pain's "gentler on the joints" claim is gated on actual flag signal, no overclaim); **LogView persists the reason, ProgramView (permanent swap) does not**. Reason threads through `setExerciseSwap` (`reason ?? null` clears on re-swap) / `useSwaps.setSwap` / `PulseContextValue`. **#7 payoff:** `analyzeSwapBehavior` now skips constraint-reason rows (null still counts), so behavior learning demotes only from genuine preference swaps. Reviewed by an architecture lens + a training-science/UX lens (the pain-safety fix, same-class-dominates tiering, and ProgramView chip-gating all came from that loop) + a code-reviewer and adversarial second-opinion pass (the routine-embedded-enrichment + honest-pain-copy fixes applied). Migration `2026-06-09-21-54-49-exercise-swaps-reason.sql` (apply manually). +12 tests. Suite: typecheck clean, **1026 tests**. Spec: `docs/superpowers/specs/2026-06-09-21-54-49-smart-substitution-v2-design.md`; plan: `docs/superpowers/plans/2026-06-09-smart-substitution-v2.md`.

- **Behavior-driven adaptation, v1.5 swap signal** (Tier 3 #7; 2026-06-09, merged #111): generation learns from a user's repeated exercise swaps and **soft-deprioritizes (demote-only)** exercises they keep swapping away from. **Safety frame (what defines the slice):** demote-only (the trustworthy "from" signal, not the comfort-ratchet-prone "to"/promote); **non-anchor patterns only** (the six `COMPOUND_ANCHOR_PATTERNS` are never reordered, so learning away a main compound is structurally impossible); a **soft sort** (sinks within the pattern group, never empties it); **recency-bounded** (`BEHAVIOR_RECENCY_DAYS=120`) and **threshold-gated** (`BEHAVIOR_MIN_SWAPS=3`). **Correctness fix:** `routine_exercises.exercise_id` is mutable (permanent swaps overwrite it), so the swapped-`from` is now recorded at swap time in a new nullable `exercise_swaps.from_exercise_id` (captured in `setExerciseSwap`, which also refreshes `created_at` on re-swap so recency dates from the latest swap). Pure `src/lib/pulse/behavior.ts` (`analyzeSwapBehavior` -> `BehaviorSignal { demote }`, sorted/deterministic, NaN/stale-safe), loader `loadSwapHistory` (flat select, null-from filtered), a guarded leading layer in `byPattern` (`if (!anchorPattern)`), and a named, soft-worded `buildRationale` clause ("leans away from X, Y"). Derived each generation (no behavior table); `EMPTY_BEHAVIOR` fallback never blocks generation; the empty/absent path is byte-identical (golden test). **Deferred (v1.6, ideally behind reason-tagged swaps #8):** promote, skips, volume->emphasis, permanent-swap capture. Reviewed by an architecture lens + a training-science lens (reconciliation in the spec: the from-exercise fix, demote-only, and anchor protection all came from that loop) + a code-reviewer and adversarial second-opinion pass (frozen-`created_at` + hidden-name fixes applied). Migration `2026-06-09-18-51-56-exercise-swaps-from-exercise.sql` (apply manually). +11 tests. Suite: typecheck clean, **1014 tests**. Spec: `docs/superpowers/specs/2026-06-09-18-51-56-behavior-driven-adaptation-design.md`; plan: `docs/superpowers/plans/2026-06-09-behavior-driven-adaptation.md`.

- **Travel mode** (Tier 2 #322; 2026-06-09, merged #110): a temporary equipment-profile **overlay** that auto-reverts. A profile gains a nullable `expires_at`; while it is a FUTURE calendar day in the user's tz that profile is the *effective* set, past it the overlay is ignored at read time and the effective set falls back to the untouched default (`active_equipment_profile_id`). **No cron:** expiry is computed at read time via the existing DST-safe `dayIndex` (extracted into a cycle-free `dates.ts`, re-exported from `adherence.ts`); `expires_at` stores noon-UTC of the return day so the tz calendar day is stable. **One overlay per user** enforced by a partial unique index (`equipment_profiles_one_overlay_per_user`) plus an atomic clear-others-then-set in `startTravel` (the Supabase JS client can't do a per-row CASE; on a partial-set failure the user has no overlay, which is safe, and the hook's revalidate-in-finally rolls the optimistic patch back). Pure helpers in `utils.ts` (`isTravelActive` / `activeTravelProfile` / `defaultProfile` / travel-aware `resolveEquipmentPrefill` / `travelDaysLeft` / `travelReturnDate` / `travelEndedRecently` / `computeTravelExpiry`); a golden identity test pins the legacy 2-arg `resolveEquipmentPrefill` path byte-identical, and an engine-change guard asserts the overlay wins. Actions `startTravel` (uuid + future-ISO + ≤90-day + revert-target guards, ownership-checked) / `endTravel` (clears to null, also the nudge dismiss); `useEquipmentProfiles` + context wiring. UI: manager "Use until…" (3/7/14-day chips + custom date) / "End travel" / a "Create a travel set" path for single-profile users / complete badge rule, and a Train `TravelPill` with an active state (reverts in N days, tap to manage) and a post-expiry **regenerate nudge** (Regenerate via `GenerateRoutineButton` / Dismiss via `endTravel`). Generation seeds the equipment step from the active overlay (`RoutineSetupFlow` gains a `timezone` prop; `TuneYourPlanPanel` inherits via `answers.equipment`); the engine is unchanged. Reviewed by Claude.ai + Perplexity (reconciliation in the spec) and a code-reviewer pass (no blocking findings). Migration `2026-06-09-18-07-12-equipment-profile-expiry.sql` (apply manually). +23 tests. Suite: typecheck clean, **1003 tests**. Spec: `docs/superpowers/specs/2026-06-09-18-07-12-travel-mode-design.md`; plan: `docs/superpowers/plans/2026-06-09-travel-mode.md`.

- **Program pause / injury mode** (Tier 3 #14; 2026-06-09, merged #109): the pre-emptive, user-initiated half of injury-aware training (the *return* side, ramp-back + auto-deload, already shipped). An explicit "Pause program" on Train suspends a routine's program calendar; a "Resume program" closes it. While paused the status reads `paused` (a new `AdherenceStatus`), program time freezes (no `behind` / `lapsed`, no missed-week hit, the regen nudge is suppressed), and the calendar picks up where it left off on resume. **The anchor-semantics rethink resolved to a dedicated `program_pauses` table, not a new `AdjustmentKind`:** a pause is a date span (`paused_at`, nullable `resumed_at`, `resumed_at IS NULL` = active, a partial unique index allows one active pause per routine), which the week-keyed `program_adjustments` schema (unique per `effective_week`) cannot model without colliding with a same-week deload. **Two clocks, deliberately:** `behindBy` excludes scheduled sessions that fell in a paused span and `calendarWeek` subtracts paused days (program time freezes), but `daysSinceLastSession` stays the *real* gap, so resuming from a long pause still trips the existing `lapsed` to ramp-back hand-off (detraining is real; the pause protects adherence *standing*, not physiology). Pure engine in `adherence.ts` (new `activePause` / interval-union helpers threaded into `computeProgramPosition` / `computeWeekAdherence` / `computeRegenSuggestion`; `progressionInfo` needed no change, it has no calendar input). Full stack: migration + `loadPauses` + `/api/pulse/pauses` + `pauseProgram` / `resumeProgram` actions (23505-idempotent, ownership-checked) + `useProgramPauses` (optimistic) + provider/context wiring + the Train control. **No `decision_events` dual-write in v1** (a date-based pause does not dedupe in the week-keyed log; Coach Timeline pause entries + an injury/illness/travel `reason` picker, the column exists, are the v2 follow-on). +9 engine tests. Suite: typecheck clean, **980 tests**. Plan: `docs/superpowers/plans/2026-06-09-program-pause.md`.
- **Quick-start generation + post-generation "Tune your plan"** (Tier 1 #15; 2026-06-08, two branches): cuts the first-generation flow from 12-14 steps to 6 (equipment, experience, goal, days/week, session length, confirm+start) and moves personalization to a regenerate-in-place panel shown right after. **Branch 1** (`feature/profile-training-preference-editors`, merged #96): the long-deferred standing **Profile editors** for training style / variety / loading lean (a "Training preferences" group in `ProfileView`, alongside the existing restrictions editor), persist-only via `updateTrainingStyle` (now exposed through context after sitting dormant) / `updateVarietyPreference` / `updateLoadingLean` (new actions + optimistic hooks), with shared option label/desc constants in `generationPreferences.ts`. **Branch 2** (`feature/quick-start-flow-trim`): a new `mode:'quick'` on `RoutineSetupFlow` (auto-picks the split via `recommendStyle`, defaults length to 12 weeks, defers personalization to the stored profile, drops the which-days step via `SUGGESTED_DAYS`, drops gender to Profile) wired into both `OnboardingModal` and `GenerateRoutineButton`; and `TuneYourPlanPanel`, the post-generation panel that surfaces "Change split" (when the day count supports more than one style) plus all four personalization pickers seeded from the stored profile, tracks a `dirty`/`applied` snapshot so "Apply changes" stays disabled until something actually changed, and on apply **regenerates in place**: calls `generateRoutine` with the new params, re-applies the carried-over program anchor and length, then deletes the superseded routine, all before the user has logged a single set. Both hand-off sites share a `handingOffRef` guard (`RoutineSetupFlow`'s `onClose` always fires right after `onComplete` resolves, which would otherwise unmount the freshly-rendered panel) and `OnboardingModal` additionally pins itself open via `triggerOnboarding()` so the `routines.length` 0→1 SWR flip can't compute itself closed mid-handoff. **No engine change, no migration**, relying entirely on the existing `param ?? profile ?? default` resolution and the safety-aware restriction write-back. **One scope adjustment:** the panel does not reuse `CoachPanel` / `decisionCopy` as originally analyzed, no `decision_events` rows exist at generation time for it to surface, so that idea was dropped rather than faked. +15 tests across both branches (`TuneYourPlanPanel`, `GenerateRoutineButton`, `OnboardingModal`, `RoutineSetupFlow` quick-mode coverage). Suite: typecheck clean, **912 tests**.
- **Movement restrictions** (Tier 2 #5; 2026-06-08, `feature/movement-restrictions`, merged #94): the first injury-aware generation input, materially widening the addressable audience (home/gym lifters training around a cranky joint). A `RestrictionFlag` union ('knee' / 'lower_back' / 'shoulder' / 'wrist') plus a per-exercise `contraindications: RestrictionFlag[]` metadata column drive `isContraindicated`, a **hard pool filter** applied beside `hasEquipment` in `generateRoutine`. It is deliberately never relaxed (unlike the equipment thin-pool / heavy-dedup / unilateral-cap relax fallbacks): a flagged lift is never re-added to fill a slot, and if a restriction empties a movement pattern the existing GQ1 backfill covers it from the remaining safe patterns. The empty/undefined-restriction path is byte-identical to base (golden identity test across 3/4/6 days). Two migrations (hand-applied in Supabase): `profiles.movement_restrictions text[]` and `exercises.contraindications text[]` with per-exercise tags seeded against the corrected catalog (knee: Barbell Squat / Hack Squat / Bulgarian Split Squat / Walking Lunge / Leg Extension; lower_back: the deadlift/RDL/row family; shoulder: overhead barbell press / upright row / dips; wrist: straight-bar press / push-up / barbell curl), with the seeding invariant that a safe leg + push option always survives each single flag. Threaded through `generateAndSaveRoutine` (param wins over stored, then `[]`) with a **safety-aware write-back**: restrictions persist only when the param is explicitly passed, so a re-generate that omits the setup step can never silently clear a stored flag. Collected in an optional `RoutineSetupFlow` step (multi-select chips, `TemplatesTab` opts out) and a standing **Profile editor** (`updateMovementRestrictions` action + optimistic hook + context). v1 is a pure subtractive filter, safe lifts surface because they are what remains, with no preferred-substitute front-loading (documented boundary; future hook is GQ3's `substitution_class`). Two external spec reviews folded (write-back sentinel, RLS inheritance note, name-specific exclusion + 3-day invariant tests). +8 tests (identity, name-specific exclusion, single-flag-never-empties-leg-work over 6-day and 3-day, two-flag union). Suite: typecheck clean, **887 tests**.
- **GQ3, role-aware exercise ranking** (Tier 2; 2026-06-08, `feature/gq3-role-aware-ranking`): five focused fixes to `generation.ts` that close the gap between the generator's pattern-level slot logic and how a human coach actually picks exercises, plus a small data correction. (1) **Anchor-aware fatigue sort**: for the six `COMPOUND_ANCHOR_PATTERNS` (squat / hinge / horizontal_push / vertical_push / horizontal_pull / vertical_pull) the higher-fatigue option now wins when both are fresh, the better anchor *because* it drives more adaptation (Barbell Squat over Goblet Squat, Bench Press over Push-Up), inverting GQ2's lower-fatigue-first tiebreak for anchors only; accessory slots are unaffected and fresh still beats used regardless. (2) **Substitution-class cross-session dedup**: a routine-wide `usedSubstitutionClasses` set (mirrors the `used` avoid-set) soft-deprioritizes a second exercise from a substitution class already seated elsewhere, so Romanian Deadlift on Pull no longer crowds Legs with another hinge variant when a distinct alternative (Good Morning) exists. (3) **Unilateral cap**: at most one `unilateral: true` exercise per session, a *hard* block once filled (`unilateralFilled` / `relaxedUnilateralCap`, faithfully mirroring `HEAVY_DEDUP_PATTERNS` / `heavyPatternFilled` / `relaxedHeavyCap` including the thin-pool relax-on-exhaustion fallback), so Walking Lunge no longer seats alongside Bulgarian Split Squat or Step-Up. (4) **Front Raise suppression**: a new `front_delt_isolation` substitution class (split out from `lateral_raise`, which keeps the legitimate lateral-raise family) is soft-deprioritized once a vertical press fills the session, pressing already loads the front delts, but Front Raise still surfaces when it is the only shoulder-isolation option (soft, not a hard block). (5) **Data correction** (migration `2026-06-08-gq3-exercise-data-corrections.sql`): Smith Machine Bench Press re-tagged `{machines, bench}` (was `{barbell, bench}`, leaking into barbell-only home-gym pools, a Smith machine is a fixed-rail machine, not a barbell). `ExerciseMeta` and the pool query gain `substitution_class: string | null` and `unilateral: boolean`. Two pre-existing GQ2 golden tests that asserted lower-fatigue-wins on the now-inverted `squat` anchor pattern were rewritten onto accessory patterns (`shoulder_iso` / `glute_iso`); their squat-based assertions moved to a new `GQ3: anchor patterns prefer higher fatigue` block asserting the inverted behavior. +6 tests (anchor ranking, substitution-class dedup, unilateral cap, Front Raise suppression x2, Smith Machine equipment gating). Suite: typecheck clean, **882 tests**. The "weekly structure validation" half of this item (a repair pass for malformed weekly structures) is held pending a real reproduction, not shipped here.
- Variety preference generation input (Tier 2 #4, second of three; 2026-06-07, merged #86): a `VarietyPreference` ('consistent' | 'varied') axis. Under `consistent`, a per-generation, never-persisted anchor map (`Map<MovementPattern, exerciseId>`) pins the main compound lifts (`COMPOUND_ANCHOR_PATTERNS` = squat / hinge / horizontal_push / vertical_push / horizontal_pull / vertical_pull) across sessions while accessories keep rotating; `varied` (default, null-resolved at the generation boundary) reproduces today's output byte-identical, locked by a golden identity + `consistent`-determinism test. Anchor lookup takes precedence over the avoid-set for compound slots. Fully orthogonal to training style. Persisted on `profiles.variety_preference` (nullable, mirrors `training_style`), with an optional `RoutineSetupFlow` step (`collectVariety`). Two external reviews folded (Claude.ai coaching, Perplexity architecture). Migration `2026-06-07-15-13-53-variety-preference.sql`. Loading lean (the third #4 input) deferred to its own branch.
- Training style generation input (Tier 2 #4, first of three; 2026-06-07, merged **#85**): Balanced / Strength / Bodybuilding / Powerbuilding, a `resolveBias` remap (Balanced = identity) + `resolveRepRange` (Powerbuilding heavy-pattern split), persisted on `profiles.training_style`, with a `RoutineSetupFlow` step. Migration `2026-06-07-12-58-20-training-style.sql`.
- Loading lean generation input (Tier 2 #4, third of three; 2026-06-07, merged #91): `LoadingPreference` ('barbell' / 'dumbbell' / 'machine' / 'cable') secondary-sorts `byPattern` so preferred-equipment exercises float first, while a fresh non-preferred still beats a used preferred. Persisted on `profiles.loading_lean` (nullable, migration `2026-06-07-loading-lean-preference.sql`); optional `RoutineSetupFlow` 'loading' step (toggle-to-deselect, "Skip" when nothing chosen), `TemplatesTab` opts out. null/undefined byte-identical to base. +8 tests.
- GQ2, fatigue metadata into backfill priority (Tier 2; 2026-06-07, merged #92): `fatigue?: number` on `ExerciseMeta` + the pool query, a secondary sort in `byPattern` (lower fatigue preferred within a loading class; loading lean still trumps it; fresh still beats used). Undefined = neutral midpoint (3). No migration. +4 tests.
- GQ1, exercise ordering + pattern guardrails (Tier 2; 2026-06-07): three `generation.ts` fixes, a post-select tier sort (`patternTier`), a heavy-compound dedup guard (`HEAVY_DEDUP_PATTERNS` = hinge + squat, one compound each per session, relaxes when the pool is thin), and backfill preferring uncovered slots (stable sort by coverage). No schema change. +16 tests.
- Finish / debrief screen rework (Tier 1 live-test; 2026-06-07, `feature/finish-debrief`): replaced `ShareCard` with a debrief-first `FinishDebrief`, optional session RPE (1-10) + free-text note (new nullable `session_rpe` / `session_note` columns), a deterministic coach summary from the `DecisionEvent` log / PRs / session tonnage / per-muscle volume, and real PNG export (`html-to-image`, Web-Share / download). Pulls the post-workout session rating forward from generation Phase 3. Deferred: surface RPE / note read-only in History; use RPE x duration as a session-load signal (Phase 3).
- Positioning copy (Tier 1 #3; 2026-06-06, live surfaces): login subtitle reads "An adaptive strength coach, on any device" and onboarding leads with mechanism-as-benefit (a new onboarding-only `intro` prop on `RoutineSetupFlow`); no longevity claim until validation earns it. Deferred: the public marketing hero (no landing page yet, launch floor), agreed line "An adaptive strength coach that works on any device" with the equipment framing in the sub-line, to wire when that page is built.
- Fast workout logging (ExerciseCard, set logger, RIR)
- 12-week progressive overload programme structure (phases, RIR targets)
- Rest timer (auto-start, persistent)
- Personal records (E1RM-based PR map, per-exercise), top 5 shown in Profile
- Streak tracking (consecutive weeks trained)
- Bodyweight logging + SVG chart (date picker, delete, last 30 entries)
- Goal weight, target with distance-to-go display in Profile
- Body measurements logging, date-stamped waist / hips / chest / arms entries (cm)
- Progressive overload suggestions, SetLogger pre-fills weight from previous week adjusted for RIR delta
- Exercise library (global seed + user-created, filterable by category)
- Expanded exercise category taxonomy (chest / back / shoulders / triceps / biceps / legs / glutes / calves / abs / other)
- Routine builder (create, add exercises, set starting weights, reorder, delete)
- 17 global routine templates, Full Body, Upper/Lower, PPL, Push/Pull, Bro Split, Arnold Split (DB / Home Gym / Gym variants) + Glute Focus, Lower Body, Full Body Tone (female-focused, Gym / Dumbbells)
- 6-step onboarding modal with rule-based recommendation engine (equipment → experience → goal → days/week → specific days → session time → template match)
- Templates tab in Library (equipment filter, description, one-tap clone)
- Dynamic volume scaling, sessionTime adjusts sets/exercises at clone time (~30 min / 45–60 min / 90+ min)
- Workout history (per-week session cards with set replay and PR badges)
- `workout_type` on `routine_exercises` + `routine_schedule` table (dynamic per-day workout tabs)
- Weekly schedule in onboarding (day picker, auto-fills schedule from template pattern)
- DayTabs, Mon–Sun training day selector with workout-type subtitle, today dot, logged-set badge
- Next.js App Router pages (`/pulse/train`, `/pulse/plan`, `/pulse/progress`, `/pulse/profile`, `/pulse/library`)
- Desktop layout via `DesktopLayout` component (≥ 1024 px)
- Route group `(protected)`, login page excluded from auth layout, no more redirect loop
- Progress & analytics page, StreakCalendar (12-dot weekly filled), VolumeChart (sets/week by type), E1RMChart (progression line with exercise picker + PR marker), BestLifts summary, session history with name resolution and PR badges
- Toast notification system, `ToastProvider` + `useToast` + `ToastContainer`; replaces inline `saveError` banner; auto-dismiss 4 s, hover-pause, max 3 stacked; success toasts on name/unit save
- Last session display in ExerciseCard, `computeLastSession` utility; shows "Last: 80 kg × 8 × 3 sets" below subtitle
- Notes per set, free-text per-exercise note keyed `week-routineExerciseId`; `useNotes` hook, `/api/pulse/notes` route, inline edit/clear UI in ExerciseCard
- Per-exercise rest timer override, `rest_seconds` column on `routine_exercises`; editable in LibraryView routine editor; `fireTrigger` passes duration to RestTimer
- A/B exercise variation, `variant` column on `routine_exercises` + `routine_schedule`; `WorkoutTabs` with A/B switching; `routineExercisesByTabKey` computed in provider; `nextVariant` pure function; PPL-style routines get auto-seeded A/B variants at clone time
- Workout sessions, `workout_sessions` table (start/complete timestamps); POST `/api/pulse/sessions` + PATCH `/api/pulse/sessions/:id`; `useWorkoutSession` hook; `WorkoutModeScreen` full-screen guided mode with exercise-by-exercise navigation and early-finish
- Warmup set generator, `computeWarmupSets` pure function; 3 steps at 50%×5 / 65%×3 / 80%×1; rounds to nearest 2.5 kg / 5 lb; shown above working sets in ExerciseCard when expanded; hidden below 40 kg working weight
- Workout share card, `computeShareStats` utility; originally a `ShareCard` full-screen overlay after finishing (workout label, duration, sets, week, top 3 lifts, PR badges/count, "Screenshot to share"). **Later superseded by the `FinishDebrief` rework** (see the "Finish / debrief screen rework" bullet): `ShareCard` was removed, the share image moved to an off-screen `ShareImageCard` rasterized via `html-to-image`; `computeShareStats` survives, feeding `computeSessionSummary`.
- Bug fixes sprint, Profile PRs now show exercise names (not UUIDs); Plan view sections filtered by scheduled workout types; WorkoutModeScreen shows instantly on Start Workout (session creates in background); Onboarding re-triggers whenever user has no routines
- UX polish sprint, onboarding re-triggers on /train when no routines (prominent empty-state CTA added); auto-activate another routine when active one is deleted; Dumbbell naming consistency throughout exercise library; editable default sets/reps per exercise in Library
- Desktop layout overhaul, two-column sidebar layout for ≥ 1024 px: 180px fixed sidebar (logo, week badge, nav links, streak, active routine context card, sign out) + scrollable content column with rest timer pinned to the bottom
- Slate UX redesign, full reskin to the dark "Slate" direction with a coral accent (Hanken Grotesk + Sora), three-zone desktop shell (icon rail + content + context rail) with a CSS-driven responsive split, tone-shift surfaces over borders
- Live PR detection, `isSetPR` flags a new E1RM personal record the moment a set is logged, inline coral PR badge on the set row plus a quiet toast, in ExerciseCard and WorkoutModeScreen
- Per-muscle weekly volume, `computePerMuscleVolume` derives sets per muscle category for the week from the existing taxonomy; `MuscleVolumeBars` horizontal bar list on Progress
- Plate calculator, `computePlates` pure function (barbell + loadable dumbbell, default bar/handle weights and plate set); compact affordance in SetLogger showing the per-side plate breakdown for the target weight
- Rich set types, drop sets stored in a `drops` jsonb column on `set_logs` plus a failure tag at RIR 0; drop-set editor in SetLogger, rendered in ExerciseCard, WorkoutModeScreen, and history
- Supersets, pair two routine exercises as a superset; merged card in the train screen; rest timer fires once after both exercises' sets; guided mode treats the pair as one step; Pair / Unpair in the routine editor with pair-aware reorder
- Exercise instructions, `exercise_instructions` table (read-only RLS) with primary/secondary muscles + technique cues per global exercise; on-demand `ExerciseInstructionModal` opened from a "How to perform" affordance in ExerciseCard and an info icon in the Library exercise list; ~92 exercises seeded
- Rule-based routine generation, `generation.ts` engine builds a routine from onboarding inputs: split selection by days/experience, session-time-driven volume (with floors so 30-min full body never collapses to one exercise), A/B variations for repeated focuses, exercise selection by equipment + movement pattern; `generateAndSaveRoutine` action; reusable stepped `RoutineSetupFlow` with prominent Generate entry points (Train empty state, Plan, Routines); template "Use this" opens the same flow instead of native dialogs
- Routine editor session grouping, active routine groups by the session type the schedule uses (`sessionTypeFor`): full body shows Full Body, upper/lower shows Upper/Lower (with A/B variants), never the granular push/pull/legs the exercises are tagged with
- Routine rename, inline rename per routine in the Library (`renameRoutine` action + optimistic hook)
- Collapsible desktop sidebar, left rail toggles between a 74px icon rail ("P" mark) and a 208px labelled rail (full "Pulse" wordmark); choice persists in localStorage
- Scroll-rail muscle filter, single non-wrapping horizontal rail with a per-category count badge and a fade edge, replacing the wrapped 11-chip row
- Profile polish, streak shown as a coral hero stat; login screen and skeleton loader reskinned to Slate tokens
- Auto-progression, `computeProgression` double-progression engine: climbs reps within the rep range, then adds weight and resets to the bottom; SetLogger pre-fills both weight and reps and shows the next target, deloading when a set came in harder than the target RIR
- Instant loading (phase 1 of offline-first), shell-first render; data fetched client-side via SWR with per-view Slate skeletons; user-scoped localStorage SWR cache (cleared on logout) makes warm visits instant via stale-while-revalidate
- Routine generation redesign, emphasis-based generation so repeated focuses differ by design; program-style picker (Full Body / Emphasis Days / PPL / Upper-Lower / PPL+FB / Heavy-Med-Pump / ULPPL etc., keyed by day count); cross-session avoid-set variation; rep ranges by bias + lift type (incl. pump); auto-supersets for 30-min sessions; equipment correctness (every global exercise tagged + `pull_up_bar` key); A/B variants generalized to A-D with a per-day `routine_schedule.variant` pin
- Audit cleanup, input validation, perf (targeted log writes, ExerciseCard precompute, HistoryView single pass, swrCache throttle), and DRY/maintainability (label-map collapse, TabKey comparator + `baseWorkoutType` helpers, `ui.ts` consolidation, `actions.ts`/`RoutinesTab.tsx` splits); `explore` route renamed to `library`
- Exercise preferences (hide / never-show), `user_exercise_preferences` table (RLS); `setExercisePreference` action + `/api/pulse/preferences` GET + `usePreferences` hook; `hiddenExerciseIds` on context; hide/unhide + "Show hidden" in the Library; generation filters hidden exercises out of the pool; non-destructive "Hidden" marker in the routine editor
- Mid-workout exercise swap, week-scoped `exercise_swaps` (per routine_exercise + week); same-movement-pattern candidate picker (`swapCandidates`) excluding hidden + in-session; `resolveExercise` overrides the displayed exercise for that week only (logs/PRs/volume stay slot-keyed); weight carries over via the existing slot suggestion; available from ExerciseCard, guided mode, and reflected in history
- Routine generation explainability, generated routines persist a human-readable `rationale` (built from the onboarding inputs + the style's `bestFor`); shown as a "Why this plan" line on the Plan screen and previewed live in the setup flow's final step
- Weekly per-muscle volume targets, goal-agnostic `VOLUME_TARGETS` set-range table + pure `computeVolumeProgress`; the Progress per-muscle bars show progress toward each target with "N sets to go" nudges and a lagging-muscle summary
- Recomp dashboard, a Progress headline card combining strength (summed best E1RM/week), bodyweight, and waist trends into a plain-language verdict ("You're recomping…", "Gaining…", "Keep logging…") with per-signal deltas; also adds the body-measurements read path (previously write-only)
- Offline-first logging (phase 2 of offline-first), IndexedDB offline write queue for set logs + notes: writes enqueue when offline or on network failure (optimistic SWR mutate keeps the UI live), flushed FIFO on reconnect / focus / mount with last-write-wins; pending-sync indicator on the train screen; installable PWA web manifest scoped to `/pulse`; `/pulse`-scoped service worker (navigation network-first with cached-shell fallback, static assets cache-first, skips `/api`, cleans old caches on activate); CSP gains `worker-src` + `manifest-src 'self'`
- Gender in profile, nullable `gender` column on `profiles` (male/female); Gender toggle in Profile + an optional onboarding step (skipped once set); `recommendStyle(count, gender)` lightly biases the default program style for female users (4-day → Aesthetic Upper/Lower, 3-day → Full Body Emphasis Days); also feeds the Strength Score. Deeper gender-specific volume/emphasis tuning is deferred to the Muscle priority item
- Strength Score, 0-100 relative-strength composite (`computeStrengthScore` in `strength.ts`): each main lift's best E1RM ÷ latest bodyweight is scored against gender-specific bodyweight-multiple standards (bench / squat / deadlift / OHP) via piecewise-linear interpolation across Beginner→Elite, averaged to one headline number with a level label and per-lift breakdown; `StrengthScoreCard` headline on Progress, with a CTA when gender / bodyweight / a main lift is missing
- Recovery-aware volume nudges, `computeRecoveryFlags` pairs each muscle's weekly working-set count with its average RIR to classify under (below floor), high fatigue (in range, avg RIR ≤ 0.5), over target (above ceiling), or on track; surfaced as a dedicated `RecoveryCard` on Progress that triages the muscles needing attention first (originally inline chips on the volume-by-muscle bars; no change when no targets/recovery passed)
- Rest timer auto-advance, guided mode (`WorkoutModeScreen`) now shows a visible rest timer and, with the Profile "Auto-advance rest timer" toggle on, jumps to the next exercise when rest ends and the current exercise is fully logged; the global pinned timer is suppressed while guided mode is open so only one timer runs (`RestTimer` gains an `onComplete` callback, `workoutModeOpen` + `autoAdvance` on context)
- Progress dashboard re-tier, feature-placement pass after the screen had grown three competing headlines plus the full log: Strength Score + the promoted `RecoveryCard` lead in a two-up row, then Recomp, then the training-trends grid (volume, e1RM, volume-by-muscle, streak, Best Lifts), with Personal Records moved over from Profile; Session History collapses to the last 4 sessions (most recent first) behind a "Show all N" toggle. Same component renders dense single-column on mobile. Layout-only; all `HistoryView` data memos preserved
- Body measurement units (cm / in), `length_unit` ('cm' | 'in') preference on `profiles` mirroring the weight `unit`; `toLengthDisplay`/`toCm` helpers convert for display and parse inputs back to canonical cm (no data migration). A compact cm/in toggle in the Profile measurements header recalculates in real time, alongside a new latest-measurements readout (waist / hips / chest / arms, previously write-only) and inch-aware inputs; the Recomp waist delta honours the unit too. Profile is slimmed to identity + settings on the left and one unified Body block (bodyweight, goal, measurements) on the right, with PRs and the streak hero removed (now on Progress)
- Recovery detail chips, `computeRecoveryFlags` now returns a `RecoveryDetail` (status plus sets, avg RIR, target band, sets-to-go); the Progress `RecoveryCard` surfaces per-muscle chips with the detail string ("high fatigue · RIR 0.4", "add volume · N to go", "over target · N sets", "on track") and token-based tints (coral for fatigue, green for on track)
- Progress time window, Week / Cycle / All segmented control in the Progress header; a windowed-logs filter scopes volume, e1RM, the recomp trend, and Session History. Strength Score and Personal Records stay all-time (lifetime records, not trends). `cycle` = weeks 1–12 (default, matches prior behaviour), `all` = every logged week, `week` = the active week; cycle and all are distinct in code and coincide until data spans multiple cycles
- Clearer nav icons, Train house → dumbbell, Plan menu-lines → calendar, Library magnifier → stacked books, swapped in both `BottomNav` and the desktop rail; Progress (bar chart) and Profile (person) kept as-is
- Profile Body trend chips, a "feeds Recomp" badge on the Body block plus bodyweight and waist trend chips (the delta of the two most recent entries, `pulse-success` when trending down, `pulse-dim` when up)
- Shared `PageTitle`, one heading component (matching the Progress header) used on Progress, Profile, Plan, and Library so every view except Train opens with the same title treatment
- Train layout consolidation, the four stacked top strips (week/phase/RIR metadata, the day/workout tabs, the 12-week dot strip, the detached Start button) fold into a single context-bar card: metadata + a compact `‹ Wk N ›` week stepper + Start on top, the day/workout tabs below. The expanded `ExerciseCard` groups its secondary actions (How to perform / Swap / Note) into one chip-style meta row instead of stacked links. Layout only; all wiring preserved
- Plan restructure, a `PageTitle` + Generate header, then a single program-header card (phase + week notes + rationale, with an inline week stepper) replacing the separate phase card / WeekSelector / "Why this plan" blocks. Below it, a full-width column: a card holding the weekly schedule + a taller, two-label weekly-volume chart whose bars are clickable to jump to that week (`setActiveWeek`), then the per-session exercise breakdown
- Library unification, one shared toolbar and one row-card style across the Exercises, Routines, and Templates tabs, plus a `PageTitle`, so the three tabs read as one library. A shared `FilterChips` rail (Exercises categories + Templates equipment) stays a single row and scrolls, swipe/trackpad on touch, mouse-wheel-to-horizontal on desktop, so every chip is reachable without stacking into many rows on narrow screens
- Progress trends as cards, the trends grid (volume, e1RM, volume-by-muscle, streak, Best Lifts, Personal Records) is wrapped in `bg-pulse-surface` cards on a tighter gap, matching the card idiom the redesigned screens use
- Muscle priority + gender-aware generation, persistent `priority_muscle` on `profiles` (`glutes | legs | chest | back | shoulders | arms | balanced`, nullable). The generator tilts each session's emphasis toward the priority's movement patterns (front-loading them where the session already trains that muscle, never injecting into unrelated days) via a pure `tiltEmphasis`; the Progress weekly volume targets bump for that muscle (`priorityAdjustedTargets`) so the recovery nudges stay coherent. Gender now only seeds the default (female → glutes) via `genderDefault`, applied server-side at generation when no explicit choice exists; the old female-only `recommendStyle` bias is retired. Editable as a "Training priority" setting in Profile. (Deferred: an explicit priority step inside the generation flow, the Profile control + server-side seeding cover the default.)
- Template metadata + Templates-tab filters, `routine_templates` gain `goal` (`build_muscle | lose_fat | general_fitness`) and `gender_fit` (`any | female`) columns, seeded across the 17 templates (Full Body → general fitness, Full Body Tone → lose fat + female, Glute Focus → female, rest build-muscle / any). The Templates tab gains a shared filter model (`templateFilters.ts`, pure + unit-tested): Goal and Equipment as always-visible `FilterChips` rails, with Experience / Days-per-week / a "For you" gender-fit toggle behind a Filters expander (active-count badge); all AND-combined. "For you" keeps gender-neutral templates plus ones matching the user's gender
- CSV data export, a pure, unit-tested `buildWorkoutCsv` produces one row per logged set (Week, Exercise, Set, Weight (kg), Reps, RIR, PR, Drop sets) with RFC-4180 escaping; weights stay in canonical kg. The export moved up to `PulseProvider` so it can resolve exercise names (honouring week-scoped swaps) and PR flags, replacing the old nameless JSON dump. Its button moved out of the mobile header into a "Data" section in Profile, reachable on every device
- Accent colour selection, users can pick their accent from a preset palette (Coral default, Emerald, Sky, Violet, Amber, Rose) in Profile. The choice persists on `profiles.accent_color` (a preset key) and is applied by overriding the `--color-pulse-accent` / `--color-pulse-accent-dim` tokens at the document root, so it cascades to every accent usage (Tailwind classes + `var()`). `ACCENT_PRESETS` is one constant; null = the coral default. No new component beyond a swatch row.
- Program start-day selection + false-"behind" fix, fixed the adherence over-count where a session scheduled for *today* counted as overdue before the day passed (`computeProgramPosition` now counts only days strictly before today, matching `computeWeekAdherence`), so a mid-week start no longer reads as "behind". Added a user-settable program start date in Plan (a date input + "Today"), via a `setProgramAnchor` action/hook/context that updates `program_anchor`. The anchor drives only the calendar overlay, not completion-paced progression, so changing it never alters logged data. No migration.
- Onboarding day-count fix, the "which days" picker now caps selection at the chosen days-per-week (`MAX_TRAINING_DAYS`: 2–3→3, 4→4, 5–6→6), so picking 6 day-slots after answering "4 days" can no longer resolve a 6-session style. Stepping back to a lower frequency trims the over-selection. Fixes a routine-generation mismatch for new users.
- Auto-applied deload for stalled lifts, the plateau nudge becomes actionable instead of advisory. When a lift stalls (no e1RM gain in ~3 weeks via `computePlateau`) and isn't already rebuilding, its next set targets auto-deload: `SetLogger` prefills ~90% of the previous weight (rounded to the 2.5 kg grid) with reps reset to the bottom of the range, shown as a `↓ deload target`. Self-limiting via `recentDrop` (a ≥3% e1RM dip in the last ~3 weeks suppresses re-deloading), so it deloads once then lets the lift rebuild. Fully derived from logs (`shouldDeload`/`deloadTarget` in utils.ts), no new storage, action, or migration. The ExerciseCard "Stalled" card explains it. (Deferred: guided WorkoutModeScreen + SupersetCard deload prefill, a follow-up; the workout-mode redesign was visual-only.)
- Adaptive missed-workout regeneration, the program becomes **calendar-aware and completion-paced**. A routine gets a `program_anchor` (set on its first completed session; existing routines backfilled), and the user's `timezone` is captured silently. A pure engine (`adherence.ts`) attributes completed `workout_sessions` to program weeks ordinally, so missing a session just slides the timeline forward instead of forcing a restart; the calendar layer flags when you're behind or have lapsed. On Train, a non-destructive nudge (same pattern as the plateau nudge) offers a **ramp-back week** after a gap of `GAP_DAYS` (10), reduced volume + easier RIR, *inserted* so no real progression week is lost, or points you at a still-open session when you're just behind. Accept/dismiss persist in an append-only `program_adjustments` table (one decision per week, atomic upsert). The week stepper now defaults to and follows the completion-paced current week (with a jump-to-current control), and a fixed `clampWeek`-at-12 bug that broke the repeating-block / 16-week designs was corrected. All engine logic unit-tested; server-portable for a future adherence cron. (Deferred: multi-tier ramp-back for very long layoffs; push/email nudges; balance-aware re-sequencing.)
- Dynamic periodized programs, the static 12-week block becomes a **repeating block of a user-chosen length** (8/10/12/16; `program_weeks` on the routine, default 12). A pure `buildProgram(weeks)` defines each block (12 = the legacy tables); `getPhase`/`getRIR`/`volumeForWeek` are block-length-aware and wrap (`weekInBlock`) so the program repeats indefinitely, week 13 of a 12-week block = block 2 week 1, deloading at each block end. Plan gets a Program-length picker and reads the routine's block for the phase card / volume chart / week stepper; Train's header RIR + stepper and `SetLogger`'s target RIR follow the same length; the week steppers no longer cap (you advance into the next block). Stage 2: `computePlateau` flags a main lift with no e1RM gain over ~3 weeks and surfaces a non-destructive nudge on the exercise (suggested deload/swap, reusing the existing swap path). All pure logic unit-tested. (Deferred: strength-calibration test week; auto-applied deload that rewrites next week's targets, currently advisory.)
- Train header + day-strip redesign, the `/train` program status card and weekly day selector rebuilt in the bold "Focus" direction (frontend-design). A Big Shoulders Display week block (`01 /16` + phase · target RIR) replaces the text status line, with a gradient Start CTA derived from the themeable accent via `color-mix`. `DayTabs` becomes an elevated 7-day rail: per-day workout label plus a thin progress bar (track + accent fill + `done/total`), coral active day, dashed rest days, today dot. Added the self-hosted `Big Shoulders Display` display font (`--font-pulse-display`, no CSP change). All adaptive-regen chips (status, jump-to-current, ramp-back card) preserved.
- WorkoutModeScreen redesign, the guided workout screen rebuilt responsively (frontend-design). **Mobile = "Focus"**: bold Big-Shoulders exercise name + set-progress ring and per-step progress pips that fill to each step's real done/total. **Desktop = two-pane**: a left focus pane (hero + set rows) and a right 392px "This session" sidebar with overall pips, a clickable whole-session exercise track (progress spine, jump-to-step), and a docked rest timer + sets-logged / sets-left stats + actions. Set rows use an "editorial" `SetLogger` variant (hairline rows, "Set N" label, big display value) scoped to guided mode so the Train cards stay untouched; the rest bar stays `RestTimer`. Each exercise carries How-to-perform + Swap + Note (parity with the Train card), and primary actions use the app's standard coral button. Exactly one layout mounts via `useMediaQuery` (hardened against a missing `matchMedia`), keeping a single DOM and the full behavioural contract (auto-advance, superset-round gating, PR toast, mid-session swap, step clamp unchanged). Shipped after two adversarial review passes (AA-contrast, accessible naming) plus live-feedback polish. (Deferred: a one-active-set focus that dims not-started rows; the mockup's green "done" token, intentionally kept coral to follow the themeable accent. Guided-mode deload prefill shipped 2026-06-13 on `feature/coach-legibility-quick-wins`.)
- Offline-queue cross-user safety, queued offline writes (set logs + notes) now carry the owning `userId`; `flushQueue(userId)` replays only the current session's writes and leaves another user's queued writes untouched (never replayed under the wrong account, never dropped), so a shared device can no longer land one user's offline writes in another's account on the next sign-in. Sign-out drains the current user's queue while still authenticated, then clears the per-user SWR cache. Pre-public hardening alongside the live RLS audit (all user-data tables verified owner-scoped in prod). Unit-tested in `offlineSync`.
- Decimal comma input, weight inputs accept "2,5" as well as "2.5". A pure `parseDecimalInput` normalizes the comma before parsing (replacing bare `parseFloat`, which truncated "2,5" to 2 and silently corrupted the logged weight for European/NL keyboards), and the weight/reps inputs gain `inputMode="decimal"`/`"numeric"` for the correct mobile keypad. Pre-public input-correctness fix; unit-tested.
- Rest timer survives phone lock, the rest countdown is anchored to a wall-clock end time and recomputed on `visibilitychange`/`focus`, instead of a per-second `setTimeout` decrement that pauses when mobile suspends background timers between sets. The timer and its completion beep now stay accurate after the phone locks. Core-loop reliability fix; unit-tested (incl. a suspended-tab resume case).
- Conversational coaching copy, the everyday progression line now reads like a coach instead of a glyph. The Train card shows `Last 90 kg × 8` then `Go 95 kg × 8` (deload: `Back off to ...`); guided mode shows a spoken sentence ("Last time you hit 90 kg × 7. Go for 95 kg × 8." and a deliberate-backoff sentence on a deload). RIR stays on its own chip so the words never repeat it. The Plan "Why this plan" rationale now also names the muscle each session tilts toward (`buildRationale` takes the resolved priority), the one adaptation that was previously silent. Copy-only on the surfaces; aria-labels, logic, and tests preserved.
- Generation v2 Phase 0 #1, `muscleMap.ts`: the canonical weighted MovementPattern → muscle bridge (`PATTERN_MUSCLE_MAP`, the frozen §1 table, 15 patterns → the 10 categories, weights sum to 1.0) plus `muscleContributions` / `primaryMuscle`. Pure module, no migration, golden-tested (locks the frozen weights and the squat-vs-hinge glute-proportion invariant). Granularity decision A (keep the 10 categories; the 15 patterns stay the generation control layer). Spec: `docs/superpowers/plans/2026-06-06-09-48-10-muscle-map-phase0-1.md`.
- Generation v2 Phase 0 #2, exercise metadata + seed: six new columns on `exercises` (`secondary_muscles`, `unilateral`, `fatigue_cost`, `joint_stress`, `difficulty`, `substitution_class`) seeded for all 94 global exercises (`2026-06-06-11-28-49-exercise-metadata-fields-seed.sql`, idempotent `add column if not exists` + a fail-loud `do $` guard). Prerequisite: ~12 old ILIKE-seeded `movement_pattern` mis-tags corrected first (`2026-06-06-10-51-33-movement-pattern-correction.sql`) so the bridge attributes to the right muscles, with the three vocab-gap judgment calls (Leg Curl / Leg Extension / Rack Pull) science-validated.
- Generation v2 Phase 0, fractional per-muscle volume wiring: the muscle bridge now feeds the live Progress bars. `secondarySets` buckets each pattern's non-primary weights (≥0.20 → 0.5 set, 0.10–0.19 → 0.25, below dropped, excluding the exercise's own category to avoid double-count), and `accumulatePerMuscle` credits every logged set its primary 1.0 plus those fractions, wired through `computePerMuscleVolume`, `computeRecoveryFlags` (RIR averaged per touched category), and `historyBundle.ts` (the real Progress production path; `queries.ts` now selects `movement_pattern`). `VOLUME_TARGETS` retuned upward for the new secondary inflow (arms/shoulders most). UI rounds to half-sets (`roundSets`). Golden-tested in `muscleMap`/`utils`/`historyBundle`; on `feature/fractional-volume` pending review (changes visible Progress numbers).
- Guided set-row polish (Tier 1 #2, Editorial draft C, guided-only). Single-active focus via a new `active` prop on `SetLogger` (default true, so the Train `card` variant is untouched): `WorkoutModeScreen` marks the first unsaved set active and the rest render as dimmed "not started" previews. Inputs regrouped (balanced Weight/Reps boxes + Save in-row), logged row decluttered to value · PR · RIR · ✓ with Edit/delete de-emphasized (brighten on hover/focus, kept visible for touch), and the plate button dropped from the editorial logged row. Opportunistic a11y Q8: both guided overlay roots gained `role="dialog"` + `aria-modal` + label. Behavioural-contract tests stayed green; +2 tests for the active/idle behavior. 775 tests; on `feature/guided-set-row-polish`.
- Per-muscle volume in the desktop right rail (Tier 1 #1, completes the item). `RailMuscleVolume` fills the previously-sparse rail with this week's per-muscle volume vs target: all 9 targeted muscles, lagging-first, a thin bar filling toward the ceiling with a tick at the weekly floor, turning success-green once the floor is hit ("This week" heading). Reuses `computePerMuscleVolume(logs, activeRoutine.exercises, currentWeek)` + the priority-tilted `VOLUME_TARGETS` + `computeVolumeProgress`, so the numbers match the Progress view exactly; desktop-only (the rail does not exist on mobile). Design draft `docs/superpowers/designs/2026-06-06-17-10-50-rail-per-muscle-volume-draft.html`; 773 tests; on `feature/rail-per-muscle-volume`.
- Exercise-instruction content review, populated the 5 global exercises that had no instructions (Dumbbell Pullover, Dumbbell Shrug, Decline Dumbbell Press, Single-Leg Glute Bridge, Dumbbell Push Press) and corrected 9 existing entries: Deadlift / Rack Pull primaries relabeled to the posterior chain (Glutes, Hamstrings, Lower Back) with an explanatory lat-bracing cue, Plank glutes demoted to secondary, the Romanian Deadlifts gained Lower Back, and Chest removed from the four tricep-extension entries (it does not extend the elbow). Added one new display label, `Lower Back` (instruction muscle columns are free-text, rendered as plain chips, so no code change). Content from the Claude.ai science review, reconciled against code-truth (names, no em dashes, render-safety) and shipped as `2026-06-06-16-56-42-exercise-instructions-review.sql` (applied). Reusable coverage query at `docs/queries/exercise-instructions-review.sql`.
- Em-dash sweep, removed em dashes from the live application and maintained docs (`src/**` 114 occurrences, `CLAUDE.md`, `docs/roadmap.md`) per the no-em-dash copy rule. Spaced ` — ` became `, ` (template-name separators became ` - `); the lone `—` empty-value placeholder (a "no data" cell) was deliberately kept. Supabase: a content-cleanup migration (`2026-06-06-16-32-34-em-dash-content-cleanup.sql`) fixes the seeded `routine_templates` names/descriptions + `exercise_instructions.cues` (apply manually; en dashes in ranges like `45–75°` left alone), and `docs/queries/exercise-instructions-review.sql` is a reusable query to review instruction coverage + discover remaining em dashes. The historical `docs/superpowers/**` + `docs/audits/**` archives were intentionally left. On `feature/em-dash-sweep`; 771 tests green.
- Ramp-back / lighten copy honesty fix, the ramp-back nudge, the Train week banner, and the `decisionCopy` ramp_back/manual strings promised "reduced volume," but the volume cut is unconsumed (only the `+1 RIR` is felt). Reworded everywhere to promise only the easier RIR; the inert `RAMPBACK_VOLUME_FACTOR` is now flagged in `constants.ts` and the real volume-cut work is tracked as Tier 2 #7. Surfaced by the dose reconciliation with Claude AI; on `feature/ramp-back-copy-fix`.
- Lighten this week (Tier 1 #11) + priority-tilt why line (Tier 1 #1, non-visual half), autonomous batch on `feature/tier1-quick-wins`. **Lighten:** `manual_deload` `AdjustmentKind` + `manual` `DecisionTrigger`; `progressionInfo` lightens the week (`isRampBack`) without the re-entry offset (one-off easier week, progression continues); `lightenThisWeek` action writes the adjustment + dual-writes a `ramp_back`/`manual` decision event (so it lands in the Coach Timeline with distinct copy); kind-aware Train banner + a "Go easier this week" button; migration widens the `program_adjustments.kind` CHECK. **Priority why line:** pure `priorityFocusLine(priority)` caption above the per-muscle volume bars on Progress. Spec `docs/superpowers/specs/2026-06-06-15-43-44-...md`; migration `2026-06-06-15-58-32-manual-deload-adjustment-kind.sql`; 771 tests; second-opinion code review caught the (now-added) migration as a blocker. Deferred: manual-deload undo, button busy-state (idempotent + optimistic unmount make it low-risk).
- Coach Decision Timeline (Tier 3 #10, increment 1), first surfacing of the `DecisionEvent` log. Read path `loadDecisionEvents` → `/api/pulse/decisions` → `useDecisionEvents` → context `decisions`. Pure `decisionCopy(event, name)` maps each type to what-changed / why / what-next copy (progression branches weight-advance vs rep-advance off the stored magnitude); `groupDecisionsByWeek` for the headers. `CoachPanel` renders a compact card (desktop right rail as a plain block; Train inline as a surface card, `lg:hidden` so it doesn't double with the rail) and a full week-grouped overlay (`role="dialog"`, aria-modal, Escape + backdrop close). Progression uses the existing `pulse-success` token, others the themeable accent. `acceptReentryDeload` now also stamps `daysAway` into the ramp-back event magnitude so the copy can say "11 days since your last session". Unit-tested: `decisionCopy`/`groupDecisionsByWeek` (8) + `CoachPanel` (4). Draft `docs/superpowers/designs/2026-06-06-15-08-34-coach-decision-timeline-draft.html`; 759 tests; on `feature/coach-decision-timeline`.
- Generation v2 Phase 0 #3, session-linked logging + unified `DecisionEvent` log (closes Phase 0): `set_logs` gains `session_id` (nullable FK → `workout_sessions`, `on delete set null`) + `workout_date` (user-local day, computed client-side from `profile.timezone` and stamped at save so an offline write keeps the day it happened, not the day it flushes, threaded through `updateLog` → `runMutation` → `upsertLog` positionally, back-compatible with pre-change queued writes). New `decision_events` table (`type` ramp_back/deload/progression/swap, `trigger`, `affected_area` `''`-sentinel for program-wide, `week`, `magnitude` jsonb, `confidence`, idempotent `unique(user,routine,type,affected_area,week)`, owner RLS). Pure `decisionForExercise` (utils) mirrors what `ExerciseCard`/`SetLogger` already compute, deload wins over progression, progression only on a genuine advance, and the provider derives + fires it at the **set-save choke point** (`logSet`, success-gated dedupe ref), so components are untouched and it stays best-effort/re-derivable (not queued). Ramp-back dual-written from `acceptReentryDeload`. `recordDecisionEvent` action validates via `validateDecisionEvent` + `assertOwnsRoutine`. Migration `2026-06-06-14-30-53-session-linked-logs-decision-events.sql`; 747 tests; on `feature/decision-events-phase0`.
